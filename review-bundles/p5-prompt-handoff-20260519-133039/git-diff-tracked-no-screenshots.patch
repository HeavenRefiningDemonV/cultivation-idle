diff --git a/docs/release/build_warning_inventory.md b/docs/release/build_warning_inventory.md
index 3185e003..7434c5b6 100644
--- a/docs/release/build_warning_inventory.md
+++ b/docs/release/build_warning_inventory.md
@@ -1,10 +1,12 @@
 # Build Warning Inventory (Packet 7.4a)
 
-- Generated: 2026-05-18T20:09:12.821Z
-- Build passed: false
-- Blockers: 1
-- Warnings: 0
+- Generated: 2026-05-19T10:12:28.411Z
+- Build passed: true
+- Blockers: 0
+- Warnings: 3
 
 | id | level | source family | summary | disposition | owner | notes |
 | --- | --- | --- | --- | --- | --- | --- |
-| build_command_failed_nonzero | blocker | unknown | Build command exited non-zero without a classified blocker line. | fix_now | release_engineering | Resolve build launcher/build command failure before RC sign-off. |
\ No newline at end of file
+| build_1_browserslist_browsers_data_caniuse_lite_is_6_mon | warning | unknown | Browserslist: browsers data (caniuse-lite) is 6 months old. Please run: | post_semester_debt | release_engineering | Track and triage for follow-up packet. |
+| build_2_assets_background_insidedungeon_png_referenced_i | warning | unknown | ../../assets/background/InsideDungeon.png referenced in ../../assets/background/InsideDungeon.png didn't resolve at build time, it will remain unchanged to be resolved at runtime | post_semester_debt | release_engineering | Track and triage for follow-up packet. |
+| build_3_some_chunks_are_larger_than_500_kb_after_minific | warning | unknown | (!) Some chunks are larger than 500 kB after minification. Consider: | post_semester_debt | release_engineering | Track and triage for follow-up packet. |
\ No newline at end of file
diff --git a/docs/release/current_implementation_baseline.json b/docs/release/current_implementation_baseline.json
index 13f12f0c..ae815a9e 100644
--- a/docs/release/current_implementation_baseline.json
+++ b/docs/release/current_implementation_baseline.json
@@ -1,6 +1,6 @@
 {
   "schemaVersion": "implementation-baseline-v1",
-  "generatedAt": "2026-05-18T20:10:23.825Z",
+  "generatedAt": "2026-05-18T22:01:41.112Z",
   "cwd": "C:\\Users\\abdul\\Desktop\\cultivation-idle",
   "packageName": "cultivation-idle",
   "packageVersion": "0.0.0",
@@ -13,23 +13,23 @@
       "M docs/release/build_warning_inventory.md",
       " M docs/release/current_implementation_baseline.json",
       " M docs/release/current_implementation_baseline.md",
-      " M docs/release/go_no_go_checklist.md",
       " M docs/release/known_issues.md",
-      " M docs/release/release_handoff_bundle.md",
       " M docs/release/runtime_content_manifest.md",
-      " M docs/release/signoff_sheet.md",
-      " M src/components/modals/WorldBuildingModal.tsx",
-      " M src/components/screens/CityMapHub.scss",
-      " M src/components/screens/CityMapHub.tsx",
-      " M src/components/screens/ManualPavilionPanel.tsx",
-      " M src/components/screens/WorldScreen.tsx",
-      " M src/features/apothecary/exact/buildApothecaryExactSurface.ts",
-      " M src/features/professions/forgeExact/buildForgeExactSurface.ts",
-      " M src/features/techniquesExact/TechniquesExactScreen.scss",
-      " M src/features/techniquesExact/TechniquesExactScreen.tsx",
-      " M src/features/techniquesExact/TechniquesScreenOwner.tsx",
-      " M src/features/techniquesExact/buildTechniquesExactSurface.ts",
-      " M src/features/world/bountiesExact/buildBountiesExactSurface.ts"
+      " M package.json",
+      " M src/components/GameLayout.tsx",
+      " M src/components/modals/LifeSummaryModal.tsx",
+      " M src/components/modals/OfflineProgressModal.scss",
+      " M src/components/modals/OfflineProgressModal.tsx",
+      " M src/features/prestige/lifeSummarySurface.ts",
+      " M src/features/prestige/prestigeAdvisorSurface.ts",
+      " M src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss",
+      " M src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx",
+      " M src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx",
+      " M src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts",
+      " M src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts",
+      " M src/services/prestige/PrestigeResetService.ts",
+      " M src/services/time/OfflineCatchup.ts",
+      " M src/stores/prestigeStore.ts"
     ]
   },
   "checks": [
@@ -86,7 +86,7 @@
       "id": "test_sources",
       "title": "Test sources",
       "status": "pass",
-      "summary": "608 TypeScript test source files found under tests/.",
+      "summary": "615 TypeScript test source files found under tests/.",
       "evidence": [
         "tests/**/*.ts"
       ]
diff --git a/docs/release/current_implementation_baseline.md b/docs/release/current_implementation_baseline.md
index 5b0418e1..a0812546 100644
--- a/docs/release/current_implementation_baseline.md
+++ b/docs/release/current_implementation_baseline.md
@@ -1,6 +1,6 @@
 # Current Implementation Baseline
 
-Generated: 2026-05-18T20:10:23.825Z
+Generated: 2026-05-18T22:01:41.112Z
 CWD: `C:\Users\abdul\Desktop\cultivation-idle`
 Package: cultivation-idle 0.0.0
 Node: v24.14.0
@@ -12,23 +12,23 @@ Git dirty: yes
 - `M docs/release/build_warning_inventory.md`
 - ` M docs/release/current_implementation_baseline.json`
 - ` M docs/release/current_implementation_baseline.md`
-- ` M docs/release/go_no_go_checklist.md`
 - ` M docs/release/known_issues.md`
-- ` M docs/release/release_handoff_bundle.md`
 - ` M docs/release/runtime_content_manifest.md`
-- ` M docs/release/signoff_sheet.md`
-- ` M src/components/modals/WorldBuildingModal.tsx`
-- ` M src/components/screens/CityMapHub.scss`
-- ` M src/components/screens/CityMapHub.tsx`
-- ` M src/components/screens/ManualPavilionPanel.tsx`
-- ` M src/components/screens/WorldScreen.tsx`
-- ` M src/features/apothecary/exact/buildApothecaryExactSurface.ts`
-- ` M src/features/professions/forgeExact/buildForgeExactSurface.ts`
-- ` M src/features/techniquesExact/TechniquesExactScreen.scss`
-- ` M src/features/techniquesExact/TechniquesExactScreen.tsx`
-- ` M src/features/techniquesExact/TechniquesScreenOwner.tsx`
-- ` M src/features/techniquesExact/buildTechniquesExactSurface.ts`
-- ` M src/features/world/bountiesExact/buildBountiesExactSurface.ts`
+- ` M package.json`
+- ` M src/components/GameLayout.tsx`
+- ` M src/components/modals/LifeSummaryModal.tsx`
+- ` M src/components/modals/OfflineProgressModal.scss`
+- ` M src/components/modals/OfflineProgressModal.tsx`
+- ` M src/features/prestige/lifeSummarySurface.ts`
+- ` M src/features/prestige/prestigeAdvisorSurface.ts`
+- ` M src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss`
+- ` M src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx`
+- ` M src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx`
+- ` M src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts`
+- ` M src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts`
+- ` M src/services/prestige/PrestigeResetService.ts`
+- ` M src/services/time/OfflineCatchup.ts`
+- ` M src/stores/prestigeStore.ts`
 
 ## Checks
 | Check | Status | Summary |
@@ -38,7 +38,7 @@ Git dirty: yes
 | Lockfile | PASS | package-lock.json is present. |
 | Local vendor dependencies | PASS | vendor directory is present. |
 | Runtime content manifest | PASS | All runtime content files required by the manifest are present and non-empty. |
-| Test sources | PASS | 608 TypeScript test source files found under tests/. |
+| Test sources | PASS | 615 TypeScript test source files found under tests/. |
 | Compiled tmp-tests | INFO | tmp-tests directory is present. |
 | Test tsconfig files | PASS | Checks for tsconfig.tests.json and tsconfig.progression-fixtures.json. |
 | Key package scripts | PASS | All 9 expected scripts are present. |
diff --git a/docs/release/go_no_go_checklist.md b/docs/release/go_no_go_checklist.md
index 8c748c5f..1b72ecea 100644
--- a/docs/release/go_no_go_checklist.md
+++ b/docs/release/go_no_go_checklist.md
@@ -1,6 +1,6 @@
 # Go / No-Go Checklist
 
-- generatedAt: 2026-05-18T20:05:08.954Z
+- generatedAt: 2026-05-19T10:16:16.865Z
 - release gate status: NO_GO (gate headline: NO_GO)
 - cleanPass: false
 - acceptedWaivers: 0
diff --git a/docs/release/known_issues.md b/docs/release/known_issues.md
index c2fc2714..b6e21aae 100644
--- a/docs/release/known_issues.md
+++ b/docs/release/known_issues.md
@@ -1,8 +1,15 @@
 # Known Issues Ledger
 
-- Generated: 2026-05-18T20:03:49.531Z
+- Generated: 2026-05-19T10:17:30.038Z
 - Release gate headline: NO_GO
 
+## P4 closeout release-gate blockers
+- [fresh_run_acceptance] fresh_run_manual_pending: Required manual fresh-run coverage is incomplete for normal, cautious, and aggressive routes. Classification: non-P4 manual coverage gap.
+- [balance_regression] balance_regression_command_failed: `npm run balance:report:json` returned non-zero. Classification: non-P4 balance/report debt.
+- [route_comparison] adapter_exception: route comparison still throws `Cannot read properties of undefined (reading 'toLowerCase')`. Classification: non-P4 route-report adapter debt.
+- [runtime_diagnostics] runtime_diagnostics_errors: runtime diagnostics reported five error-level findings in diagnostic scenarios. Classification: non-P4 diagnostics/harness debt.
+- [full_test_suite] test_suite_failed: broad `npm run test` still fails across pre-existing exact-screen, economy, content, forge/apothecary, P6 visual, and legacy life-summary contract tests. Classification: broad non-P4 suite debt; focused P4 tests pass.
+
 ## Open blockers
 - None
 
diff --git a/docs/release/release_handoff_bundle.md b/docs/release/release_handoff_bundle.md
index 373aef10..39723e92 100644
--- a/docs/release/release_handoff_bundle.md
+++ b/docs/release/release_handoff_bundle.md
@@ -14,7 +14,7 @@
 - binary decision: NO_GO
 - cleanPass: false
 - acceptedWaivers: 0
-- unresolvedBlockers: 6
+- unresolvedBlockers: 5
 - pendingManual: 1
 
 ## Command map
diff --git a/docs/release/runtime_content_manifest.md b/docs/release/runtime_content_manifest.md
index 9f9c51b2..07a36528 100644
--- a/docs/release/runtime_content_manifest.md
+++ b/docs/release/runtime_content_manifest.md
@@ -1,6 +1,6 @@
 # Runtime Content Manifest
 
-Generated: 2026-05-18T20:10:23.498Z
+Generated: 2026-05-19T09:56:01.850Z
 
 ## Result
 PASS
diff --git a/docs/release/signoff_sheet.md b/docs/release/signoff_sheet.md
index 7724b34d..10dd67cd 100644
--- a/docs/release/signoff_sheet.md
+++ b/docs/release/signoff_sheet.md
@@ -1,11 +1,11 @@
 # Release Sign-off Sheet
 
 ## Release candidate identity
-- generatedAt: 2026-05-18T20:05:08.954Z
+- generatedAt: 2026-05-19T10:16:16.865Z
 - releaseDecision: NO_GO
 - cleanPass: false
 - acceptedWaiverCount: 0
-- unresolvedBlockerCount: 6
+- unresolvedBlockerCount: 5
 - pendingManualCount: 1
 - version: unknown
 - buildId: unknown
@@ -24,7 +24,7 @@
 ## Evidence summary: Build / Content / Test
 | check id | status | evidence command/doc | summary | blockers | warnings |
 | --- | --- | --- | --- | --- | --- |
-| `build_audit` | fail | `npm run release:build-audit:json` | build_audit failed with blocker findings. | 1 | 0 |
+| `build_audit` | warning | `npm run release:build-audit:json` | build_audit completed with warnings. | 0 | 3 |
 | `content_validation` | pass | `npm run validate:content` | Content validation passed. | 0 | 0 |
 | `full_test_suite` | fail | `npm run test` | Full test suite failed. | 1 | 0 |
 
@@ -87,7 +87,6 @@
 ### Unresolved blockers
 | issue id | title | classification | owner | rationale | mitigation | evidence |
 | --- | --- | --- | --- | --- | --- | --- |
-| build_audit_build_command_failed_nonzero_build_command_exited | build_command_failed_nonzero | blocker | from_check | Build command exited non-zero without a classified blocker line. | fix before GO | npm run release:build-audit:json |
 | fresh_run_acceptance_fresh_run_manual_pending_required_manual_fres | fresh_run_manual_pending | blocker | from_check | Required manual fresh-run coverage is incomplete. | fix before GO | npm run release:fresh-run-report:json |
 | balance_regression_balance_regression_command_failed_balance_regression_c | balance_regression_command_failed | blocker | from_check | balance_regression command returned non-zero exit code. | fix before GO | npm run balance:report:json |
 | route_comparison_adapter_exception | adapter_exception | blocker | from_check | Cannot read properties of undefined (reading 'toLowerCase') | fix before GO | route_comparison |
@@ -104,5 +103,5 @@
 
 ## Final decision
 - RELEASE DECISION: NO_GO
-- reason: 6 unresolved blockers remain; 1 pending-manual checks remain; 3 waiver-candidate findings are untracked/unaccepted
+- reason: 5 unresolved blockers remain; 1 pending-manual checks remain; 6 waiver-candidate findings are untracked/unaccepted
 - accepted waivers listed separately: 6
diff --git a/package.json b/package.json
index af2ef74f..4e38a308 100644
--- a/package.json
+++ b/package.json
@@ -48,6 +48,8 @@
     "release:build-audit:json": "npm run release:build-audit -- --json",
     "release:runtime-diagnostics": "tsc --project tsconfig.tests.json && node --loader=./scripts/relativeJsLoader.mjs tmp-tests/scripts/release/runRuntimeDiagnostics.js",
     "release:runtime-diagnostics:json": "npm run release:runtime-diagnostics -- --json",
+    "release:prestige-runtime-audit": "node --loader=./scripts/relativeJsLoader.mjs --experimental-strip-types scripts/release/buildPrestigeRuntimeEffectAudit.ts --write",
+    "release:prestige-runtime-audit:json": "npm run release:prestige-runtime-audit -- --json",
     "release:menu-interactions-audit": "node --experimental-strip-types scripts/release/auditMenuInteractions.ts",
     "release:pavilion-overflow-audit": "playwright test tests/e2e/pavilion-overflow.spec.ts",
     "test:e2e": "playwright test",
diff --git a/scripts/release/captureBuildWarnings.ts b/scripts/release/captureBuildWarnings.ts
index e25b1ed6..9707f476 100644
--- a/scripts/release/captureBuildWarnings.ts
+++ b/scripts/release/captureBuildWarnings.ts
@@ -56,7 +56,9 @@ async function run() {
     return;
   }
 
-  const buildResult = spawnSync('npm', ['run', 'build'], {
+  const buildCommand = process.platform === 'win32' ? process.env.ComSpec ?? 'cmd.exe' : 'npm';
+  const buildArgs = process.platform === 'win32' ? ['/d', '/s', '/c', 'npm run build'] : ['run', 'build'];
+  const buildResult = spawnSync(buildCommand, buildArgs, {
     cwd: process.cwd(),
     encoding: 'utf8',
     maxBuffer: 20 * 1024 * 1024,
diff --git a/src/components/GameLayout.tsx b/src/components/GameLayout.tsx
index 623a6340..e98c30e0 100644
--- a/src/components/GameLayout.tsx
+++ b/src/components/GameLayout.tsx
@@ -254,10 +254,14 @@ export function GameLayout() {
   const showSectionCAuditHarness = isSectionCAuditQueryEnabled();
   const showPhase0CoreAuditHarness = isPhase0CoreAuditQueryEnabled();
   const showPhase6CombatAuditHarness = isPhase6CombatAuditQueryEnabled();
+  const shouldShowOfflineProgress =
+    showOfflineProgressModal && showOfflineModalSetting && !suppressExactCaptureChrome;
+  const shouldShowLifeStartWizard =
+    !suppressExactCaptureChrome && !shouldDelayLifeStartForStory && !shouldShowOfflineProgress;
 
   useEffect(() => {
-    setLifeStartWizardOpenForNotifications(lifeStartWizardOpen);
-  }, [lifeStartWizardOpen, setLifeStartWizardOpenForNotifications]);
+    setLifeStartWizardOpenForNotifications(lifeStartWizardOpen && shouldShowLifeStartWizard);
+  }, [lifeStartWizardOpen, setLifeStartWizardOpenForNotifications, shouldShowLifeStartWizard]);
 
   return (
     <FxQualityProvider>
@@ -277,7 +281,7 @@ export function GameLayout() {
 
         {!apothecaryExactFixtureRouteEnabled && <BottomTabBar />}
 
-        {showOfflineProgressModal && showOfflineModalSetting && !suppressExactCaptureChrome && <OfflineProgressModal />}
+        {shouldShowOfflineProgress && <OfflineProgressModal />}
         {showManualSatchelModal && !suppressExactCaptureChrome && <ManualSatchelModal />}
         {showTechniqueLearnedModal && !suppressExactCaptureChrome && <TechniqueLearnedModal />}
         {showWorldBuildingModal && <BuildingModalHost />}
@@ -287,7 +291,7 @@ export function GameLayout() {
         {showSystemStatusOverlay && activeTab !== 'cultivation' && !apothecaryExactModalOpen && !suppressExactCaptureChrome && <SystemStatusPanelOverlay />}
         <CombatPresentationHost />
         {!suppressExactCaptureChrome && <OnboardingPromptRuntime />}
-        {!suppressExactCaptureChrome && !shouldDelayLifeStartForStory && <LifeStartWizardModal />}
+        {shouldShowLifeStartWizard && <LifeStartWizardModal />}
         {!suppressExactCaptureChrome && activeStoryCutsceneId && <StoryCutsceneOverlay />}
         {!suppressExactCaptureChrome && <CityArrivalBanner />}
         {!suppressExactCaptureChrome && <OnboardingPromptHost />}
diff --git a/src/components/modals/LifeSummaryModal.tsx b/src/components/modals/LifeSummaryModal.tsx
index 51dadb54..6e2b1c73 100644
--- a/src/components/modals/LifeSummaryModal.tsx
+++ b/src/components/modals/LifeSummaryModal.tsx
@@ -88,6 +88,14 @@ export function LifeSummaryModal() {
         )}
       </section>
 
+      {surface ? (
+        <section className="lifeSummaryModal__headline" data-ui="life-summary-headline">
+          <h3>{surface.headline}</h3>
+          <p>{surface.nextLifeFocus.label}: {surface.nextLifeFocus.detail}</p>
+          {surface.warnings.map((warning) => <small key={warning}>{warning}</small>)}
+        </section>
+      ) : null}
+
       {surface ? (
         <section className="lifeSummaryModal__blocks" data-ui="life-summary-blocks">
           {surface.blocks.map((block) => (
@@ -102,6 +110,20 @@ export function LifeSummaryModal() {
       ) : (
         <section className="lifeSummaryModal__empty">Finish one Reincarnation to unlock the last completed life summary.</section>
       )}
+
+      {surface && surface.majorMemories.length > 0 ? (
+        <section className="lifeSummaryModal__memories" data-ui="life-summary-major-memories">
+          <h3>Major Memories</h3>
+          <ul>
+            {surface.majorMemories.map((memory) => (
+              <li key={memory.id}>
+                <strong>{memory.title}</strong>
+                <span>{memory.detail}</span>
+              </li>
+            ))}
+          </ul>
+        </section>
+      ) : null}
     </RitualModalFrame>
   );
 }
diff --git a/src/components/modals/OfflineProgressModal.scss b/src/components/modals/OfflineProgressModal.scss
index 8f2bffcd..a10f63b5 100644
--- a/src/components/modals/OfflineProgressModal.scss
+++ b/src/components/modals/OfflineProgressModal.scss
@@ -6,7 +6,7 @@
   align-items: center;
   justify-content: center;
   padding: 16px;
-  z-index: 50;
+  z-index: 2400;
 }
 
 .offlineProgressModalModal {
@@ -47,12 +47,19 @@
 }
 
 .offlineProgressModalSummaryRow {
-  display: flex;
-  justify-content: space-between;
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) max-content;
+  gap: 16px;
+  align-items: start;
   color: #000000;
   font-size: 16px;
 }
 
+.offlineProgressModalSummaryRow > span:first-child {
+  min-width: 0;
+  overflow-wrap: anywhere;
+}
+
 .offlineProgressModalSummarySmall {
   font-size: 14px;
   color: #000000;
@@ -77,6 +84,43 @@
   padding: 8px;
 }
 
+.offlineProgressModalTrustList {
+  display: grid;
+  gap: 8px;
+  margin-top: 4px;
+  padding-top: 8px;
+  border-top: 1px solid rgba(91, 64, 38, 0.22);
+}
+
+.offlineProgressModalTrustRow {
+  display: grid;
+  grid-template-columns: minmax(135px, 0.78fr) minmax(0, 1fr);
+  gap: 14px;
+  align-items: start;
+  color: #000000;
+  font-size: 13px;
+  line-height: 1.35;
+}
+
+.offlineProgressModalTrustLabel {
+  min-width: 0;
+  font-weight: 800;
+  overflow-wrap: anywhere;
+}
+
+.offlineProgressModalTrustValue {
+  justify-self: end;
+  font-weight: 800;
+  color: var(--paper-jade, #2f8f66);
+  white-space: nowrap;
+}
+
+.offlineProgressModalTrustDetail {
+  min-width: 0;
+  color: rgba(24, 24, 24, 0.78);
+  overflow-wrap: anywhere;
+}
+
 .offlineProgressModalActions {
   margin-top: 24px;
   text-align: center;
diff --git a/src/components/modals/OfflineProgressModal.tsx b/src/components/modals/OfflineProgressModal.tsx
index 0b97ca16..b419857b 100644
--- a/src/components/modals/OfflineProgressModal.tsx
+++ b/src/components/modals/OfflineProgressModal.tsx
@@ -1,5 +1,6 @@
 import { useUIStore } from '../../stores/uiStore.js';
 import { buildOfflineModalRows } from '../../systems/offline/offlineSummaryReadModel.js';
+import { buildOfflineCatchupSurface } from '../../systems/offline/offlineCatchupSurface.js';
 import './OfflineProgressModal.scss';
 
 export function OfflineProgressModal() {
@@ -8,6 +9,11 @@ export function OfflineProgressModal() {
 
   if (!offlineProgressSummary) return null;
   const visibleRows = buildOfflineModalRows(offlineProgressSummary);
+  const surface = buildOfflineCatchupSurface({
+    summary: offlineProgressSummary,
+    generatedAt: Date.now(),
+    rawSeconds: offlineProgressSummary.rawOfflineSeconds,
+  });
 
   return (
     <div className={'offlineProgressModalOverlay'}>
@@ -19,8 +25,12 @@ export function OfflineProgressModal() {
 
         <div className={'offlineProgressModalSummaryCard'}>
           <div className={'offlineProgressModalSummaryRow'}>
-            <span>Time Offline</span>
-            <span className={'offlineProgressModalEmphasis'}>{offlineProgressSummary.offlineDuration}</span>
+            <span>Time considered</span>
+            <span className={'offlineProgressModalEmphasis'}>{surface.durationLabel}</span>
+          </div>
+          <div className={'offlineProgressModalSummaryRow'}>
+            <span>Efficiency</span>
+            <span className={'offlineProgressModalEmphasis'}>{surface.efficiency.label}</span>
           </div>
           {visibleRows.map((row) => (
             <div key={row.kind} className={'offlineProgressModalSummaryRow'}>
@@ -30,9 +40,23 @@ export function OfflineProgressModal() {
           ))}
           {offlineProgressSummary.wasCapped && (
             <div className={'offlineProgressModalCappedNotice'}>
-              Offline time capped at 12 hours.
+              Offline time capped at {surface.cap.maxSeconds / 3600} hours.
             </div>
           )}
+          <div className={'offlineProgressModalTrustList'}>
+            {surface.efficiency.sources.map((source) => (
+              <div key={source.id} className={'offlineProgressModalTrustRow'}>
+                <span className={'offlineProgressModalTrustLabel'}>{source.label}</span>
+                <span className={'offlineProgressModalTrustValue'}>{source.value}</span>
+              </div>
+            ))}
+            {surface.blockedReasons.map((reason) => (
+              <div key={reason.id} className={'offlineProgressModalTrustRow'}>
+                <span className={'offlineProgressModalTrustLabel'}>{reason.label}</span>
+                <span className={'offlineProgressModalTrustDetail'}>{reason.detail}</span>
+              </div>
+            ))}
+          </div>
         </div>
 
         <div className={'offlineProgressModalActions'}>
diff --git a/src/features/prestige/lifeSummarySurface.ts b/src/features/prestige/lifeSummarySurface.ts
index 0a4fc8d3..48421fe1 100644
--- a/src/features/prestige/lifeSummarySurface.ts
+++ b/src/features/prestige/lifeSummarySurface.ts
@@ -5,10 +5,15 @@ import { useCultivationStore } from '../../stores/cultivationStore.js';
 import { useGameStore } from '../../stores/gameStore.js';
 import { useInventoryStore } from '../../stores/inventoryStore.js';
 import { usePrestigeStore } from '../../stores/prestigeStore.js';
+import { useUIStore } from '../../stores/uiStore.js';
 import { useRuinsStore } from '../../stores/ruinsStore.js';
 import { useTrialStore } from '../../stores/trialStore.js';
+import { useBountyStore } from '../../stores/bountyStore.js';
+import { useExpeditionStore } from '../../stores/expeditionStore.js';
+import { useProfessionStore } from '../../stores/professionStore.js';
 import { useBreakthroughEchoStore } from '../breakthroughEchoes/index.js';
 import { getPrestigeAdvisorSurface, type PrestigeAdvisorStateLabel } from './prestigeAdvisorSurface.js';
+import { buildPostResetObjectivePreview, type PrestigePostResetObjective } from './prestigeForecastSurface.js';
 import {
   formatArchetypeLabel,
   formatCityLabel,
@@ -26,9 +31,26 @@ export type LifeSummaryBlockKey =
   | 'world_progress'
   | 'gate_trials'
   | 'ruins_supply'
+  | 'economy_support'
+  | 'offline_background'
   | 'next_life_focus';
 
-export type LifeSummaryMode = 'current' | 'last_completed';
+export type LifeSummaryMode = 'current' | 'last_completed' | 'post_reset';
+
+export type LifeSummarySeal =
+  | 'quiet_life'
+  | 'gate_challenger'
+  | 'realm_successor'
+  | 'chapter_exhausted'
+  | 'reclaimer';
+
+export type LifeMemoryLine = {
+  id: string;
+  title: string;
+  detail: string;
+  source: 'breakthrough_echo' | 'gate_trial' | 'city_arrival' | 'dao_impression' | 'ruins' | 'bounty' | 'expedition' | 'offline' | 'prestige';
+  summaryEligible: boolean;
+};
 
 export interface LifeSummaryBlock {
   key: LifeSummaryBlockKey;
@@ -37,20 +59,34 @@ export interface LifeSummaryBlock {
 }
 
 export interface PrestigeLifeSummarySnapshot {
+  version: 2;
   capturedAt: number;
+  lifeOrdinal: number;
   advisorLabel: PrestigeAdvisorStateLabel;
   apForecastGain: number;
   apAfterRitual: number;
+  headline: string;
+  summarySeal: LifeSummarySeal;
   blocks: LifeSummaryBlock[];
+  majorMemories: LifeMemoryLine[];
+  nextLifeFocus: PrestigePostResetObjective;
+  warnings: string[];
 }
 
 export interface LifeSummarySurface {
+  version: 2;
   mode: LifeSummaryMode;
   capturedAt: number;
+  lifeOrdinal: number;
   advisorLabel: PrestigeAdvisorStateLabel;
   apForecastGain: number;
   apAfterRitual: number;
+  headline: string;
+  summarySeal: LifeSummarySeal;
   blocks: LifeSummaryBlock[];
+  majorMemories: LifeMemoryLine[];
+  nextLifeFocus: PrestigePostResetObjective;
+  warnings: string[];
 }
 
 const BLOCKS: ReadonlyArray<{ key: LifeSummaryBlockKey; title: string }> = Object.freeze([
@@ -60,13 +96,16 @@ const BLOCKS: ReadonlyArray<{ key: LifeSummaryBlockKey; title: string }> = Objec
   { key: 'world_progress', title: 'World Progress' },
   { key: 'gate_trials', title: 'Gate Trials' },
   { key: 'ruins_supply', title: 'Ruins & Supply' },
+  { key: 'economy_support', title: 'Economy Support' },
+  { key: 'offline_background', title: 'Offline & Background' },
   { key: 'next_life_focus', title: 'Next Life Focus' },
 ]);
 
 const clampLines = (lines: string[], fallback: string): string[] => {
   const normalized = lines.filter((line) => typeof line === 'string' && line.trim().length > 0).map((line) => line.trim());
-  if (normalized.length === 0) return [fallback];
-  return normalized.slice(0, 5);
+  const deduped = normalized.filter((line, index, values) => values.indexOf(line) === index);
+  if (deduped.length === 0) return [fallback];
+  return deduped.slice(0, 5);
 };
 
 const formatDuration = (ms: number): string => {
@@ -78,6 +117,87 @@ const formatDuration = (ms: number): string => {
   return `${hours}h ${minutes}m`;
 };
 
+const resolveSummarySeal = (args: {
+  advisor: PrestigeAdvisorStateLabel;
+  clearedTrials: number;
+  highestRealmIndex: number;
+  prestigeCount: number;
+}): LifeSummarySeal => {
+  if (args.advisor === 'Recommended') return 'chapter_exhausted';
+  if (args.prestigeCount > 0) return 'reclaimer';
+  if (args.highestRealmIndex >= 2) return 'realm_successor';
+  if (args.clearedTrials > 0) return 'gate_challenger';
+  return 'quiet_life';
+};
+
+const buildHeadline = (args: {
+  realmName: string;
+  advisor: PrestigeAdvisorStateLabel;
+  apGain: number;
+}): string => {
+  if (args.advisor === 'Too Early') {
+    return `${args.realmName} life is still gathering enough proof for reincarnation.`;
+  }
+  if (args.advisor === 'Recommended') {
+    return `${args.realmName} life has reached a rational reincarnation handoff for +${args.apGain} AP.`;
+  }
+  return `${args.realmName} life can be sealed now for +${args.apGain} AP, with more value possible if you continue.`;
+};
+
+const buildMajorMemories = (args: {
+  echoes: { echoId: string; memoryLine: string }[];
+  clearedTrials: number;
+  bypassedTrials: number;
+  totalRuinsRuns: number;
+  offlineLine: string | null;
+}): LifeMemoryLine[] => {
+  const memories: LifeMemoryLine[] = args.echoes.slice(-3).map((echo) => ({
+    id: `echo_${echo.echoId}`,
+    title: 'Breakthrough Echo',
+    detail: echo.memoryLine,
+    source: 'breakthrough_echo',
+    summaryEligible: true,
+  }));
+
+  if (args.clearedTrials > 0 || args.bypassedTrials > 0) {
+    memories.push({
+      id: 'gate_trial_record',
+      title: 'Gate Trial Record',
+      detail: `${args.clearedTrials} cleared, ${args.bypassedTrials} bypassed.`,
+      source: 'gate_trial',
+      summaryEligible: true,
+    });
+  }
+  if (args.totalRuinsRuns > 0) {
+    memories.push({
+      id: 'ruins_record',
+      title: 'Ruins Record',
+      detail: `${args.totalRuinsRuns} ruins run${args.totalRuinsRuns === 1 ? '' : 's'} completed this life.`,
+      source: 'ruins',
+      summaryEligible: true,
+    });
+  }
+  if (args.offlineLine) {
+    memories.push({
+      id: 'offline_record',
+      title: 'Offline Record',
+      detail: args.offlineLine,
+      source: 'offline',
+      summaryEligible: true,
+    });
+  }
+  if (memories.length === 0) {
+    memories.push({
+      id: 'quiet_life',
+      title: 'Quiet Life',
+      detail: 'No major breakthrough, gate, ruins, or offline memories have been recorded yet.',
+      source: 'prestige',
+      summaryEligible: true,
+    });
+  }
+  return memories.slice(0, 6);
+};
+
 const buildCurrentBlocks = (): LifeSummaryBlock[] => {
   const now = Date.now();
   const game = useGameStore.getState();
@@ -87,6 +207,10 @@ const buildCurrentBlocks = (): LifeSummaryBlock[] => {
   const trial = useTrialStore.getState();
   const ruins = useRuinsStore.getState();
   const inventory = useInventoryStore.getState();
+  const ui = useUIStore.getState();
+  const bounties = useBountyStore.getState();
+  const expeditions = useExpeditionStore.getState();
+  const profession = useProfessionStore.getState();
   const echoes = useBreakthroughEchoStore.getState().echoes;
   const advisor = getPrestigeAdvisorSurface();
   const status = buildSection5StatusSurface();
@@ -96,8 +220,17 @@ const buildCurrentBlocks = (): LifeSummaryBlock[] => {
   const bypassedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'bypassed').length;
   const totalRuinsRuns = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRuns, 0);
   const totalRuinsRooms = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRoomsCleared, 0);
+  const activeQueues = profession.alchemyQueue.length + profession.talismanQueue.length + profession.forgeQueue.length;
+  const activeExpeditions = expeditions.active.length;
+  const completedExpeditions = expeditions.active.filter((run) => run.status === 'complete' || now >= run.endsAt).length;
+  const bountyRecords = Object.values(bounties.activeByCityId ?? {}).reduce((sum, board) => sum + board.length, 0);
+  const lastOffline = ui.lastOfflineSummary;
+  const offlineLine = lastOffline
+    ? `${lastOffline.offlineDuration} considered at ${Math.round(lastOffline.efficiency * 100)}% efficiency.`
+    : null;
 
   const topPurchase = advisor.topRecommendedPurchase;
+  const nextLifeFocus = buildPostResetObjectivePreview();
 
   const linesByKey: Record<LifeSummaryBlockKey, string[]> = {
     life_arc: clampLines([
@@ -134,7 +267,22 @@ const buildCurrentBlocks = (): LifeSummaryBlock[] => {
       ruins.lastRunSummary ? `Last ruins run: ${ruins.lastRunSummary.victory ? 'Victory' : 'Defeat'} (${ruins.lastRunSummary.roomsCleared}/${ruins.lastRunSummary.roomCount} rooms)` : 'No ruins run completed this life.',
       `Current gold on hand: ${inventory.currencies.gold}`,
     ], 'No ruins or supply activity has been recorded yet.'),
+    economy_support: clampLines([
+      `Profession queues active: ${activeQueues}`,
+      `Bounty records touched: ${bountyRecords}`,
+      activeExpeditions > 0 ? `Expeditions running: ${activeExpeditions}` : 'No expedition currently running.',
+      completedExpeditions > 0 ? `Expeditions ready: ${completedExpeditions}` : '',
+      status.currentDiagnosis ? `Strongest support route: fix ${formatDiagnosisLabel(status.currentDiagnosis.primary)}.` : '',
+    ], 'No major support loop has been recorded yet.'),
+    offline_background: clampLines([
+      offlineLine ?? 'No offline settlement has been recorded in this session.',
+      lastOffline?.wasCapped ? 'Last offline settlement hit the cap.' : '',
+      lastOffline?.parts.find((part) => part.kind === 'queued_actions') ? 'Queued actions became ready while away.' : '',
+      lastOffline?.parts.find((part) => part.kind === 'expeditions') ? 'Expeditions became ready while away.' : '',
+      'Combat never progresses offline.',
+    ], 'No offline background summary is available yet.'),
     next_life_focus: clampLines([
+      `${nextLifeFocus.label}: ${nextLifeFocus.detail}`,
       status.currentDiagnosis ? `Fix diagnosis first: ${formatDiagnosisLabel(status.currentDiagnosis.primary)}` : '',
       status.currentGateTrialId ? `Center prep around ${formatGateTrialLabel(status.currentGateTrialId)} before your next reset.` : '',
       topPurchase ? `${topPurchase.mode === 'buy_now' ? 'Buy now' : 'Save for'} ${topPurchase.name} (${topPurchase.nextCost} AP)` : '',
@@ -151,25 +299,68 @@ const buildCurrentBlocks = (): LifeSummaryBlock[] => {
 export const buildCurrentLifeSummarySurface = (): LifeSummarySurface => {
   const advisor = getPrestigeAdvisorSurface();
   const prestige = usePrestigeStore.getState();
+  const game = useGameStore.getState();
+  const trial = useTrialStore.getState();
+  const ruins = useRuinsStore.getState();
+  const ui = useUIStore.getState();
+  const echoes = useBreakthroughEchoStore.getState().echoes;
+  const clearedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'cleared').length;
+  const bypassedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'bypassed').length;
+  const totalRuinsRuns = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRuns, 0);
+  const highestRealmIndex = Math.max(prestige.highestRealmReached, game.realm?.index ?? 0);
+  const realmName = getLiveRealmNameByIndex(highestRealmIndex);
+  const nextLifeFocus = buildPostResetObjectivePreview();
+  const summarySeal = resolveSummarySeal({
+    advisor: advisor.stateLabel,
+    clearedTrials,
+    highestRealmIndex,
+    prestigeCount: prestige.prestigeCount,
+  });
 
   return {
+    version: 2,
     mode: 'current',
     capturedAt: Date.now(),
+    lifeOrdinal: prestige.prestigeCount + 1,
     advisorLabel: advisor.stateLabel,
     apForecastGain: advisor.apForecast.potentialGain,
     apAfterRitual: prestige.totalAP + advisor.apForecast.potentialGain,
+    headline: buildHeadline({
+      realmName,
+      advisor: advisor.stateLabel,
+      apGain: advisor.apForecast.potentialGain,
+    }),
+    summarySeal,
     blocks: buildCurrentBlocks(),
+    majorMemories: buildMajorMemories({
+      echoes,
+      clearedTrials,
+      bypassedTrials,
+      totalRuinsRuns,
+      offlineLine: ui.lastOfflineSummary
+        ? `${ui.lastOfflineSummary.offlineDuration} at ${Math.round(ui.lastOfflineSummary.efficiency * 100)}% efficiency.`
+        : null,
+    }),
+    nextLifeFocus,
+    warnings: advisor.apForecast.potentialGain <= 0 ? ['Potential AP gain is zero; do not treat this as a strong reset.'] : [],
   };
 };
 
 export const buildPrestigeLifeSummarySnapshot = (): PrestigeLifeSummarySnapshot => {
   const current = buildCurrentLifeSummarySurface();
   return {
+    version: 2,
     capturedAt: current.capturedAt,
+    lifeOrdinal: current.lifeOrdinal,
     advisorLabel: current.advisorLabel,
     apForecastGain: current.apForecastGain,
     apAfterRitual: current.apAfterRitual,
+    headline: current.headline,
+    summarySeal: current.summarySeal,
     blocks: current.blocks.map((block) => ({ ...block, lines: [...block.lines] })),
+    majorMemories: current.majorMemories.map((memory) => ({ ...memory })),
+    nextLifeFocus: current.nextLifeFocus,
+    warnings: [...current.warnings],
   };
 };
 
@@ -178,11 +369,15 @@ export const buildLastCompletedLifeSummarySurface = (
 ): LifeSummarySurface => {
   const normalizedBlockByKey = new Map(snapshot.blocks.map((block) => [block.key, block]));
   return {
+    version: 2,
     mode: 'last_completed',
     capturedAt: snapshot.capturedAt,
+    lifeOrdinal: snapshot.lifeOrdinal,
     advisorLabel: snapshot.advisorLabel,
     apForecastGain: snapshot.apForecastGain,
     apAfterRitual: snapshot.apAfterRitual,
+    headline: snapshot.headline,
+    summarySeal: snapshot.summarySeal,
     blocks: BLOCKS.map((block) => {
       const existing = normalizedBlockByKey.get(block.key);
       return {
@@ -191,5 +386,8 @@ export const buildLastCompletedLifeSummarySurface = (
         lines: clampLines(existing?.lines ?? [], 'No notable notes were captured for this section.'),
       };
     }),
+    majorMemories: snapshot.majorMemories.map((memory) => ({ ...memory })),
+    nextLifeFocus: snapshot.nextLifeFocus,
+    warnings: [...snapshot.warnings],
   };
 };
diff --git a/src/features/prestige/prestigeAdvisorSurface.ts b/src/features/prestige/prestigeAdvisorSurface.ts
index ff848918..3d302f67 100644
--- a/src/features/prestige/prestigeAdvisorSurface.ts
+++ b/src/features/prestige/prestigeAdvisorSurface.ts
@@ -11,6 +11,7 @@ import {
 } from '../../systems/prestige/prestigeApReadModel.js';
 import { PRESTIGE_CATEGORIES, getPrestigeCategoryKey } from './prestigeCategories.js';
 import { buildPrestigeStarterSpendPlan } from '../../systems/prestige/prestigeStarterSpendPlanner.js';
+import { getPrestigeResetContractSurface } from '../../services/prestige/PrestigeResetContract.js';
 
 export type PrestigeAdvisorStateLabel = 'Too Early' | 'Viable' | 'Recommended';
 
@@ -46,29 +47,6 @@ export type PrestigeAdvisorSurface = {
   topRecommendedPurchase: PrestigeAdvisorRecommendedPurchase | null;
 };
 
-const RESETS_THIS_LIFE = Object.freeze([
-  'Realm progress',
-  'Qi and combat run state',
-  'Inventory and currencies',
-  'Equipment loadout',
-  'Trial and ruins progress',
-  'Outskirts and bounty progress',
-  'Current activity',
-]);
-
-const CARRIES_FORWARD = Object.freeze([
-  'Ascension Points (AP)',
-  'Lifetime AP earned',
-  'Purchased AP upgrades',
-  'Prestige run history',
-  'Prestige count',
-]);
-
-const REBUILT_NEXT_LIFE = Object.freeze([
-  'Spirit root (new roll)',
-  'City baseline (starter city)',
-]);
-
 const getAdvisorDetail = (stateLabel: PrestigeAdvisorStateLabel): string => {
   if (stateLabel === 'Too Early') {
     return 'Push this life to Core Formation before beginning Reincarnation.';
@@ -127,6 +105,7 @@ export const getPrestigeAdvisorSurface = (): PrestigeAdvisorSurface => {
   const potentialGain = Math.max(0, forecast.totalAp);
   const breakdown = prestige.getApBreakdown();
   const stateLabel = resolvePrestigeAdvisorLabel(forecast);
+  const resetContract = getPrestigeResetContractSurface({ purchasesById: prestige.purchasesById });
 
   return {
     stateLabel,
@@ -136,9 +115,9 @@ export const getPrestigeAdvisorSurface = (): PrestigeAdvisorSurface => {
       breakdown,
     },
     resetPreview: {
-      resetsThisLife: [...RESETS_THIS_LIFE],
-      carriesForward: [...CARRIES_FORWARD],
-      rebuiltNextLife: [...REBUILT_NEXT_LIFE],
+      resetsThisLife: resetContract.reset.map((line) => line.label),
+      carriesForward: resetContract.carry.map((line) => line.label),
+      rebuiltNextLife: resetContract.rebuilt.map((line) => line.label),
     },
     spiritRootClassification: prestige.spiritRoot ? 'Awakened' : 'Dormant',
     topRecommendedPurchase: buildTopRecommendedPurchase(),
diff --git a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss
index 434255db..56a368d6 100644
--- a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss
+++ b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.scss
@@ -15,6 +15,7 @@
   --prestige-left-width: clamp(390px, 20.4vw, 430px);
   --prestige-right-width: clamp(430px, 22.8vw, 480px);
   --prestige-bottom-contract-height: clamp(146px, 13.9vh, 168px);
+  --prestige-forecast-height: clamp(128px, 12.4vh, 152px);
   --prestige-decree-min-width: 720px;
 
   width: 100%;
@@ -25,10 +26,12 @@
   box-sizing: border-box;
   display: grid;
   grid-template-columns: var(--prestige-left-width) minmax(var(--prestige-decree-min-width), 1fr) var(--prestige-right-width);
-  grid-template-rows: var(--prestige-header-height) minmax(0, 1fr) var(--prestige-bottom-contract-height);
+  grid-template-rows: var(--prestige-header-height) auto minmax(0, 1fr) var(--prestige-forecast-height) var(--prestige-bottom-contract-height);
   grid-template-areas:
     "header header header"
+    "reclaim reclaim reclaim"
     "ledger decree preview"
+    "forecast forecast forecast"
     "contract contract contract";
   gap: var(--prestige-gap-y) var(--prestige-gap-x);
   padding: var(--prestige-pad-top) var(--prestige-pad-x) 12px;
@@ -146,6 +149,87 @@
   font-size: 12px;
 }
 
+.prestigeLedgerReclaimBanner {
+  grid-area: reclaim;
+  display: grid;
+  grid-template-columns: minmax(220px, 0.85fr) minmax(260px, 1fr) minmax(260px, 1.05fr) auto;
+  gap: 14px;
+  align-items: start;
+  padding: 12px 18px;
+  z-index: 3;
+}
+
+.prestigeLedgerReclaimBanner__copy,
+.prestigeLedgerReclaimBanner__advantages,
+.prestigeLedgerReclaimBanner__steps,
+.prestigeLedgerReclaimBanner__actions {
+  min-width: 0;
+}
+
+.prestigeLedgerReclaimBanner__copy {
+  display: grid;
+  gap: 4px;
+}
+
+.prestigeLedgerReclaimBanner__copy span {
+  color: var(--prestige-cinnabar);
+  font-size: 11px;
+  font-weight: 900;
+  text-transform: uppercase;
+}
+
+.prestigeLedgerReclaimBanner__copy strong,
+.prestigeLedgerReclaimBanner__steps strong {
+  overflow-wrap: anywhere;
+}
+
+.prestigeLedgerReclaimBanner__copy p,
+.prestigeLedgerReclaimBanner__steps span {
+  margin: 0;
+  color: var(--prestige-muted);
+  font-size: 12px;
+  line-height: 1.35;
+  overflow-wrap: anywhere;
+}
+
+.prestigeLedgerReclaimBanner__advantages {
+  display: grid;
+  gap: 6px;
+  color: var(--prestige-jade);
+  font-size: 12px;
+  font-weight: 800;
+}
+
+.prestigeLedgerReclaimBanner__advantages span {
+  overflow-wrap: anywhere;
+}
+
+.prestigeLedgerReclaimBanner__steps {
+  display: grid;
+  gap: 6px;
+  margin: 0;
+  padding-left: 18px;
+}
+
+.prestigeLedgerReclaimBanner__steps li {
+  min-width: 0;
+}
+
+.prestigeLedgerReclaimBanner__steps strong {
+  display: block;
+  font-size: 12px;
+}
+
+.prestigeLedgerReclaimBanner__actions {
+  display: grid;
+  gap: 8px;
+}
+
+.prestigeLedgerReclaimBanner__actions .prestigeLedgerButton {
+  min-width: 132px;
+  font-size: 13px;
+}
+
 .prestigeLedgerHeader::after {
   content: "";
   position: absolute;
@@ -320,7 +404,7 @@
   flex-direction: column;
   min-height: 0;
   padding: 22px 24px;
-  overflow: hidden;
+  overflow: auto;
 }
 
 .prestigeLedgerCard--ledger {
@@ -386,7 +470,7 @@
 
 .prestigeLedgerRow {
   display: grid;
-  grid-template-columns: 118px minmax(0, 1fr);
+  grid-template-columns: minmax(96px, 0.42fr) minmax(0, 1fr);
   gap: 10px;
   align-items: baseline;
   font-size: clamp(14px, 0.78vw, 16px);
@@ -458,8 +542,8 @@
 }
 
 .prestigeLedgerReceipt__row {
-  display: flex;
-  justify-content: space-between;
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) max-content;
   gap: 14px;
   padding: 8px 2px;
   border-bottom: 1px solid rgba(91, 64, 38, 0.17);
@@ -475,6 +559,11 @@
   white-space: nowrap;
 }
 
+.prestigeLedgerReceipt__row span {
+  min-width: 0;
+  overflow-wrap: anywhere;
+}
+
 .prestigeLedgerReceipt__total {
   display: grid;
   grid-template-columns: 1fr auto;
@@ -510,6 +599,10 @@
   margin-top: auto;
 }
 
+.prestigeLedgerCard__lastLifeButton {
+  margin-top: 8px;
+}
+
 .prestigeLedgerButton,
 .prestigeLedgerPrimaryPlaque,
 .prestigeLedgerRecommendation__body,
@@ -855,9 +948,7 @@
 }
 
 .prestigeLedgerRecommendation__copy span {
-  overflow: hidden;
-  text-overflow: ellipsis;
-  white-space: nowrap;
+  overflow-wrap: anywhere;
 }
 
 .prestigeLedgerRecommendation__title {
@@ -900,7 +991,7 @@
   align-items: stretch;
   gap: 22px;
   padding: 14px 36px;
-  overflow: hidden;
+  overflow: auto;
 }
 
 .prestigeLedgerContract header {
@@ -917,8 +1008,8 @@
 
 .prestigeLedgerContract__tablets {
   display: grid;
-  grid-template-columns: repeat(3, minmax(0, 1fr));
-  gap: 34px;
+  grid-template-columns: repeat(4, minmax(0, 1fr));
+  gap: 16px;
   min-width: 0;
   z-index: 1;
 }
@@ -959,7 +1050,7 @@
 
 .prestigeLedgerTablet > span {
   position: absolute;
-  right: 22px;
+  right: 14px;
   top: 50%;
   width: 52px;
   height: 52px;
@@ -967,6 +1058,7 @@
   border-radius: 50%;
   border: 2px solid rgba(163, 58, 46, 0.6);
   opacity: 0.58;
+  pointer-events: none;
 }
 
 .prestigeLedgerTablet--carry > span {
@@ -977,6 +1069,10 @@
   border-color: rgba(119, 82, 43, 0.7);
 }
 
+.prestigeLedgerTablet--hybrid > span {
+  border-color: rgba(92, 88, 139, 0.66);
+}
+
 .prestigeLedgerContract__wash {
   position: absolute;
   bottom: 0;
@@ -1146,6 +1242,95 @@
   color: var(--prestige-jade);
 }
 
+.prestigeLedgerForecast {
+  grid-area: forecast;
+  display: grid;
+  grid-template-columns: minmax(240px, 0.72fr) minmax(0, 1.4fr) minmax(220px, 0.7fr);
+  gap: 14px;
+  padding: 12px 18px;
+  overflow: auto;
+}
+
+.prestigeLedgerForecast > header {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+}
+
+.prestigeLedgerForecast h2,
+.prestigeLedgerForecast h3,
+.prestigeLedgerForecast p {
+  margin: 0;
+}
+
+.prestigeLedgerForecast h2 {
+  font-size: 21px;
+}
+
+.prestigeLedgerForecast__objective {
+  display: grid;
+  gap: 4px;
+  align-content: center;
+  min-width: 0;
+}
+
+.prestigeLedgerForecast__objective strong {
+  color: var(--prestige-jade);
+}
+
+.prestigeLedgerForecast__objective p,
+.prestigeLedgerForecastLine p,
+.prestigeLedgerForecast__warnings {
+  color: var(--prestige-muted);
+  font-size: 13px;
+}
+
+.prestigeLedgerForecast__lines {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 10px;
+  min-width: 0;
+}
+
+.prestigeLedgerForecastLine {
+  display: grid;
+  gap: 4px;
+  min-width: 0;
+  padding: 9px 10px;
+  border: 1px solid rgba(91, 64, 38, .22);
+  border-radius: 8px;
+  background: rgba(255, 249, 232, .58);
+}
+
+.prestigeLedgerForecastLine div {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) max-content;
+  gap: 10px;
+}
+
+.prestigeLedgerForecastLine span,
+.prestigeLedgerForecastLine strong,
+.prestigeLedgerForecastLine p {
+  overflow-wrap: anywhere;
+}
+
+.prestigeLedgerForecastLine strong {
+  white-space: nowrap;
+}
+
+.prestigeLedgerForecastLine small {
+  color: rgba(120, 57, 38, .82);
+  font-weight: 800;
+}
+
+.prestigeLedgerForecast__warnings {
+  display: grid;
+  gap: 4px;
+  margin: 0;
+  padding-left: 18px;
+  align-content: center;
+}
+
 @media (max-width: 1500px) {
   .prestigeLedgerExactPage {
     --prestige-decree-min-width: 560px;
@@ -1162,11 +1347,11 @@
   }
 
   .prestigeLedgerRow {
-    grid-template-columns: 104px minmax(0, 1fr);
+    grid-template-columns: minmax(96px, 0.42fr) minmax(0, 1fr);
   }
 }
 
-@media (max-width: 1180px) {
+@media (max-width: 1320px) {
   .prestigeLedgerExactPage {
     height: auto;
     min-height: 100%;
@@ -1175,13 +1360,17 @@
     grid-template-rows: auto;
     grid-template-areas:
       "header"
+      "reclaim"
       "decree"
       "ledger"
       "preview"
+      "forecast"
       "contract";
   }
 
   .prestigeLedgerHeader,
+  .prestigeLedgerReclaimBanner,
+  .prestigeLedgerForecast,
   .prestigeLedgerContract {
     grid-template-columns: 1fr;
   }
diff --git a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx
index 1639a9f6..59fe6163 100644
--- a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx
+++ b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx
@@ -17,9 +17,15 @@ export function PrestigeLedgerExactScreen({
   onReviewAndReincarnate,
   onOpenLibrary,
   onViewLifeSummary,
+  onViewLastCompletedLifeSummary,
   onOpenApBreakdown,
   onSelectRecommendedDecree,
+  onRoutePostResetObjective,
+  onDismissPostResetObjective,
 }: PrestigeLedgerExactScreenProps) {
+  const reclaimObjective = surface.postResetReclaimObjective ?? null;
+  const primaryReclaimAction = reclaimObjective?.firstActions[0] ?? null;
+
   return (
     <main
       className={`prestigeLedgerExactPage prestigeLedgerExactPage--${surface.reincarnationDecree.sealState}`}
@@ -77,6 +83,39 @@ export function PrestigeLedgerExactScreen({
         </section>
       ) : null}
 
+      {reclaimObjective ? (
+        <section className="prestigeLedgerReclaimBanner parchmentPanel" aria-label="Post-reset reclaim objective">
+          <div className="prestigeLedgerReclaimBanner__copy">
+            <span>Previous life sealed</span>
+            <strong>{reclaimObjective.headline}</strong>
+            <p>{reclaimObjective.detail}</p>
+          </div>
+          <div className="prestigeLedgerReclaimBanner__advantages" aria-label="Retained advantages">
+            {reclaimObjective.retainedAdvantages.slice(0, 3).map((line) => (
+              <span key={line}>{line}</span>
+            ))}
+          </div>
+          <ol className="prestigeLedgerReclaimBanner__steps" aria-label="First actions">
+            {reclaimObjective.firstActions.slice(0, 3).map((action) => (
+              <li key={action.id}>
+                <strong>{action.label}</strong>
+                <span>{action.detail}</span>
+              </li>
+            ))}
+          </ol>
+          <div className="prestigeLedgerReclaimBanner__actions">
+            {primaryReclaimAction ? (
+              <button type="button" className="prestigeLedgerButton prestigeLedgerButton--secondary" onClick={onRoutePostResetObjective}>
+                {primaryReclaimAction.label}
+              </button>
+            ) : null}
+            <button type="button" className="prestigeLedgerButton prestigeLedgerButton--secondary" onClick={onDismissPostResetObjective}>
+              Dismiss
+            </button>
+          </div>
+        </section>
+      ) : null}
+
       <section className="prestigeLedgerCard prestigeLedgerCard--ledger parchmentPanel" aria-labelledby="prestige-ledger-current-title">
         <div className="prestigeLedgerCard__stamp" aria-hidden="true" />
         <h2 id="prestige-ledger-current-title">{surface.currentLifeLedger.title}</h2>
@@ -132,6 +171,17 @@ export function PrestigeLedgerExactScreen({
         >
           {surface.currentLifeLedger.viewLifeSummaryButton.label}
         </button>
+        {surface.meta.hasLastLifeSummary ? (
+          <button
+            type="button"
+            className="prestigeLedgerButton prestigeLedgerButton--secondary prestigeLedgerCard__lastLifeButton"
+            onClick={onViewLastCompletedLifeSummary}
+            disabled={!surface.currentLifeLedger.viewLastLifeSummaryButton.enabled}
+            title={surface.currentLifeLedger.viewLastLifeSummaryButton.reason}
+          >
+            {surface.currentLifeLedger.viewLastLifeSummaryButton.label}
+          </button>
+        ) : null}
       </section>
 
       <section className="prestigeLedgerDecree parchmentPanel" aria-labelledby="prestige-ledger-decree-title">
@@ -200,11 +250,13 @@ export function PrestigeLedgerExactScreen({
                   aria-label={card.actualUpgradeName ? `Inspect ${card.actualUpgradeName}` : card.displayTitle}
                 >
                   <span className="prestigeLedgerRecommendation__seal" aria-hidden="true" />
-                  <span className="prestigeLedgerRecommendation__copy">
-                    <span className="prestigeLedgerRecommendation__title">{card.displayTitle}</span>
-                    <span>Effect: {card.effectLine}</span>
-                    <span>Why: {card.whyLine}</span>
-                  </span>
+                    <span className="prestigeLedgerRecommendation__copy">
+                      <span className="prestigeLedgerRecommendation__title">{card.displayTitle}</span>
+                      <span>Effect: {card.effectLine}</span>
+                      <span>Why: {card.whyLine}</span>
+                      {card.levelLine ? <span>{card.levelLine}</span> : null}
+                      {card.runtimeHonesty ? <span>Runtime: {card.runtimeHonesty === 'live' ? 'Live' : card.runtimeHonesty}</span> : null}
+                    </span>
                   <span className="prestigeLedgerRecommendation__cost">
                     <small>Cost</small>
                     <strong>{card.costLabel}</strong>
@@ -229,6 +281,35 @@ export function PrestigeLedgerExactScreen({
         </button>
       </aside>
 
+      <section className="prestigeLedgerForecast parchmentPanel" aria-labelledby="prestige-ledger-forecast-title">
+        <header>
+          <h2 id="prestige-ledger-forecast-title">{surface.forecast.title}</h2>
+          <span className="prestigeLedgerStamp prestigeLedgerStamp--tiny" aria-hidden="true">AP</span>
+        </header>
+        <div className="prestigeLedgerForecast__objective">
+          <strong>{surface.forecast.objective.label}</strong>
+          <p>{surface.forecast.objective.detail}</p>
+        </div>
+        <div className="prestigeLedgerForecast__lines">
+          {surface.forecast.lines.map((line) => (
+            <article key={line.id} className={`prestigeLedgerForecastLine prestigeLedgerForecastLine--${line.confidence}`}>
+              <h3>{line.label}</h3>
+              <div>
+                <span>{line.previous}</span>
+                <strong>{line.expectedNext}</strong>
+              </div>
+              <p>{line.reason}</p>
+              <small>Confidence: {line.confidence}</small>
+            </article>
+          ))}
+        </div>
+        {surface.forecast.warnings.length > 0 ? (
+          <ul className="prestigeLedgerForecast__warnings">
+            {surface.forecast.warnings.slice(0, 3).map((warning) => <li key={warning}>{warning}</li>)}
+          </ul>
+        ) : null}
+      </section>
+
       <section className="prestigeLedgerContract parchmentPanel" aria-labelledby="prestige-ledger-contract-title">
         <div className="prestigeLedgerContract__wash prestigeLedgerContract__wash--left" aria-hidden="true" />
         <div className="prestigeLedgerContract__wash prestigeLedgerContract__wash--right" aria-hidden="true" />
diff --git a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx
index a3dc0ad8..2bb1622a 100644
--- a/src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx
+++ b/src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx
@@ -6,7 +6,7 @@ import { useInventoryStore } from '../../../stores/inventoryStore.js';
 import { usePrestigeStore } from '../../../stores/prestigeStore.js';
 import { useTrialStore } from '../../../stores/trialStore.js';
 import { useCityStore } from '../../../stores/cityStore.js';
-import { useUIStore } from '../../../stores/uiStore.js';
+import { useUIStore, type GameTab } from '../../../stores/uiStore.js';
 import { RewardService } from '../../../services/rewards/index.js';
 import { D } from '../../../utils/numbers.js';
 import type { PrestigeUpgradeDef } from '../../../content/index.js';
@@ -17,6 +17,7 @@ import {
 } from '../../../systems/prestige/prestigeApReadModel.js';
 import { buildPrestigeLifeSummarySnapshot } from '../lifeSummarySurface.js';
 import { getPrestigeAdvisorSurface } from '../prestigeAdvisorSurface.js';
+import { buildPrestigeForecastSurfaceV2 } from '../prestigeForecastSurface.js';
 import { ApBreakdownModal } from '../../../components/modals/ApBreakdownModal.js';
 import { PrestigeRitualModal } from '../../../components/modals/PrestigeRitualModal.js';
 import { PrestigeUpgradeModal } from '../../../components/modals/PrestigeUpgradeModal.js';
@@ -49,6 +50,8 @@ export function PrestigeLedgerScreenOwner() {
   const spiritRoot = usePrestigeStore((state) => state.spiritRoot);
   const purchasesById = usePrestigeStore((state) => state.purchasesById);
   const lastLifeSummary = usePrestigeStore((state) => state.lastLifeSummary);
+  const postResetReclaimObjective = usePrestigeStore((state) => state.postResetReclaimObjective);
+  const dismissPostResetReclaimObjective = usePrestigeStore((state) => state.dismissPostResetReclaimObjective);
   const calculateAPGain = usePrestigeStore((state) => state.calculateAPGain);
   const canPrestige = usePrestigeStore((state) => state.canPrestige);
   const getApBreakdown = usePrestigeStore((state) => state.getApBreakdown);
@@ -65,6 +68,7 @@ export function PrestigeLedgerScreenOwner() {
   const requirePrestigeConfirm = useUIStore((state) => state.settings.requirePrestigeConfirm);
   const setLifeStartWizardContext = useUIStore((state) => state.setLifeStartWizardContext);
   const openLifeSummaryModal = useUIStore((state) => state.openLifeSummaryModal);
+  const setActiveTab = useUIStore((state) => state.setActiveTab);
   const contentRaw = useContentStore((state) => state.raw);
   const heartLawsById = useContentStore((state) => state.maps.heartLawsById);
   const citiesById = useContentStore((state) => state.maps.citiesById);
@@ -90,6 +94,7 @@ export function PrestigeLedgerScreenOwner() {
   const apBreakdown = getApBreakdown();
   const canPrestigeNow = canPrestige();
   const advisor = getPrestigeAdvisorSurface();
+  const forecast = buildPrestigeForecastSurfaceV2();
   const runCompass = useRunCompassSurface();
   const visibleUpgrades = useMemo(() => getVisiblePrestigeUpgrades(), [getVisiblePrestigeUpgrades, contentRaw]);
 
@@ -147,11 +152,13 @@ export function PrestigeLedgerScreenOwner() {
       stateDetail: advisor.stateDetail,
       resetPreview: advisor.resetPreview,
     },
+    forecast,
     heartLawName,
     cityNamesReached,
     resolvedGateCount,
     visibleUpgrades,
     runCompassHint,
+    postResetReclaimObjective,
     });
   }, [
     advisor.resetPreview,
@@ -162,12 +169,14 @@ export function PrestigeLedgerScreenOwner() {
     canPrestigeNow,
     cityNamesReached,
     fixtureMode,
+    forecast,
     heartLawName,
     highestRealmReached,
     lastLifeSummary,
     lifetimeAP,
     prestigeCount,
     purchasesById,
+    postResetReclaimObjective,
     realm,
     resolvedGateCount,
     runCompass.v2,
@@ -220,6 +229,30 @@ export function PrestigeLedgerScreenOwner() {
     performImmediatePrestige,
   });
 
+  const handleRoutePostResetObjective = useCallback(() => {
+    const route = postResetReclaimObjective?.firstActions.find((action) => action.route)?.route ?? null;
+    if (!route) return;
+    if (route.kind === 'tab' && route.tabId) {
+      setActiveTab(route.tabId as GameTab);
+      return;
+    }
+    if (route.kind === 'status') {
+      setActiveTab('status');
+      return;
+    }
+    if (route.kind === 'world_module') {
+      setActiveTab('adventure');
+      return;
+    }
+    if (route.kind === 'life_start') {
+      setActiveTab('cultivation');
+    }
+  }, [postResetReclaimObjective, setActiveTab]);
+
+  const handleDismissPostResetObjective = useCallback(() => {
+    dismissPostResetReclaimObjective(postResetReclaimObjective?.id);
+  }, [dismissPostResetReclaimObjective, postResetReclaimObjective?.id]);
+
   const closeRitualModal = () => {
     setShowConfirmation(false);
     setRitualError(null);
@@ -318,12 +351,15 @@ export function PrestigeLedgerScreenOwner() {
         }}
         onOpenLibrary={actionController.openDecreeLibrary}
         onViewLifeSummary={actionController.viewLifeSummary}
+        onViewLastCompletedLifeSummary={() => openLifeSummaryModal('last_completed')}
         onOpenApBreakdown={actionController.openApBreakdown}
         onSelectRecommendedDecree={(upgradeId) => {
           setPurchaseError(null);
           setPurchaseSuccessMessage(null);
           actionController.inspectUpgrade(upgradeId);
         }}
+        onRoutePostResetObjective={handleRoutePostResetObjective}
+        onDismissPostResetObjective={handleDismissPostResetObjective}
       />
 
       <PrestigeDecreeLibraryDrawer
diff --git a/src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts b/src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts
index c5f06e9d..74b2fc44 100644
--- a/src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts
+++ b/src/features/prestige/prestigeLedgerExact/buildPrestigeLedgerExactSurface.ts
@@ -18,6 +18,7 @@ import type {
   PrestigeLedgerSealState,
   PrestigeRecommendedDecreeCard,
 } from './prestigeLedgerExactTypes.js';
+import type { PrestigeForecastSurfaceV2 } from '../prestigeForecastSurface.js';
 
 const QUALITY_NAMES: Record<number, string> = {
   1: 'Mortal',
@@ -162,6 +163,7 @@ const buildRecommendationCard = (args: {
   nextCost: number | null;
   affordance: PrestigeRecommendedDecreeCard['affordance'];
   reasonLine?: string;
+  runtimeHonesty?: PrestigeRecommendedDecreeCard['runtimeHonesty'];
 }): PrestigeRecommendedDecreeCard => {
   const alias = PRESTIGE_LEDGER_RECOMMENDATION_ALIASES[args.upgrade.id];
   const isMaxed = args.currentLevel >= args.upgrade.maxLevel;
@@ -175,11 +177,29 @@ const buildRecommendationCard = (args: {
     whyLine: args.reasonLine ?? alias?.whyLine ?? 'Improves reclaim pacing in next life.',
     sealTone: alias?.sealTone ?? 'ink',
     affordance,
+    runtimeHonesty: args.runtimeHonesty ?? 'live',
+    levelLine: `Lv ${args.currentLevel}/${args.upgrade.maxLevel ?? args.currentLevel + 1}`,
     button: affordance === 'info' ? undefined : buildButton('Inspect', true, 'secondary'),
   };
 };
 
 const buildLiveRecommendations = (input: PrestigeLedgerExactLiveInput): PrestigeRecommendedDecreeCard[] => {
+  const forecastCards = (input.forecast?.recommendedPurchases ?? []).slice(0, 3).flatMap((line) => {
+    const upgrade = input.visibleUpgrades.find((candidate) => candidate.id === line.upgradeId);
+    if (!upgrade || line.runtimeHonesty !== 'live') return [];
+    return buildRecommendationCard({
+      upgrade,
+      currentLevel: line.currentLevel,
+      nextCost: line.cost,
+      affordance: line.affordance === 'buy_now' ? 'buy_now' : line.affordance === 'owned' ? 'owned' : 'save_for_next',
+      reasonLine: line.reason,
+      runtimeHonesty: line.runtimeHonesty,
+    });
+  });
+  if (forecastCards.length > 0) {
+    return forecastCards;
+  }
+
   const planned = buildPrestigeStarterSpendPlan({
     apBudget: input.prestige.totalAP,
     purchasedLevels: input.prestige.purchasesById,
@@ -198,6 +218,7 @@ const buildLiveRecommendations = (input: PrestigeLedgerExactLiveInput): Prestige
       nextCost: candidate.nextCost,
       affordance: 'buy_now',
       reasonLine: candidate.reasonLine,
+      runtimeHonesty: 'live',
     });
   });
 
@@ -222,6 +243,7 @@ const buildLiveRecommendations = (input: PrestigeLedgerExactLiveInput): Prestige
       nextCost,
       affordance: nextCost === null ? 'locked' : 'save_for_next',
       reasonLine: PRESTIGE_LEDGER_RECOMMENDATION_ALIASES[upgrade.id]?.whyLine,
+      runtimeHonesty: 'live',
     }));
     used.add(upgrade.id);
   }
@@ -234,6 +256,32 @@ const buildLiveRecommendations = (input: PrestigeLedgerExactLiveInput): Prestige
   return cards.slice(0, 3);
 };
 
+const formatBucket = (line: { label: string; detail?: string }) =>
+  line.detail ? `${line.label}: ${line.detail}` : line.label;
+
+const buildResetTabletsFromForecast = (forecast: PrestigeForecastSurfaceV2) => [
+  {
+    title: 'Resets this life',
+    tone: 'reset' as const,
+    bullets: forecast.resetBuckets.slice(0, 5).map(formatBucket),
+  },
+  {
+    title: 'Carries forward',
+    tone: 'carry' as const,
+    bullets: forecast.carryBuckets.slice(0, 5).map(formatBucket),
+  },
+  {
+    title: 'Rebuilt next life',
+    tone: 'rebuild' as const,
+    bullets: forecast.rebuiltBuckets.slice(0, 5).map(formatBucket),
+  },
+  {
+    title: 'Hybrid retained',
+    tone: 'hybrid' as const,
+    bullets: forecast.hybridBuckets.slice(0, 3).map(formatBucket),
+  },
+];
+
 const buildResetTabletsFromPreview = (preview: PrestigeLedgerExactLiveInput['advisor']['resetPreview']) => [
   {
     title: 'Resets this life',
@@ -250,8 +298,32 @@ const buildResetTabletsFromPreview = (preview: PrestigeLedgerExactLiveInput['adv
     tone: 'rebuild' as const,
     bullets: (preview.rebuiltNextLife.length > 0 ? preview.rebuiltNextLife : ['New life-start flow']).slice(0, 4),
   },
+  {
+    title: 'Hybrid retained',
+    tone: 'hybrid' as const,
+    bullets: ['Mastery memory requires a purchased retention decree.'],
+  },
 ];
 
+const fixtureForecast: PrestigeLedgerExactSurfaceV1['forecast'] = {
+  title: 'Reclaim Forecast',
+  lines: [{
+    id: 'fixture_reclaim',
+    label: 'Core reclaim',
+    previous: 'Previous life: 58m',
+    expectedNext: '35m-45m',
+    reason: 'AP Qi decree and prior-life route memory.',
+    confidence: 'medium',
+  }],
+  objective: {
+    id: 'fixture_objective',
+    label: 'Choose path and reclaim Foundation',
+    detail: 'Begin with life setup, then reclaim the first gate faster.',
+    route: { kind: 'tab', tabId: 'cultivation', anchor: 'life_setup' },
+  },
+  warnings: ['Combat does not progress offline.'],
+};
+
 export function createPrestigeLedgerExactMockupFixture(): PrestigeLedgerExactSurfaceV1 {
   return {
     meta: {
@@ -292,6 +364,7 @@ export function createPrestigeLedgerExactMockupFixture(): PrestigeLedgerExactSur
       projectedGainLabel: 'Projected gain',
       projectedGainValue: '+21 AP',
       viewLifeSummaryButton: buildButton('View Life Summary', true, 'secondary'),
+      viewLastLifeSummaryButton: buildButton('View Last Life Summary', false, 'secondary', 'No completed life summary is stored yet.'),
     },
     reincarnationDecree: {
       title: 'Reincarnation Decree',
@@ -346,6 +419,7 @@ export function createPrestigeLedgerExactMockupFixture(): PrestigeLedgerExactSur
       title: 'Reset Contract',
       tablets: PRESTIGE_LEDGER_RESET_TABLETS.map((tablet) => ({ ...tablet, bullets: [...tablet.bullets] })),
     },
+    forecast: fixtureForecast,
   };
 }
 
@@ -354,6 +428,7 @@ export function buildPrestigeLedgerExactSurfaceFromStores(input: PrestigeLedgerE
   const wall = currentWallFor(input);
   const sealState = resolveSealState(input);
   const canReview = input.prestige.canPrestige && input.prestige.apGain > 0;
+  const forecast = input.forecast;
 
   return {
     meta: {
@@ -398,6 +473,12 @@ export function buildPrestigeLedgerExactSurfaceFromStores(input: PrestigeLedgerE
       projectedGainLabel: 'Projected gain',
       projectedGainValue: formatAp(input.prestige.apGain),
       viewLifeSummaryButton: buildButton('View Life Summary', true, 'secondary'),
+      viewLastLifeSummaryButton: buildButton(
+        'View Last Life Summary',
+        input.prestige.hasLastLifeSummary,
+        'secondary',
+        input.prestige.hasLastLifeSummary ? undefined : 'No completed life summary is stored yet.',
+      ),
     },
     reincarnationDecree: {
       title: 'Reincarnation Decree',
@@ -415,15 +496,38 @@ export function buildPrestigeLedgerExactSurfaceFromStores(input: PrestigeLedgerE
       secondaryAction: buildButton('Open Decree Library', true, 'secondary'),
     },
     nextLifeRail: {
-      previewBullets: [...PRESTIGE_LEDGER_NEXT_LIFE_PREVIEW],
+      previewBullets: [
+        forecast?.postResetObjective.detail,
+        ...(forecast?.retainedEffects.slice(0, 2).map((line) => line.label) ?? []),
+        ...PRESTIGE_LEDGER_NEXT_LIFE_PREVIEW,
+      ].filter((line): line is string => Boolean(line)).slice(0, 4),
       recommendedDecrees: buildLiveRecommendations(input),
       openLibraryButton: buildButton('Open Full Decree Library', true, 'secondary'),
     },
     resetContract: {
       title: 'Reset Contract',
-      tablets: buildResetTabletsFromPreview(input.advisor.resetPreview),
+      tablets: forecast ? buildResetTabletsFromForecast(forecast) : buildResetTabletsFromPreview(input.advisor.resetPreview),
+    },
+    forecast: {
+      title: 'Reclaim Forecast',
+      lines: forecast?.reclaimForecast ?? [{
+        id: 'fallback_reclaim_forecast',
+        label: 'Reclaim forecast unavailable',
+        previous: 'No prior milestone time recorded',
+        expectedNext: 'Unavailable until the V2 forecast is provided',
+        reason: 'Legacy callers supplied only the advisor surface.',
+        confidence: 'low',
+      }],
+      objective: forecast?.postResetObjective ?? {
+        id: 'fallback_objective',
+        label: 'Review the next life',
+        detail: 'Open Cultivation after reset and rebuild from live state.',
+        route: { kind: 'tab', tabId: 'cultivation' },
+      },
+      warnings: forecast?.warnings ?? [],
     },
     runCompassHint: input.runCompassHint ?? null,
+    postResetReclaimObjective: input.postResetReclaimObjective ?? null,
     debug: {
       notes: [
         `Live recommendations drawn from ${input.visibleUpgrades.length} visible runtime prestige upgrades.`,
diff --git a/src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts b/src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts
index 4ab95572..9147b05f 100644
--- a/src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts
+++ b/src/features/prestige/prestigeLedgerExact/prestigeLedgerExactTypes.ts
@@ -2,6 +2,8 @@ import type { PrestigeUpgradeDef } from '../../../content/index.js';
 import type { ApBreakdown } from '../../../stores/prestigeStore.js';
 import type { CultivationPath, Realm, SpiritRoot } from '../../../types/index.js';
 import type { PrestigeAdvisorStateLabel, PrestigeResetPreviewBuckets } from '../prestigeAdvisorSurface.js';
+import type { PrestigeForecastSurfaceV2 } from '../prestigeForecastSurface.js';
+import type { PostResetReclaimObjectiveSurface } from '../postResetReclaimObjectiveSurface.js';
 
 export type PrestigeLedgerButtonVariant = 'primary' | 'secondary' | 'danger' | 'disabled';
 
@@ -29,6 +31,8 @@ export type PrestigeRecommendedDecreeCard = {
   whyLine: string;
   sealTone: 'jade' | 'gold' | 'cinnabar' | 'ink';
   affordance: 'buy_now' | 'save_for_next' | 'locked' | 'owned' | 'info';
+  runtimeHonesty?: 'live' | 'deferred' | 'unsupported_hidden' | 'unknown_blocked';
+  levelLine?: string;
   button?: PrestigeLedgerButtonSurface;
 };
 
@@ -64,6 +68,7 @@ export type PrestigeLedgerExactSurfaceV1 = {
     projectedGainLabel: string;
     projectedGainValue: string;
     viewLifeSummaryButton: PrestigeLedgerButtonSurface;
+    viewLastLifeSummaryButton: PrestigeLedgerButtonSurface;
   };
   reincarnationDecree: {
     title: 'Reincarnation Decree';
@@ -82,7 +87,13 @@ export type PrestigeLedgerExactSurfaceV1 = {
   };
   resetContract: {
     title: 'Reset Contract';
-    tablets: { title: string; bullets: string[]; tone: 'reset' | 'carry' | 'rebuild' }[];
+    tablets: { title: string; bullets: string[]; tone: 'reset' | 'carry' | 'rebuild' | 'hybrid' }[];
+  };
+  forecast: {
+    title: 'Reclaim Forecast';
+    lines: PrestigeForecastSurfaceV2['reclaimForecast'];
+    objective: PrestigeForecastSurfaceV2['postResetObjective'];
+    warnings: string[];
   };
   runCompassHint?: {
     milestoneLabel: string;
@@ -92,6 +103,7 @@ export type PrestigeLedgerExactSurfaceV1 = {
     active: boolean;
     recentDeltaLine: string | null;
   } | null;
+  postResetReclaimObjective?: PostResetReclaimObjectiveSurface | null;
   debug?: { notes: string[] };
 };
 
@@ -121,17 +133,22 @@ export type PrestigeLedgerExactLiveInput = {
     realm: Realm;
   };
   advisor: PrestigeLedgerExactAdvisorInput;
+  forecast?: PrestigeForecastSurfaceV2;
   heartLawName: string | null;
   cityNamesReached: string[];
   resolvedGateCount: number;
   visibleUpgrades: PrestigeUpgradeDef[];
   runCompassHint?: PrestigeLedgerExactSurfaceV1['runCompassHint'];
+  postResetReclaimObjective?: PostResetReclaimObjectiveSurface | null;
 };
 
 export type PrestigeLedgerExactScreenActions = {
   onReviewAndReincarnate: () => void;
   onOpenLibrary: () => void;
   onViewLifeSummary: () => void;
+  onViewLastCompletedLifeSummary: () => void;
   onOpenApBreakdown: () => void;
   onSelectRecommendedDecree: (upgradeId: string) => void;
+  onRoutePostResetObjective: () => void;
+  onDismissPostResetObjective: () => void;
 };
diff --git a/src/services/diagnostics/release/buildAuditReport.ts b/src/services/diagnostics/release/buildAuditReport.ts
index bf3c451a..bb12d2c0 100644
--- a/src/services/diagnostics/release/buildAuditReport.ts
+++ b/src/services/diagnostics/release/buildAuditReport.ts
@@ -40,9 +40,29 @@ function detectSourceFamily(line: string): BuildAuditSourceFamily {
   return 'unknown';
 }
 
+function stripAnsi(line: string): string {
+  return line.replace(/\x1b\[[0-9;]*m/g, '');
+}
+
+function isViteAssetListingLine(line: string): boolean {
+  const normalized = stripAnsi(line).trim().replace(/\\/g, '/');
+  return /^dist\/assets\/\S+\s+\d+(?:\.\d+)?\s+kB\b/i.test(normalized);
+}
+
 function detectLevel(line: string): BuildAuditLevel | null {
-  const lower = line.toLowerCase();
-  const isWarningLine = lower.includes('[warning]') || lower.startsWith('npm warn') || lower.includes(' warning ');
+  if (isViteAssetListingLine(line)) return null;
+
+  const normalized = stripAnsi(line).trim();
+  const lower = normalized.toLowerCase();
+  const isWarningLine =
+    lower.includes('[warning]')
+    || lower.startsWith('npm warn')
+    || lower.startsWith('warning:')
+    || /^[a-z]+warning:/i.test(normalized)
+    || lower.includes('experimentalwarning:')
+    || lower.startsWith('browserslist:')
+    || lower.includes("didn't resolve at build time")
+    || lower.startsWith('(!) some chunks are larger');
   if (
     isWarningLine
     && !lower.includes('permission denied')
@@ -63,12 +83,11 @@ function detectLevel(line: string): BuildAuditLevel | null {
     || lower.includes('enoent')
     || lower.includes('spawn ')
   ) return 'blocker';
-  if (lower.includes('warning') || lower.startsWith('npm warn')) return 'warning';
   return null;
 }
 
 function summarize(line: string): string {
-  return line
+  return stripAnsi(line)
     .replace(/^\s*#?\s*/, '')
     .replace(/\(node:\d+\)/gi, '(node:PID)')
     .replace(/\bpid[:=]?\s*\d+\b/gi, 'pid=PID')
diff --git a/src/services/prestige/PrestigeResetService.ts b/src/services/prestige/PrestigeResetService.ts
index 07e0193e..238baf47 100644
--- a/src/services/prestige/PrestigeResetService.ts
+++ b/src/services/prestige/PrestigeResetService.ts
@@ -20,6 +20,7 @@ import { useTrialStore } from '../../stores/trialStore.js';
 import { useZoneStore } from '../../stores/zoneStore.js';
 import { useUIStore } from '../../stores/uiStore.js';
 import { hardResetBreakthroughEchoes } from '../../features/breakthroughEchoes/index.js';
+import { deriveMasteryRetentionCarryOver } from './PrestigeResetContract.js';
 
 export interface PrestigeResetOptions {
   resetGameRun: () => void;
@@ -39,18 +40,6 @@ export interface PrestigeResetSummary {
   };
 }
 
-const MASTERY_RETENTION_BY_NODE_ID: Readonly<Record<string, number>> = {
-  ap_mastery_retention_10: 0.1,
-  ap_mastery_retention_25: 0.25,
-  ap_mastery_retention_50: 0.5,
-};
-
-export const deriveMasteryRetentionCarryOver = (purchasesById: Record<string, number>): number =>
-  Object.entries(MASTERY_RETENTION_BY_NODE_ID).reduce((best, [nodeId, ratio]) => {
-    const purchasedLevels = purchasesById[nodeId] ?? 0;
-    return purchasedLevels > 0 ? Math.max(best, ratio) : best;
-  }, 0);
-
 const snapshotTechniqueMasteryXp = (): Record<string, number> => {
   const unlocked = useTechCollectionStore.getState().unlockedTechs;
   return Object.fromEntries(
diff --git a/src/services/time/OfflineCatchup.ts b/src/services/time/OfflineCatchup.ts
index 4668caaf..0cc9c29b 100644
--- a/src/services/time/OfflineCatchup.ts
+++ b/src/services/time/OfflineCatchup.ts
@@ -11,6 +11,7 @@ import { cultivationService } from '../cultivationService.js';
 import { buildCultivationConsumableCarryoverWindows } from '../../systems/consumables/cultivationConsumableEffects.js';
 import { getHeartLawBonuses } from '../../systems/heartLaw/heartLawLogic.js';
 import { getExpeditionReadinessDelta, getQueuedActionReadinessDelta } from '../../systems/offline/offlineSummaryReadModel.js';
+import { buildOfflineCatchupSurface, type OfflineCatchupSurfaceV1 } from '../../systems/offline/offlineCatchupSurface.js';
 
 export interface OfflineCatchupSummaryPart {
   kind: 'qi_gained' | 'queued_actions' | 'expeditions';
@@ -18,19 +19,33 @@ export interface OfflineCatchupSummaryPart {
   value: string;
 }
 
+export interface OfflineEfficiencySource {
+  id: string;
+  label: string;
+  value: string;
+}
+
 export interface OfflineCatchupSummary {
   offlineSeconds: number;
+  rawOfflineSeconds?: number;
+  maxOfflineSeconds?: number;
   offlineDuration: string;
   efficiency: number;
+  efficiencySources?: OfflineEfficiencySource[];
   wasCapped: boolean;
   parts: OfflineCatchupSummaryPart[];
 }
 
 export interface OfflineCatchupResult {
   summary: OfflineCatchupSummary | null;
+  surface: OfflineCatchupSurfaceV1 | null;
 }
 
-function getLiveOfflineEfficiency(): number {
+function formatPercent(value: number): string {
+  return `${Math.round(value * 100)}%`;
+}
+
+function getLiveOfflineEfficiency(): { value: number; sources: OfflineEfficiencySource[] } {
   const prestigeStore = usePrestigeStore.getState();
   const cultivationStore = useCultivationStore.getState();
   const heartLawId = cultivationStore.selectedHeartLawId;
@@ -41,10 +56,24 @@ function getLiveOfflineEfficiency(): number {
     spiritRoot: prestigeStore.spiritRoot,
   }).offlineEfficiencyAdd;
 
-  return resolveOfflineCultivationEfficiency({
-    prestigeEfficiencyAdd: prestigeStore.getOfflineEfficiencyBonusAdditive(),
+  const prestigeEfficiencyAdd = prestigeStore.getOfflineEfficiencyBonusAdditive();
+  const value = resolveOfflineCultivationEfficiency({
+    prestigeEfficiencyAdd,
     heartLawBonus,
   });
+  const sources: OfflineEfficiencySource[] = [
+    { id: 'base', label: 'Base idle settlement', value: formatPercent(resolveOfflineCultivationEfficiency({ prestigeEfficiencyAdd: 0, heartLawBonus: 0 })) },
+  ];
+  if (prestigeEfficiencyAdd > 0) {
+    sources.push({ id: 'prestige', label: 'Prestige decree', value: `+${formatPercent(prestigeEfficiencyAdd)}` });
+  }
+  if (heartLawBonus > 0) {
+    sources.push({ id: 'heart_law', label: 'Heart Law', value: `+${formatPercent(heartLawBonus)}` });
+  }
+  if (sources.length === 1) {
+    sources.push({ id: 'no_bonus', label: 'No active offline bonus', value: '+0%' });
+  }
+  return { value, sources };
 }
 
 function calculateOfflineQiGain(startAt: number, endAt: number, offlineEfficiency: number) {
@@ -67,7 +96,14 @@ function calculateOfflineQiGain(startAt: number, endAt: number, offlineEfficienc
 
 export function apply(context: OfflineContext): OfflineCatchupResult {
   if (context.dtMs <= 0) {
-    return { summary: null };
+    return {
+      summary: null,
+      surface: buildOfflineCatchupSurface({
+        summary: null,
+        generatedAt: context.now,
+        rawSeconds: Math.floor(context.rawMs / 1000),
+      }),
+    };
   }
 
   const seconds = Math.floor(Math.min(context.dtMs, MAX_OFFLINE_MS) / 1000);
@@ -76,7 +112,8 @@ export function apply(context: OfflineContext): OfflineCatchupResult {
 
   const gameStore = useGameStore.getState();
   useGameStore.setState({ lastActiveTime: context.now, lastTickTime: context.now });
-  const offlineEfficiency = getLiveOfflineEfficiency();
+  const offlineEfficiencyDetail = getLiveOfflineEfficiency();
+  const offlineEfficiency = offlineEfficiencyDetail.value;
   const qiGain = calculateOfflineQiGain(startAt, context.now, offlineEfficiency);
   if (qiGain.greaterThan(0)) {
     const nextQi = D(gameStore.qi ?? '0').plus(qiGain);
@@ -126,13 +163,23 @@ export function apply(context: OfflineContext): OfflineCatchupResult {
     summaryParts.push({ kind: 'expeditions', label: 'Expeditions ready', value: `${expeditionReadiness.newlyComplete}` });
   }
 
-  return {
-    summary: {
+  const summary: OfflineCatchupSummary = {
       offlineSeconds: seconds,
+      rawOfflineSeconds: Math.floor(context.rawMs / 1000),
+      maxOfflineSeconds: MAX_OFFLINE_MS / 1000,
       offlineDuration: formatOfflineDuration(seconds),
       efficiency: offlineEfficiency,
+      efficiencySources: offlineEfficiencyDetail.sources,
       wasCapped: context.wasCapped,
       parts: summaryParts,
-    },
+    };
+
+  return {
+    summary,
+    surface: buildOfflineCatchupSurface({
+      summary,
+      generatedAt: context.now,
+      rawSeconds: Math.floor(context.rawMs / 1000),
+    }),
   };
 }
diff --git a/src/stores/prestigeStore.ts b/src/stores/prestigeStore.ts
index d6cff81f..b7b52827 100644
--- a/src/stores/prestigeStore.ts
+++ b/src/stores/prestigeStore.ts
@@ -22,6 +22,10 @@ import {
   extractLiveTrialIds,
   resolvePrestigeAdvisorLabel,
 } from '../systems/prestige/prestigeApReadModel.js';
+import {
+  buildPostResetReclaimObjectiveSurface,
+  type PostResetReclaimObjectiveSurface,
+} from '../features/prestige/postResetReclaimObjectiveSurface.js';
 
 /**
  * Lazy getter for game store to avoid circular dependency
@@ -128,6 +132,7 @@ interface PrestigeState {
   // Spirit root
   spiritRoot: SpiritRoot | null;
   lastLifeSummary: PrestigeLifeSummarySnapshot | null;
+  postResetReclaimObjective: PostResetReclaimObjectiveSurface | null;
 
   // Methods
   calculateAPGain: () => number;
@@ -157,6 +162,7 @@ interface PrestigeState {
   getSpiritRootPurityMultiplier: () => number;
   getSpiritRootTotalMultiplier: () => number;
   hardResetPrestige: () => void;
+  dismissPostResetReclaimObjective: (objectiveId?: string) => void;
 }
 
 /**
@@ -197,6 +203,7 @@ const createInitialPrestigeState = () => ({
   rerollCount: 0,
   spiritRoot: null as SpiritRoot | null,
   lastLifeSummary: null as PrestigeLifeSummarySnapshot | null,
+  postResetReclaimObjective: null as PostResetReclaimObjectiveSurface | null,
 });
 
 function getUpgradesFromContent(): PrestigeUpgradeDef[] {
@@ -279,18 +286,39 @@ export const usePrestigeStore = create<PrestigeState>()(
         }),
       );
       const lifeSummarySnapshot: PrestigeLifeSummarySnapshot = preparedLifeSummary ?? {
+        version: 2,
         capturedAt: Date.now(),
+        lifeOrdinal: state.prestigeCount + 1,
         advisorLabel: resolvePrestigeAdvisorLabel(fallbackForecast),
         apForecastGain: fallbackForecast.totalAp,
         apAfterRitual: state.totalAP + fallbackForecast.totalAp,
+        headline: 'Life summary was captured from runtime fallback.',
+        summarySeal: 'quiet_life',
         blocks: [
           { key: 'life_arc', title: 'Life Arc', lines: ['Life summary was captured from runtime fallback.'] },
+          { key: 'breakthrough_echoes', title: 'Breakthrough Echoes', lines: ['Breakthrough echoes were not captured for this ritual call.'] },
           { key: 'doctrine_build', title: 'Doctrine & Build', lines: ['Doctrine details were not captured for this ritual call.'] },
           { key: 'world_progress', title: 'World Progress', lines: ['World progress details were not captured for this ritual call.'] },
           { key: 'gate_trials', title: 'Gate Trials', lines: ['Gate-trial details were not captured for this ritual call.'] },
           { key: 'ruins_supply', title: 'Ruins & Supply', lines: ['Ruins and supply details were not captured for this ritual call.'] },
+          { key: 'economy_support', title: 'Economy Support', lines: ['Economy support details were not captured for this ritual call.'] },
+          { key: 'offline_background', title: 'Offline & Background', lines: ['Offline details were not captured for this ritual call.'] },
           { key: 'next_life_focus', title: 'Next Life Focus', lines: ['Open the live Life Summary panel for full guidance.'] },
         ],
+        majorMemories: [{
+          id: 'fallback_summary',
+          title: 'Fallback Summary',
+          detail: 'The ritual call did not provide a prepared life summary.',
+          source: 'prestige',
+          summaryEligible: true,
+        }],
+        nextLifeFocus: {
+          id: 'fallback_reclaim',
+          label: 'Review the new life',
+          detail: 'Choose path and Heart Law, then rebuild the reclaim route from live state.',
+          route: { kind: 'tab', tabId: 'cultivation', anchor: 'life_setup' },
+        },
+        warnings: ['Emergency fallback summary was used.'],
       };
 
       if (!state.canPrestige()) return;
@@ -316,6 +344,10 @@ export const usePrestigeStore = create<PrestigeState>()(
         state.highestRealmReached = 0;
         state.runStartTime = Date.now();
         state.lastLifeSummary = lifeSummarySnapshot;
+        state.postResetReclaimObjective = buildPostResetReclaimObjectiveSurface({
+          sourceSummary: lifeSummarySnapshot,
+          purchasesById: state.purchasesById,
+        });
       });
       GameEvents.emit({
         type: 'prestige/performed',
@@ -594,6 +626,15 @@ export const usePrestigeStore = create<PrestigeState>()(
       });
       get().initializeUpgrades();
     },
+
+    dismissPostResetReclaimObjective: (objectiveId) => {
+      const current = get().postResetReclaimObjective;
+      if (!current) return;
+      if (objectiveId && current.id !== objectiveId) return;
+      set((state) => {
+        state.postResetReclaimObjective = null;
+      });
+    },
   }))
 );
 
diff --git a/src/systems/offline.ts b/src/systems/offline.ts
index d0d1eb03..3cd3714c 100644
--- a/src/systems/offline.ts
+++ b/src/systems/offline.ts
@@ -17,6 +17,14 @@ import {
 
 export { formatOfflineDuration };
 
+/**
+ * P4 offline trust note:
+ * OfflineCatchup.apply() is the single mutating catchup implementation. This
+ * module keeps legacy calculation helpers and thin wrappers for callers that
+ * still import src/systems/offline.ts; wrappers must delegate to OfflineCatchup
+ * instead of applying queue, expedition, combat, or Qi mutation themselves.
+ */
+
 /**
  * Offline progress result
  */
diff --git a/src/systems/prestige/runtime/prestigeRuntimeCatalog.ts b/src/systems/prestige/runtime/prestigeRuntimeCatalog.ts
index 4ff2eeca..277a489b 100644
--- a/src/systems/prestige/runtime/prestigeRuntimeCatalog.ts
+++ b/src/systems/prestige/runtime/prestigeRuntimeCatalog.ts
@@ -25,6 +25,42 @@ export interface PrestigeRuntimeCatalog {
   legacyResidueNodeIds: string[];
 }
 
+export type PrestigeEffectRuntimeStatus =
+  | 'visible_live'
+  | 'visible_partial'
+  | 'deferred'
+  | 'hidden_unsupported'
+  | 'unknown_blocked';
+
+export type PrestigeEffectRuntimeHonesty =
+  | 'live'
+  | 'deferred'
+  | 'unsupported_hidden'
+  | 'unknown_blocked';
+
+export type PrestigeEffectAuditRow = {
+  upgradeId: string;
+  upgradeName: string;
+  status: PrestigeEffectRuntimeStatus;
+  contentEffectKeys: string[];
+  runtimeConsumers: string[];
+  purchaseAllowed: boolean;
+  displayAllowed: boolean;
+  reason: string;
+  remediation: 'none' | 'hide' | 'label_deferred' | 'implement_consumer' | 'fix_content_key';
+};
+
+export type PrestigeEffectAuditReport = {
+  generatedAt: number;
+  rows: PrestigeEffectAuditRow[];
+  visibleLiveCount: number;
+  deferredCount: number;
+  hiddenUnsupportedCount: number;
+  unknownBlockedCount: number;
+  blockers: string[];
+  warnings: string[];
+};
+
 const classifyRuntimeStatus = (
   upgrade: PrestigeUpgradeDef,
 ): PrestigeRuntimeNode => {
@@ -136,3 +172,89 @@ export const getPurchasedVisiblePrestigeNodeIds = (
     .filter(([nodeId, level]) => visibleIds.has(nodeId) && level > 0)
     .map(([nodeId]) => nodeId);
 };
+
+const isRecord = (value: unknown): value is Record<string, unknown> =>
+  Boolean(value) && typeof value === 'object' && !Array.isArray(value);
+
+const collectEffectKeys = (upgrade: PrestigeUpgradeDef): string[] => {
+  const keys = new Set<string>();
+  if (typeof upgrade.stat === 'string' && upgrade.stat.trim()) keys.add(upgrade.stat);
+  if (isRecord(upgrade.effect)) {
+    Object.keys(upgrade.effect).forEach((key) => keys.add(key));
+  }
+  if (isRecord(upgrade.effectPerLevel)) {
+    Object.keys(upgrade.effectPerLevel).forEach((key) => keys.add(key));
+  } else if (typeof upgrade.effectPerLevel === 'number' && upgrade.stat) {
+    keys.add(upgrade.stat);
+  }
+  if (Array.isArray(upgrade.unlocks) && upgrade.unlocks.length > 0) keys.add('unlocks');
+  return Array.from(keys).sort();
+};
+
+const toAuditStatus = (status: PrestigeNodeRuntimeStatus): PrestigeEffectRuntimeStatus => {
+  if (status === 'visible_live') return 'visible_live';
+  if (status === 'deferred') return 'deferred';
+  if (status === 'hidden_unsupported') return 'hidden_unsupported';
+  return 'unknown_blocked';
+};
+
+const reasonForAuditStatus = (node: PrestigeRuntimeNode): string => {
+  if (node.status === 'visible_live') {
+    return `Live runtime consumer(s): ${node.consumers.join(', ')}.`;
+  }
+  if (node.status === 'deferred') {
+    return 'Deferred future-slice decree; blocked from live purchasing.';
+  }
+  if (node.status === 'hidden_unsupported') {
+    return 'Content effect has no confirmed runtime consumer in this slice, so it is hidden.';
+  }
+  return 'Runtime status is unknown; blocked until content or consumer metadata is fixed.';
+};
+
+const remediationForAuditStatus = (status: PrestigeEffectRuntimeStatus): PrestigeEffectAuditRow['remediation'] => {
+  if (status === 'visible_live') return 'none';
+  if (status === 'deferred') return 'label_deferred';
+  if (status === 'hidden_unsupported') return 'hide';
+  return 'fix_content_key';
+};
+
+export function buildPrestigeEffectAuditReport(
+  content: ValidatedContent | null | undefined,
+  options?: { generatedAt?: number },
+): PrestigeEffectAuditReport {
+  const catalog = getPrestigeRuntimeCatalog(content);
+  const rows = catalog.nodes.map((node): PrestigeEffectAuditRow => {
+    const status = toAuditStatus(node.status);
+    const purchaseAllowed = status === 'visible_live';
+    const displayAllowed = status === 'visible_live' || status === 'deferred';
+    return {
+      upgradeId: node.upgrade.id,
+      upgradeName: node.upgrade.name ?? node.upgrade.id,
+      status,
+      contentEffectKeys: collectEffectKeys(node.upgrade),
+      runtimeConsumers: node.consumers,
+      purchaseAllowed,
+      displayAllowed,
+      reason: reasonForAuditStatus(node),
+      remediation: remediationForAuditStatus(status),
+    };
+  }).sort((left, right) => left.upgradeId.localeCompare(right.upgradeId));
+
+  const blockers = rows
+    .filter((row) => row.purchaseAllowed && row.runtimeConsumers.length === 0)
+    .map((row) => `${row.upgradeId} is purchasable without a runtime consumer.`);
+  const warnings = rows
+    .filter((row) => row.status === 'unknown_blocked')
+    .map((row) => `${row.upgradeId} is unknown and blocked.`);
+
+  return {
+    generatedAt: options?.generatedAt ?? Date.now(),
+    rows,
+    visibleLiveCount: rows.filter((row) => row.status === 'visible_live').length,
+    deferredCount: rows.filter((row) => row.status === 'deferred').length,
+    hiddenUnsupportedCount: rows.filter((row) => row.status === 'hidden_unsupported').length,
+    unknownBlockedCount: rows.filter((row) => row.status === 'unknown_blocked').length,
+    blockers,
+    warnings,
+  };
+}
diff --git a/src/systems/readiness/section5Adapters.ts b/src/systems/readiness/section5Adapters.ts
index 51bc9451..c48d1773 100644
--- a/src/systems/readiness/section5Adapters.ts
+++ b/src/systems/readiness/section5Adapters.ts
@@ -145,6 +145,10 @@ function buildPatchedProgressByTrialId(trialId: TrialId): ReturnType<typeof useT
   ) as ReturnType<typeof useTrialStore.getState>['progressByTrialId'];
 }
 
+function getTrialProgressSnapshot(trialId: TrialId) {
+  return normalizeTrialProgress(useTrialStore.getState().progressByTrialId[trialId] ?? createDefaultTrialProgress());
+}
+
 function buildBypassAvailability(trialId: TrialId): boolean {
   const trial = useContentStore.getState().maps.trialsById[trialId] ?? null;
   const content = useContentStore.getState().raw;
@@ -153,7 +157,7 @@ function buildBypassAvailability(trialId: TrialId): boolean {
   const lifecycle = getTrialLifecycleSnapshot({
     content,
     trial,
-    progress: useTrialStore.getState().getProgress(trialId),
+    progress: getTrialProgressSnapshot(trialId),
     realm: game.realm,
     qi: game.qi,
     breakthroughRequirement: game.getBreakthroughRequirement(),
@@ -254,7 +258,7 @@ export function buildSection5ReadinessSurface(
 
     const resolvedSnapshot = snapshot ?? buildDoctrineSnapshot();
     const build = analyzeSelectedBuild(resolvedSnapshot);
-    const progress = useTrialStore.getState().getProgress(resolvedTrialId);
+    const progress = getTrialProgressSnapshot(resolvedTrialId);
     const lastAttemptSummary = progress.lastAttemptSummary ?? null;
     const readiness = buildTrialGateReadinessResult(resolvedTrialId, resolvedSnapshot);
     const bypassAvailable = buildBypassAvailability(resolvedTrialId);
@@ -294,7 +298,7 @@ export function buildSection5StatusSurface(
 
     let currentDiagnosis: FailureDiagnosis | null = null;
     if (currentGateTrialId && readiness) {
-      const progress = useTrialStore.getState().getProgress(currentGateTrialId);
+      const progress = getTrialProgressSnapshot(currentGateTrialId);
       if (progress.lastAttemptSummary) {
         currentDiagnosis = diagnoseTrialFailure({
           trialId: currentGateTrialId,
@@ -337,7 +341,7 @@ export function buildSection5PostFailureSurface(trialId: string): Section5PostFa
   const trialDef = useContentStore.getState().maps.trialsById[readinessSurface.trialId] ?? null;
   const content = useContentStore.getState().raw;
   const game = useGameStore.getState();
-  const progress = useTrialStore.getState().getProgress(readinessSurface.trialId);
+  const progress = getTrialProgressSnapshot(readinessSurface.trialId);
   const requiredItemSatisfied = !trialDef?.requiredItemId || useInventoryStore.getState().getItemCount(trialDef.requiredItemId) > 0;
   const lifecycle = getTrialLifecycleSnapshot({
     content,
@@ -436,7 +440,7 @@ export function buildGateTrialReadinessSurface(trialId: string): GateTrialReadin
   const game = useGameStore.getState();
   const trialDef = maps.trialsById[resolvedTrialId] ?? null;
   if (!trialDef) return null;
-  const progress = useTrialStore.getState().getProgress(resolvedTrialId);
+  const progress = getTrialProgressSnapshot(resolvedTrialId);
   const requiredItemCount = trialDef.requiredItemId ? useInventoryStore.getState().getItemCount(trialDef.requiredItemId) : 0;
   const requiredItemSatisfied = !trialDef.requiredItemId || requiredItemCount > 0;
   const lifecycle = getTrialLifecycleSnapshot({
diff --git a/src/systems/ui/runCompass/buildRunCompassSurfaceV2.ts b/src/systems/ui/runCompass/buildRunCompassSurfaceV2.ts
index 036cafdb..612a0673 100644
--- a/src/systems/ui/runCompass/buildRunCompassSurfaceV2.ts
+++ b/src/systems/ui/runCompass/buildRunCompassSurfaceV2.ts
@@ -5,7 +5,7 @@ import { useGameStore } from '../../../stores/gameStore.js';
 import { useInventoryStore } from '../../../stores/inventoryStore.js';
 import { usePrestigeStore } from '../../../stores/prestigeStore.js';
 import { useCultivationStore } from '../../../stores/cultivationStore.js';
-import { useTrialStore } from '../../../stores/trialStore.js';
+import { createDefaultTrialProgress, normalizeTrialProgress, useTrialStore } from '../../../stores/trialStore.js';
 import { buildLiveEconomicRecommendationEngine } from '../../economy/economicRecommendationEngine.js';
 import type { EconomicRecommendationCandidate, EconomicShortfall } from '../../economy/economicRecommendationTypes.js';
 import { buildSupportEconomySurfaceModel } from '../../economy/supportEconomySurfaceModel.js';
@@ -346,7 +346,9 @@ export function buildLiveRunCompassSurfaceV2(): RunCompassSurfaceV2 | null {
     const nextRealm = getNextLiveRealm(currentRealmIndex);
     const activeTransition = economic.snapshot.phase.currentGateTransition;
     const activeTrial = activeTransition ? contentStore.maps.trialsById[activeTransition.trialId] ?? null : null;
-    const activeProgress = activeTrial ? trials.getProgress(activeTrial.id) : null;
+    const activeProgress = activeTrial
+      ? normalizeTrialProgress(trials.progressByTrialId[activeTrial.id] ?? createDefaultTrialProgress())
+      : null;
     const gateItemId = activeTrial ? getTrialGateItemId(content, activeTrial) : null;
     const gateItemDef = gateItemId ? contentStore.maps.itemsById[gateItemId] ?? null : null;
     const gateItemCount = gateItemId ? inventory.getItemCount(gateItemId) : 0;
@@ -518,6 +520,29 @@ export function buildLiveRunCompassSurfaceV2(): RunCompassSurfaceV2 | null {
         source: 'progression',
         priority: 2,
       });
+    } else if (prestige.postResetReclaimObjective) {
+      const reclaimAction = prestige.postResetReclaimObjective.firstActions.find((action) => action.id !== 'choose_next_life_identity')
+        ?? prestige.postResetReclaimObjective.firstActions[0]
+        ?? null;
+      milestoneState = 'cultivating';
+      milestoneLabel = prestige.postResetReclaimObjective.headline;
+      milestoneDetail = reclaimAction?.detail ?? prestige.postResetReclaimObjective.detail;
+      primaryBlocker = blocker({
+        kind: 'none',
+        label: reclaimAction?.label ?? 'Previous life memory',
+        detail: milestoneDetail,
+        severity: 'success',
+        source: 'prestige',
+      });
+      primaryRoute = tabRoute({
+        id: 'post-reset-reclaim',
+        label: reclaimAction?.label ?? 'Review Reclaim Objective',
+        detail: milestoneDetail,
+        tab: reclaimAction?.route?.kind === 'tab' && reclaimAction.route.tabId === 'prestige' ? 'prestige' : 'cultivation',
+        expectedDeltaLabel: 'New life reclaim route updates from previous-life memory.',
+        source: 'prestige',
+        priority: 4,
+      });
     } else if (atCap) {
       milestoneState = canPrestige ? 'prestige_recommended' : 'content_cap';
       milestoneLabel = 'Current authored chapter complete';
diff --git a/tests/contracts/buildWarningInventory.test.ts b/tests/contracts/buildWarningInventory.test.ts
index c830954e..fb61fca0 100644
--- a/tests/contracts/buildWarningInventory.test.ts
+++ b/tests/contracts/buildWarningInventory.test.ts
@@ -63,6 +63,24 @@ test('warning-only css syntax warnings stay warnings when build passed', () => {
   assert.equal(report.warningCount, 1);
 });
 
+test('build audit ignores Vite asset rows that contain warning in filenames', () => {
+  const sample = [
+    'dist/assets/warning_unknown_problem-DGCRrYVu.png                     174.89 kB',
+    'Browserslist: browsers data (caniuse-lite) is 6 months old. Please run:',
+    '../../assets/background/InsideDungeon.png referenced in ../../assets/background/InsideDungeon.png didn\'t resolve at build time, it will remain unchanged to be resolved at runtime',
+    '(!) Some chunks are larger than 500 kB after minification. Consider:',
+    '- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.',
+  ].join('\n');
+
+  const entries = parseBuildAuditOutput(sample);
+
+  assert.equal(entries.some((entry) => entry.summary.includes('warning_unknown_problem')), false);
+  assert.equal(entries.some((entry) => entry.summary.includes('Browserslist')), true);
+  assert.equal(entries.some((entry) => entry.summary.includes("didn't resolve at build time")), true);
+  assert.equal(entries.some((entry) => entry.summary.includes('Some chunks are larger')), true);
+  assert.equal(entries.length, 3);
+});
+
 test('build audit report + renderer keep stable release fields', () => {
   const report = buildBuildAuditReport({
     output: 'warning: sample warning\nerror: sample blocker',
diff --git a/tests/contracts/prestigeStarterSpendPlanner.test.ts b/tests/contracts/prestigeStarterSpendPlanner.test.ts
index 275eeb7b..45b85c54 100644
--- a/tests/contracts/prestigeStarterSpendPlanner.test.ts
+++ b/tests/contracts/prestigeStarterSpendPlanner.test.ts
@@ -32,6 +32,7 @@ async function loadValidatedContent() {
     bounties: await readJson('bounties.json'),
     heart_laws: await readJson('heart_laws.json'),
     prestige_store: await readJson('prestige_store.json'),
+    pavilion_records: await readJson('pavilion_records.json'),
   } as never);
 }
 
diff --git a/tests/integration/lifeSummaryPrestigeFlow.test.ts b/tests/integration/lifeSummaryPrestigeFlow.test.ts
index 61ebcc94..5873c3c8 100644
--- a/tests/integration/lifeSummaryPrestigeFlow.test.ts
+++ b/tests/integration/lifeSummaryPrestigeFlow.test.ts
@@ -77,4 +77,6 @@ test('current chapter exhausted and prestige entry points include life summary a
   assert.equal(exhaustedSource.includes("openLifeSummaryModal('current')"), true);
   assert.equal(prestigeSurfaceSource.includes('View Life Summary'), true);
   assert.equal(prestigeOwnerSource.includes("openLifeSummaryModal('current')"), true);
+  assert.equal(prestigeSurfaceSource.includes('View Last Life Summary'), true);
+  assert.equal(prestigeOwnerSource.includes("openLifeSummaryModal('last_completed')"), true);
 });
diff --git a/tests/integration/prestigeAdvisorUiBridge.test.ts b/tests/integration/prestigeAdvisorUiBridge.test.ts
index 5536e02a..416d8c20 100644
--- a/tests/integration/prestigeAdvisorUiBridge.test.ts
+++ b/tests/integration/prestigeAdvisorUiBridge.test.ts
@@ -18,9 +18,13 @@ test('ritual and AP breakdown modals use canonical prestige state vocabulary', a
 });
 
 test('prestige screen bridges to advisor reset buckets and recommendation strip', async () => {
-  const screen = await readSource('src/components/screens/PrestigeScreen.tsx');
+  const wrapper = await readSource('src/components/screens/PrestigeScreen.tsx');
+  const owner = await readSource('src/features/prestige/prestigeLedgerExact/PrestigeLedgerScreenOwner.tsx');
+  const exactScreen = await readSource('src/features/prestige/prestigeLedgerExact/PrestigeLedgerExactScreen.tsx');
 
-  assert.equal(screen.includes('advisor.resetPreview'), true);
-  assert.equal(screen.includes('Top recommended decree'), true);
-  assert.equal(screen.includes('RunCompassCompact'), true);
+  assert.equal(wrapper.includes('PrestigeLedgerScreenOwner'), true);
+  assert.equal(owner.includes('advisor.resetPreview'), true);
+  assert.equal(owner.includes('buildPrestigeForecastSurfaceV2'), true);
+  assert.equal(exactScreen.includes('Recommended Decrees'), true);
+  assert.equal(exactScreen.includes('surface.forecast.title'), true);
 });
diff --git a/tmp-progression-fixtures/src/features/prestige/lifeSummarySurface.js b/tmp-progression-fixtures/src/features/prestige/lifeSummarySurface.js
index a66fbd6c..2f6b3654 100644
--- a/tmp-progression-fixtures/src/features/prestige/lifeSummarySurface.js
+++ b/tmp-progression-fixtures/src/features/prestige/lifeSummarySurface.js
@@ -5,10 +5,15 @@ import { useCultivationStore } from '../../stores/cultivationStore.js';
 import { useGameStore } from '../../stores/gameStore.js';
 import { useInventoryStore } from '../../stores/inventoryStore.js';
 import { usePrestigeStore } from '../../stores/prestigeStore.js';
+import { useUIStore } from '../../stores/uiStore.js';
 import { useRuinsStore } from '../../stores/ruinsStore.js';
 import { useTrialStore } from '../../stores/trialStore.js';
+import { useBountyStore } from '../../stores/bountyStore.js';
+import { useExpeditionStore } from '../../stores/expeditionStore.js';
+import { useProfessionStore } from '../../stores/professionStore.js';
 import { useBreakthroughEchoStore } from '../breakthroughEchoes/index.js';
 import { getPrestigeAdvisorSurface } from './prestigeAdvisorSurface.js';
+import { buildPostResetObjectivePreview } from './prestigeForecastSurface.js';
 import { formatArchetypeLabel, formatCityLabel, formatDiagnosisLabel, formatGateTrialLabel, formatHeartLawLabel, formatPathLabel, formatReadinessBandLabel, } from '../../ui/text/playerFacingFormatters.js';
 const BLOCKS = Object.freeze([
     { key: 'life_arc', title: 'Life Arc' },
@@ -17,13 +22,16 @@ const BLOCKS = Object.freeze([
     { key: 'world_progress', title: 'World Progress' },
     { key: 'gate_trials', title: 'Gate Trials' },
     { key: 'ruins_supply', title: 'Ruins & Supply' },
+    { key: 'economy_support', title: 'Economy Support' },
+    { key: 'offline_background', title: 'Offline & Background' },
     { key: 'next_life_focus', title: 'Next Life Focus' },
 ]);
 const clampLines = (lines, fallback) => {
     const normalized = lines.filter((line) => typeof line === 'string' && line.trim().length > 0).map((line) => line.trim());
-    if (normalized.length === 0)
+    const deduped = normalized.filter((line, index, values) => values.indexOf(line) === index);
+    if (deduped.length === 0)
         return [fallback];
-    return normalized.slice(0, 5);
+    return deduped.slice(0, 5);
 };
 const formatDuration = (ms) => {
     if (ms <= 0)
@@ -35,6 +43,72 @@ const formatDuration = (ms) => {
         return `${minutes}m`;
     return `${hours}h ${minutes}m`;
 };
+const resolveSummarySeal = (args) => {
+    if (args.advisor === 'Recommended')
+        return 'chapter_exhausted';
+    if (args.prestigeCount > 0)
+        return 'reclaimer';
+    if (args.highestRealmIndex >= 2)
+        return 'realm_successor';
+    if (args.clearedTrials > 0)
+        return 'gate_challenger';
+    return 'quiet_life';
+};
+const buildHeadline = (args) => {
+    if (args.advisor === 'Too Early') {
+        return `${args.realmName} life is still gathering enough proof for reincarnation.`;
+    }
+    if (args.advisor === 'Recommended') {
+        return `${args.realmName} life has reached a rational reincarnation handoff for +${args.apGain} AP.`;
+    }
+    return `${args.realmName} life can be sealed now for +${args.apGain} AP, with more value possible if you continue.`;
+};
+const buildMajorMemories = (args) => {
+    const memories = args.echoes.slice(-3).map((echo) => ({
+        id: `echo_${echo.echoId}`,
+        title: 'Breakthrough Echo',
+        detail: echo.memoryLine,
+        source: 'breakthrough_echo',
+        summaryEligible: true,
+    }));
+    if (args.clearedTrials > 0 || args.bypassedTrials > 0) {
+        memories.push({
+            id: 'gate_trial_record',
+            title: 'Gate Trial Record',
+            detail: `${args.clearedTrials} cleared, ${args.bypassedTrials} bypassed.`,
+            source: 'gate_trial',
+            summaryEligible: true,
+        });
+    }
+    if (args.totalRuinsRuns > 0) {
+        memories.push({
+            id: 'ruins_record',
+            title: 'Ruins Record',
+            detail: `${args.totalRuinsRuns} ruins run${args.totalRuinsRuns === 1 ? '' : 's'} completed this life.`,
+            source: 'ruins',
+            summaryEligible: true,
+        });
+    }
+    if (args.offlineLine) {
+        memories.push({
+            id: 'offline_record',
+            title: 'Offline Record',
+            detail: args.offlineLine,
+            source: 'offline',
+            summaryEligible: true,
+        });
+    }
+    if (memories.length === 0) {
+        memories.push({
+            id: 'quiet_life',
+            title: 'Quiet Life',
+            detail: 'No major breakthrough, gate, ruins, or offline memories have been recorded yet.',
+            source: 'prestige',
+            summaryEligible: true,
+        });
+    }
+    return memories.slice(0, 6);
+};
 const buildCurrentBlocks = () => {
     const now = Date.now();
     const game = useGameStore.getState();
@@ -44,6 +118,10 @@ const buildCurrentBlocks = () => {
     const trial = useTrialStore.getState();
     const ruins = useRuinsStore.getState();
     const inventory = useInventoryStore.getState();
+    const ui = useUIStore.getState();
+    const bounties = useBountyStore.getState();
+    const expeditions = useExpeditionStore.getState();
+    const profession = useProfessionStore.getState();
     const echoes = useBreakthroughEchoStore.getState().echoes;
     const advisor = getPrestigeAdvisorSurface();
     const status = buildSection5StatusSurface();
@@ -52,7 +130,16 @@ const buildCurrentBlocks = () => {
     const bypassedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'bypassed').length;
     const totalRuinsRuns = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRuns, 0);
     const totalRuinsRooms = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRoomsCleared, 0);
+    const activeQueues = profession.alchemyQueue.length + profession.talismanQueue.length + profession.forgeQueue.length;
+    const activeExpeditions = expeditions.active.length;
+    const completedExpeditions = expeditions.active.filter((run) => run.status === 'complete' || now >= run.endsAt).length;
+    const bountyRecords = Object.values(bounties.activeByCityId ?? {}).reduce((sum, board) => sum + board.length, 0);
+    const lastOffline = ui.lastOfflineSummary;
+    const offlineLine = lastOffline
+        ? `${lastOffline.offlineDuration} considered at ${Math.round(lastOffline.efficiency * 100)}% efficiency.`
+        : null;
     const topPurchase = advisor.topRecommendedPurchase;
+    const nextLifeFocus = buildPostResetObjectivePreview();
     const linesByKey = {
         life_arc: clampLines([
             `Current realm: ${getLiveRealmNameByIndex(game.realm.index)}`,
@@ -85,7 +172,22 @@ const buildCurrentBlocks = () => {
             ruins.lastRunSummary ? `Last ruins run: ${ruins.lastRunSummary.victory ? 'Victory' : 'Defeat'} (${ruins.lastRunSummary.roomsCleared}/${ruins.lastRunSummary.roomCount} rooms)` : 'No ruins run completed this life.',
             `Current gold on hand: ${inventory.currencies.gold}`,
         ], 'No ruins or supply activity has been recorded yet.'),
+        economy_support: clampLines([
+            `Profession queues active: ${activeQueues}`,
+            `Bounty records touched: ${bountyRecords}`,
+            activeExpeditions > 0 ? `Expeditions running: ${activeExpeditions}` : 'No expedition currently running.',
+            completedExpeditions > 0 ? `Expeditions ready: ${completedExpeditions}` : '',
+            status.currentDiagnosis ? `Strongest support route: fix ${formatDiagnosisLabel(status.currentDiagnosis.primary)}.` : '',
+        ], 'No major support loop has been recorded yet.'),
+        offline_background: clampLines([
+            offlineLine ?? 'No offline settlement has been recorded in this session.',
+            lastOffline?.wasCapped ? 'Last offline settlement hit the cap.' : '',
+            lastOffline?.parts.find((part) => part.kind === 'queued_actions') ? 'Queued actions became ready while away.' : '',
+            lastOffline?.parts.find((part) => part.kind === 'expeditions') ? 'Expeditions became ready while away.' : '',
+            'Combat never progresses offline.',
+        ], 'No offline background summary is available yet.'),
         next_life_focus: clampLines([
+            `${nextLifeFocus.label}: ${nextLifeFocus.detail}`,
             status.currentDiagnosis ? `Fix diagnosis first: ${formatDiagnosisLabel(status.currentDiagnosis.primary)}` : '',
             status.currentGateTrialId ? `Center prep around ${formatGateTrialLabel(status.currentGateTrialId)} before your next reset.` : '',
             topPurchase ? `${topPurchase.mode === 'buy_now' ? 'Buy now' : 'Save for'} ${topPurchase.name} (${topPurchase.nextCost} AP)` : '',
@@ -100,33 +202,80 @@ const buildCurrentBlocks = () => {
 export const buildCurrentLifeSummarySurface = () => {
     const advisor = getPrestigeAdvisorSurface();
     const prestige = usePrestigeStore.getState();
+    const game = useGameStore.getState();
+    const trial = useTrialStore.getState();
+    const ruins = useRuinsStore.getState();
+    const ui = useUIStore.getState();
+    const echoes = useBreakthroughEchoStore.getState().echoes;
+    const clearedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'cleared').length;
+    const bypassedTrials = Object.values(trial.progressByTrialId).filter((progress) => progress.resolution === 'bypassed').length;
+    const totalRuinsRuns = Object.values(ruins.progressByRuinId).reduce((sum, progress) => sum + progress.totalRuns, 0);
+    const highestRealmIndex = Math.max(prestige.highestRealmReached, game.realm?.index ?? 0);
+    const realmName = getLiveRealmNameByIndex(highestRealmIndex);
+    const nextLifeFocus = buildPostResetObjectivePreview();
+    const summarySeal = resolveSummarySeal({
+        advisor: advisor.stateLabel,
+        clearedTrials,
+        highestRealmIndex,
+        prestigeCount: prestige.prestigeCount,
+    });
     return {
+        version: 2,
         mode: 'current',
         capturedAt: Date.now(),
+        lifeOrdinal: prestige.prestigeCount + 1,
         advisorLabel: advisor.stateLabel,
         apForecastGain: advisor.apForecast.potentialGain,
         apAfterRitual: prestige.totalAP + advisor.apForecast.potentialGain,
+        headline: buildHeadline({
+            realmName,
+            advisor: advisor.stateLabel,
+            apGain: advisor.apForecast.potentialGain,
+        }),
+        summarySeal,
         blocks: buildCurrentBlocks(),
+        majorMemories: buildMajorMemories({
+            echoes,
+            clearedTrials,
+            bypassedTrials,
+            totalRuinsRuns,
+            offlineLine: ui.lastOfflineSummary
+                ? `${ui.lastOfflineSummary.offlineDuration} at ${Math.round(ui.lastOfflineSummary.efficiency * 100)}% efficiency.`
+                : null,
+        }),
+        nextLifeFocus,
+        warnings: advisor.apForecast.potentialGain <= 0 ? ['Potential AP gain is zero; do not treat this as a strong reset.'] : [],
     };
 };
 export const buildPrestigeLifeSummarySnapshot = () => {
     const current = buildCurrentLifeSummarySurface();
     return {
+        version: 2,
         capturedAt: current.capturedAt,
+        lifeOrdinal: current.lifeOrdinal,
         advisorLabel: current.advisorLabel,
         apForecastGain: current.apForecastGain,
         apAfterRitual: current.apAfterRitual,
+        headline: current.headline,
+        summarySeal: current.summarySeal,
         blocks: current.blocks.map((block) => ({ ...block, lines: [...block.lines] })),
+        majorMemories: current.majorMemories.map((memory) => ({ ...memory })),
+        nextLifeFocus: current.nextLifeFocus,
+        warnings: [...current.warnings],
     };
 };
 export const buildLastCompletedLifeSummarySurface = (snapshot) => {
     const normalizedBlockByKey = new Map(snapshot.blocks.map((block) => [block.key, block]));
     return {
+        version: 2,
         mode: 'last_completed',
         capturedAt: snapshot.capturedAt,
+        lifeOrdinal: snapshot.lifeOrdinal,
         advisorLabel: snapshot.advisorLabel,
         apForecastGain: snapshot.apForecastGain,
         apAfterRitual: snapshot.apAfterRitual,
+        headline: snapshot.headline,
+        summarySeal: snapshot.summarySeal,
         blocks: BLOCKS.map((block) => {
             const existing = normalizedBlockByKey.get(block.key);
             return {
@@ -135,5 +284,8 @@ export const buildLastCompletedLifeSummarySurface = (snapshot) => {
                 lines: clampLines(existing?.lines ?? [], 'No notable notes were captured for this section.'),
             };
         }),
+        majorMemories: snapshot.majorMemories.map((memory) => ({ ...memory })),
+        nextLifeFocus: snapshot.nextLifeFocus,
+        warnings: [...snapshot.warnings],
     };
 };
diff --git a/tmp-progression-fixtures/src/features/prestige/prestigeAdvisorSurface.js b/tmp-progression-fixtures/src/features/prestige/prestigeAdvisorSurface.js
index 232b8afa..a4fc9ad3 100644
--- a/tmp-progression-fixtures/src/features/prestige/prestigeAdvisorSurface.js
+++ b/tmp-progression-fixtures/src/features/prestige/prestigeAdvisorSurface.js
@@ -5,26 +5,7 @@ import { useTrialStore } from '../../stores/trialStore.js';
 import { buildPrestigeProgressionSnapshot, calculatePrestigeApForecast, countResolvedSemesterGateTrials, extractLiveTrialIds, resolvePrestigeAdvisorLabel, } from '../../systems/prestige/prestigeApReadModel.js';
 import { PRESTIGE_CATEGORIES, getPrestigeCategoryKey } from './prestigeCategories.js';
 import { buildPrestigeStarterSpendPlan } from '../../systems/prestige/prestigeStarterSpendPlanner.js';
-const RESETS_THIS_LIFE = Object.freeze([
-    'Realm progress',
-    'Qi and combat run state',
-    'Inventory and currencies',
-    'Equipment loadout',
-    'Trial and ruins progress',
-    'Outskirts and bounty progress',
-    'Current activity',
-]);
-const CARRIES_FORWARD = Object.freeze([
-    'Ascension Points (AP)',
-    'Lifetime AP earned',
-    'Purchased AP upgrades',
-    'Prestige run history',
-    'Prestige count',
-]);
-const REBUILT_NEXT_LIFE = Object.freeze([
-    'Spirit root (new roll)',
-    'City baseline (starter city)',
-]);
+import { getPrestigeResetContractSurface } from '../../services/prestige/PrestigeResetContract.js';
 const getAdvisorDetail = (stateLabel) => {
     if (stateLabel === 'Too Early') {
         return 'Push this life to Core Formation before beginning Reincarnation.';
@@ -78,6 +59,7 @@ export const getPrestigeAdvisorSurface = () => {
     const potentialGain = Math.max(0, forecast.totalAp);
     const breakdown = prestige.getApBreakdown();
     const stateLabel = resolvePrestigeAdvisorLabel(forecast);
+    const resetContract = getPrestigeResetContractSurface({ purchasesById: prestige.purchasesById });
     return {
         stateLabel,
         stateDetail: getAdvisorDetail(stateLabel),
@@ -86,9 +68,9 @@ export const getPrestigeAdvisorSurface = () => {
             breakdown,
         },
         resetPreview: {
-            resetsThisLife: [...RESETS_THIS_LIFE],
-            carriesForward: [...CARRIES_FORWARD],
-            rebuiltNextLife: [...REBUILT_NEXT_LIFE],
+            resetsThisLife: resetContract.reset.map((line) => line.label),
+            carriesForward: resetContract.carry.map((line) => line.label),
+            rebuiltNextLife: resetContract.rebuilt.map((line) => line.label),
         },
         spiritRootClassification: prestige.spiritRoot ? 'Awakened' : 'Dormant',
         topRecommendedPurchase: buildTopRecommendedPurchase(),
diff --git a/tmp-progression-fixtures/src/services/prestige/PrestigeResetService.js b/tmp-progression-fixtures/src/services/prestige/PrestigeResetService.js
index ccdbbad8..bc4b260c 100644
--- a/tmp-progression-fixtures/src/services/prestige/PrestigeResetService.js
+++ b/tmp-progression-fixtures/src/services/prestige/PrestigeResetService.js
@@ -20,15 +20,7 @@ import { useTrialStore } from '../../stores/trialStore.js';
 import { useZoneStore } from '../../stores/zoneStore.js';
 import { useUIStore } from '../../stores/uiStore.js';
 import { hardResetBreakthroughEchoes } from '../../features/breakthroughEchoes/index.js';
-const MASTERY_RETENTION_BY_NODE_ID = {
-    ap_mastery_retention_10: 0.1,
-    ap_mastery_retention_25: 0.25,
-    ap_mastery_retention_50: 0.5,
-};
-export const deriveMasteryRetentionCarryOver = (purchasesById) => Object.entries(MASTERY_RETENTION_BY_NODE_ID).reduce((best, [nodeId, ratio]) => {
-    const purchasedLevels = purchasesById[nodeId] ?? 0;
-    return purchasedLevels > 0 ? Math.max(best, ratio) : best;
-}, 0);
+import { deriveMasteryRetentionCarryOver } from './PrestigeResetContract.js';
 const snapshotTechniqueMasteryXp = () => {
     const unlocked = useTechCollectionStore.getState().unlockedTechs;
     return Object.fromEntries(Object.entries(unlocked)
diff --git a/tmp-progression-fixtures/src/services/time/OfflineCatchup.js b/tmp-progression-fixtures/src/services/time/OfflineCatchup.js
index 23952cbf..757e97f7 100644
--- a/tmp-progression-fixtures/src/services/time/OfflineCatchup.js
+++ b/tmp-progression-fixtures/src/services/time/OfflineCatchup.js
@@ -10,6 +10,10 @@ import { cultivationService } from '../cultivationService.js';
 import { buildCultivationConsumableCarryoverWindows } from '../../systems/consumables/cultivationConsumableEffects.js';
 import { getHeartLawBonuses } from '../../systems/heartLaw/heartLawLogic.js';
 import { getExpeditionReadinessDelta, getQueuedActionReadinessDelta } from '../../systems/offline/offlineSummaryReadModel.js';
+import { buildOfflineCatchupSurface } from '../../systems/offline/offlineCatchupSurface.js';
+function formatPercent(value) {
+    return `${Math.round(value * 100)}%`;
+}
 function getLiveOfflineEfficiency() {
     const prestigeStore = usePrestigeStore.getState();
     const cultivationStore = useCultivationStore.getState();
@@ -20,10 +24,24 @@ function getLiveOfflineEfficiency() {
         chapter: cultivationStore.chapter,
         spiritRoot: prestigeStore.spiritRoot,
     }).offlineEfficiencyAdd;
-    return resolveOfflineCultivationEfficiency({
-        prestigeEfficiencyAdd: prestigeStore.getOfflineEfficiencyBonusAdditive(),
+    const prestigeEfficiencyAdd = prestigeStore.getOfflineEfficiencyBonusAdditive();
+    const value = resolveOfflineCultivationEfficiency({
+        prestigeEfficiencyAdd,
         heartLawBonus,
     });
+    const sources = [
+        { id: 'base', label: 'Base idle settlement', value: formatPercent(resolveOfflineCultivationEfficiency({ prestigeEfficiencyAdd: 0, heartLawBonus: 0 })) },
+    ];
+    if (prestigeEfficiencyAdd > 0) {
+        sources.push({ id: 'prestige', label: 'Prestige decree', value: `+${formatPercent(prestigeEfficiencyAdd)}` });
+    }
+    if (heartLawBonus > 0) {
+        sources.push({ id: 'heart_law', label: 'Heart Law', value: `+${formatPercent(heartLawBonus)}` });
+    }
+    if (sources.length === 1) {
+        sources.push({ id: 'no_bonus', label: 'No active offline bonus', value: '+0%' });
+    }
+    return { value, sources };
 }
 function calculateOfflineQiGain(startAt, endAt, offlineEfficiency) {
     if (offlineEfficiency <= 0 || endAt <= startAt)
@@ -44,14 +62,22 @@ function calculateOfflineQiGain(startAt, endAt, offlineEfficiency) {
 }
 export function apply(context) {
     if (context.dtMs <= 0) {
-        return { summary: null };
+        return {
+            summary: null,
+            surface: buildOfflineCatchupSurface({
+                summary: null,
+                generatedAt: context.now,
+                rawSeconds: Math.floor(context.rawMs / 1000),
+            }),
+        };
     }
     const seconds = Math.floor(Math.min(context.dtMs, MAX_OFFLINE_MS) / 1000);
     const summaryParts = [];
     const startAt = context.now - seconds * 1000;
     const gameStore = useGameStore.getState();
     useGameStore.setState({ lastActiveTime: context.now, lastTickTime: context.now });
-    const offlineEfficiency = getLiveOfflineEfficiency();
+    const offlineEfficiencyDetail = getLiveOfflineEfficiency();
+    const offlineEfficiency = offlineEfficiencyDetail.value;
     const qiGain = calculateOfflineQiGain(startAt, context.now, offlineEfficiency);
     if (qiGain.greaterThan(0)) {
         const nextQi = D(gameStore.qi ?? '0').plus(qiGain);
@@ -97,13 +123,22 @@ export function apply(context) {
     if (expeditionReadiness.newlyComplete > 0) {
         summaryParts.push({ kind: 'expeditions', label: 'Expeditions ready', value: `${expeditionReadiness.newlyComplete}` });
     }
+    const summary = {
+        offlineSeconds: seconds,
+        rawOfflineSeconds: Math.floor(context.rawMs / 1000),
+        maxOfflineSeconds: MAX_OFFLINE_MS / 1000,
+        offlineDuration: formatOfflineDuration(seconds),
+        efficiency: offlineEfficiency,
+        efficiencySources: offlineEfficiencyDetail.sources,
+        wasCapped: context.wasCapped,
+        parts: summaryParts,
+    };
     return {
-        summary: {
-            offlineSeconds: seconds,
-            offlineDuration: formatOfflineDuration(seconds),
-            efficiency: offlineEfficiency,
-            wasCapped: context.wasCapped,
-            parts: summaryParts,
-        },
+        summary,
+        surface: buildOfflineCatchupSurface({
+            summary,
+            generatedAt: context.now,
+            rawSeconds: Math.floor(context.rawMs / 1000),
+        }),
     };
 }
diff --git a/tmp-progression-fixtures/src/stores/prestigeStore.js b/tmp-progression-fixtures/src/stores/prestigeStore.js
index 63ea8d83..55ac3061 100644
--- a/tmp-progression-fixtures/src/stores/prestigeStore.js
+++ b/tmp-progression-fixtures/src/stores/prestigeStore.js
@@ -8,6 +8,7 @@ import { recomputeAndApplyPrestigeUnlocks } from '../systems/prestige/applyPrest
 import { canPurchasePrestigeNode, getPrestigeNodeRuntimeStatus, isPrestigeNodeVisible, } from '../systems/prestige/runtime/prestigeRuntimeCatalog.js';
 import { useTrialStore } from './trialStore.js';
 import { buildPrestigeApBreakdownRows, buildPrestigeProgressionSnapshot, calculatePrestigeApForecast, countResolvedSemesterGateTrials, extractLiveTrialIds, resolvePrestigeAdvisorLabel, } from '../systems/prestige/prestigeApReadModel.js';
+import { buildPostResetReclaimObjectiveSurface, } from '../features/prestige/postResetReclaimObjectiveSurface.js';
 /**
  * Lazy getter for game store to avoid circular dependency
  */
@@ -93,6 +94,7 @@ const createInitialPrestigeState = () => ({
     rerollCount: 0,
     spiritRoot: null,
     lastLifeSummary: null,
+    postResetReclaimObjective: null,
 });
 function getUpgradesFromContent() {
     const content = useContentStore.getState();
@@ -167,18 +169,39 @@ export const usePrestigeStore = create()(immer((set, get) => ({
             resolvedGateCount,
         }));
         const lifeSummarySnapshot = preparedLifeSummary ?? {
+            version: 2,
             capturedAt: Date.now(),
+            lifeOrdinal: state.prestigeCount + 1,
             advisorLabel: resolvePrestigeAdvisorLabel(fallbackForecast),
             apForecastGain: fallbackForecast.totalAp,
             apAfterRitual: state.totalAP + fallbackForecast.totalAp,
+            headline: 'Life summary was captured from runtime fallback.',
+            summarySeal: 'quiet_life',
             blocks: [
                 { key: 'life_arc', title: 'Life Arc', lines: ['Life summary was captured from runtime fallback.'] },
+                { key: 'breakthrough_echoes', title: 'Breakthrough Echoes', lines: ['Breakthrough echoes were not captured for this ritual call.'] },
                 { key: 'doctrine_build', title: 'Doctrine & Build', lines: ['Doctrine details were not captured for this ritual call.'] },
                 { key: 'world_progress', title: 'World Progress', lines: ['World progress details were not captured for this ritual call.'] },
                 { key: 'gate_trials', title: 'Gate Trials', lines: ['Gate-trial details were not captured for this ritual call.'] },
                 { key: 'ruins_supply', title: 'Ruins & Supply', lines: ['Ruins and supply details were not captured for this ritual call.'] },
+                { key: 'economy_support', title: 'Economy Support', lines: ['Economy support details were not captured for this ritual call.'] },
+                { key: 'offline_background', title: 'Offline & Background', lines: ['Offline details were not captured for this ritual call.'] },
                 { key: 'next_life_focus', title: 'Next Life Focus', lines: ['Open the live Life Summary panel for full guidance.'] },
             ],
+            majorMemories: [{
+                    id: 'fallback_summary',
+                    title: 'Fallback Summary',
+                    detail: 'The ritual call did not provide a prepared life summary.',
+                    source: 'prestige',
+                    summaryEligible: true,
+                }],
+            nextLifeFocus: {
+                id: 'fallback_reclaim',
+                label: 'Review the new life',
+                detail: 'Choose path and Heart Law, then rebuild the reclaim route from live state.',
+                route: { kind: 'tab', tabId: 'cultivation', anchor: 'life_setup' },
+            },
+            warnings: ['Emergency fallback summary was used.'],
         };
         if (!state.canPrestige())
             return;
@@ -201,6 +224,10 @@ export const usePrestigeStore = create()(immer((set, get) => ({
             state.highestRealmReached = 0;
             state.runStartTime = Date.now();
             state.lastLifeSummary = lifeSummarySnapshot;
+            state.postResetReclaimObjective = buildPostResetReclaimObjectiveSurface({
+                sourceSummary: lifeSummarySnapshot,
+                purchasesById: state.purchasesById,
+            });
         });
         GameEvents.emit({
             type: 'prestige/performed',
@@ -456,6 +483,16 @@ export const usePrestigeStore = create()(immer((set, get) => ({
         });
         get().initializeUpgrades();
     },
+    dismissPostResetReclaimObjective: (objectiveId) => {
+        const current = get().postResetReclaimObjective;
+        if (!current)
+            return;
+        if (objectiveId && current.id !== objectiveId)
+            return;
+        set((state) => {
+            state.postResetReclaimObjective = null;
+        });
+    },
 })));
 if (import.meta.env?.DEV && typeof window !== 'undefined') {
     const devWindow = window;
diff --git a/tmp-progression-fixtures/src/systems/prestige/runtime/prestigeRuntimeCatalog.js b/tmp-progression-fixtures/src/systems/prestige/runtime/prestigeRuntimeCatalog.js
index 0afebf5e..7733740b 100644
--- a/tmp-progression-fixtures/src/systems/prestige/runtime/prestigeRuntimeCatalog.js
+++ b/tmp-progression-fixtures/src/systems/prestige/runtime/prestigeRuntimeCatalog.js
@@ -72,3 +72,86 @@ export const getPurchasedVisiblePrestigeNodeIds = (purchases, content) => {
         .filter(([nodeId, level]) => visibleIds.has(nodeId) && level > 0)
         .map(([nodeId]) => nodeId);
 };
+const isRecord = (value) => Boolean(value) && typeof value === 'object' && !Array.isArray(value);
+const collectEffectKeys = (upgrade) => {
+    const keys = new Set();
+    if (typeof upgrade.stat === 'string' && upgrade.stat.trim())
+        keys.add(upgrade.stat);
+    if (isRecord(upgrade.effect)) {
+        Object.keys(upgrade.effect).forEach((key) => keys.add(key));
+    }
+    if (isRecord(upgrade.effectPerLevel)) {
+        Object.keys(upgrade.effectPerLevel).forEach((key) => keys.add(key));
+    }
+    else if (typeof upgrade.effectPerLevel === 'number' && upgrade.stat) {
+        keys.add(upgrade.stat);
+    }
+    if (Array.isArray(upgrade.unlocks) && upgrade.unlocks.length > 0)
+        keys.add('unlocks');
+    return Array.from(keys).sort();
+};
+const toAuditStatus = (status) => {
+    if (status === 'visible_live')
+        return 'visible_live';
+    if (status === 'deferred')
+        return 'deferred';
+    if (status === 'hidden_unsupported')
+        return 'hidden_unsupported';
+    return 'unknown_blocked';
+};
+const reasonForAuditStatus = (node) => {
+    if (node.status === 'visible_live') {
+        return `Live runtime consumer(s): ${node.consumers.join(', ')}.`;
+    }
+    if (node.status === 'deferred') {
+        return 'Deferred future-slice decree; blocked from live purchasing.';
+    }
+    if (node.status === 'hidden_unsupported') {
+        return 'Content effect has no confirmed runtime consumer in this slice, so it is hidden.';
+    }
+    return 'Runtime status is unknown; blocked until content or consumer metadata is fixed.';
+};
+const remediationForAuditStatus = (status) => {
+    if (status === 'visible_live')
+        return 'none';
+    if (status === 'deferred')
+        return 'label_deferred';
+    if (status === 'hidden_unsupported')
+        return 'hide';
+    return 'fix_content_key';
+};
+export function buildPrestigeEffectAuditReport(content, options) {
+    const catalog = getPrestigeRuntimeCatalog(content);
+    const rows = catalog.nodes.map((node) => {
+        const status = toAuditStatus(node.status);
+        const purchaseAllowed = status === 'visible_live';
+        const displayAllowed = status === 'visible_live' || status === 'deferred';
+        return {
+            upgradeId: node.upgrade.id,
+            upgradeName: node.upgrade.name ?? node.upgrade.id,
+            status,
+            contentEffectKeys: collectEffectKeys(node.upgrade),
+            runtimeConsumers: node.consumers,
+            purchaseAllowed,
+            displayAllowed,
+            reason: reasonForAuditStatus(node),
+            remediation: remediationForAuditStatus(status),
+        };
+    }).sort((left, right) => left.upgradeId.localeCompare(right.upgradeId));
+    const blockers = rows
+        .filter((row) => row.purchaseAllowed && row.runtimeConsumers.length === 0)
+        .map((row) => `${row.upgradeId} is purchasable without a runtime consumer.`);
+    const warnings = rows
+        .filter((row) => row.status === 'unknown_blocked')
+        .map((row) => `${row.upgradeId} is unknown and blocked.`);
+    return {
+        generatedAt: options?.generatedAt ?? Date.now(),
+        rows,
+        visibleLiveCount: rows.filter((row) => row.status === 'visible_live').length,
+        deferredCount: rows.filter((row) => row.status === 'deferred').length,
+        hiddenUnsupportedCount: rows.filter((row) => row.status === 'hidden_unsupported').length,
+        unknownBlockedCount: rows.filter((row) => row.status === 'unknown_blocked').length,
+        blockers,
+        warnings,
+    };
+}
diff --git a/tmp-progression-fixtures/src/systems/readiness/section5Adapters.js b/tmp-progression-fixtures/src/systems/readiness/section5Adapters.js
index 01dbfb04..7401ecb7 100644
--- a/tmp-progression-fixtures/src/systems/readiness/section5Adapters.js
+++ b/tmp-progression-fixtures/src/systems/readiness/section5Adapters.js
@@ -52,6 +52,9 @@ function buildPatchedProgressByTrialId(trialId) {
         return [currentTrialId, normalized];
     }));
 }
+function getTrialProgressSnapshot(trialId) {
+    return normalizeTrialProgress(useTrialStore.getState().progressByTrialId[trialId] ?? createDefaultTrialProgress());
+}
 function buildBypassAvailability(trialId) {
     const trial = useContentStore.getState().maps.trialsById[trialId] ?? null;
     const content = useContentStore.getState().raw;
@@ -60,7 +63,7 @@ function buildBypassAvailability(trialId) {
     const lifecycle = getTrialLifecycleSnapshot({
         content,
         trial,
-        progress: useTrialStore.getState().getProgress(trialId),
+        progress: getTrialProgressSnapshot(trialId),
         realm: game.realm,
         qi: game.qi,
         breakthroughRequirement: game.getBreakthroughRequirement(),
@@ -151,7 +154,7 @@ export function buildSection5ReadinessSurface(trialId, snapshot) {
             return null;
         const resolvedSnapshot = snapshot ?? buildDoctrineSnapshot();
         const build = analyzeSelectedBuild(resolvedSnapshot);
-        const progress = useTrialStore.getState().getProgress(resolvedTrialId);
+        const progress = getTrialProgressSnapshot(resolvedTrialId);
         const lastAttemptSummary = progress.lastAttemptSummary ?? null;
         const readiness = buildTrialGateReadinessResult(resolvedTrialId, resolvedSnapshot);
         const bypassAvailable = buildBypassAvailability(resolvedTrialId);
@@ -186,7 +189,7 @@ export function buildSection5StatusSurface(snapshot) {
         const readiness = evaluateCurrentGateReadiness(resolvedSnapshot);
         let currentDiagnosis = null;
         if (currentGateTrialId && readiness) {
-            const progress = useTrialStore.getState().getProgress(currentGateTrialId);
+            const progress = getTrialProgressSnapshot(currentGateTrialId);
             if (progress.lastAttemptSummary) {
                 currentDiagnosis = diagnoseTrialFailure({
                     trialId: currentGateTrialId,
@@ -227,7 +230,7 @@ export function buildSection5PostFailureSurface(trialId) {
     const trialDef = useContentStore.getState().maps.trialsById[readinessSurface.trialId] ?? null;
     const content = useContentStore.getState().raw;
     const game = useGameStore.getState();
-    const progress = useTrialStore.getState().getProgress(readinessSurface.trialId);
+    const progress = getTrialProgressSnapshot(readinessSurface.trialId);
     const requiredItemSatisfied = !trialDef?.requiredItemId || useInventoryStore.getState().getItemCount(trialDef.requiredItemId) > 0;
     const lifecycle = getTrialLifecycleSnapshot({
         content,
@@ -323,7 +326,7 @@ export function buildGateTrialReadinessSurface(trialId) {
     const trialDef = maps.trialsById[resolvedTrialId] ?? null;
     if (!trialDef)
         return null;
-    const progress = useTrialStore.getState().getProgress(resolvedTrialId);
+    const progress = getTrialProgressSnapshot(resolvedTrialId);
     const requiredItemCount = trialDef.requiredItemId ? useInventoryStore.getState().getItemCount(trialDef.requiredItemId) : 0;
     const requiredItemSatisfied = !trialDef.requiredItemId || requiredItemCount > 0;
     const lifecycle = getTrialLifecycleSnapshot({
