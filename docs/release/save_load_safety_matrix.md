# Save/Load Safety Matrix (Packet 7.4d)

| Scenario | Setup path | Expected after reload | Current automated coverage | Notes / known limitations |
| --- | --- | --- | --- | --- |
| After combat | `seedScenario('after_city_unlock')` then seed `ActivityStore.active=outskirts` + `CombatStore.isInCombat=true`; `saveGame -> resetRuntime -> loadGame` | Combat-coupled activity is cleared, no ghost combat context remains, and realm/city/path truth stays intact | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`after combat state clears ghost combat activity`) | Combat settings (`autoAttack`, `autoCombatAI`) persist; live combat session does not persist by design. |
| After city unlock | `seedScenario('after_city_unlock')` seeds live stores to a post-first-gate equivalent city state, then reload | Unlocked city set preserved, current city valid+unlocked, no fake `city_six` leakage | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`after city unlock preserves unlocked set`) | Uses canonical `normalizeCitySaveState` during hydrate path. |
| After prestige-ready state | `seedScenario('after_prestige_ready')`, then reload | AP projection remains coherent, selected path/current city preserved, no half-reset | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`prestige-ready state preserves AP projection`) | Keeps this matrix lightweight by asserting read-model coherence (`canPrestige`, projected AP bounds). |
| After content-cap acknowledgement | `seedScenario('after_cap_acknowledged')`, call `acknowledgeCurrentChapterExhausted`, save/reload, then prestige reset | Acknowledgement persists within the same life and resets after prestige/new life | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`chapter-cap acknowledgement persists...resets on prestige`) | Only acknowledgement truth persists; modal-open booleans are intentionally not persisted. |
| Offline normalization + immediate second load | Save baseline, inject split timestamp surfaces in main slot, `SaveService.load` twice with runtime reset between loads | Timestamps normalize to a single value and second load does not duplicate offline grant | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`offline timestamp normalization and immediate second-load safety`) | Uses real `SaveService.load` and offline catchup path. |
| Corrupted main + valid backup recovery | Save valid slot, copy to backup A, corrupt main slot payload, load via real slot order | Loader recovers from backup slot, runtime stays sane, and last load failure remains inspectable | `tests/integration/release/saveReloadSafetyMatrix.test.ts` (`corrupted main slot recovers from backup`) | Reuses existing quarantine/failure surfaces from packet 7.2d+/7.4c work. |
