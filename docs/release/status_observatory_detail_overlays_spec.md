# Status Observatory — Detail Overlay Implementation Specs

_Generated 2026-06-13 from the artifact `living-state-observatory (3).html` (`ovShell` + `ovAstrolabe`/`ovVessel`/`ovCanopy`/`ovConstellation`/`ovBuild`/`ovWork`/`ovLedger`) mapped to `StatusObservatorySurfaceV1`. Shared chrome: `StatusObservatoryOverlayShell.tsx`._


## astrolabe → `StatusAstrolabeOverlay.tsx`  (surface 25/27)

**Current app:** Today there is NO dedicated full-screen "astrolabe" overlay. The astrolabe is rendered inline inside the on-page Root/Law Coupled Instrument: StatusRootLawCoupledInstrument.tsx mounts StatusSpiritRootAstrolabe.tsx (the gold dial SVG + Grade/Purity/Fit summary + expression-cap bar + proc + run-validity wax seal + a single "Observe Spirit Root" route button), a small StatusRootLawBridge cord/label block, and StatusHeartLawSeal.tsx (book-flame medallion + chapter beads + Dao Heart state + "Open Dao Heart Sanctuary" route button). There is no modal shell, no zoomed dial, no rich diagnostic columns (Impact / How to Improve / Synergy Map / Compatible Laws / Root Grade & Multiplier / tabbed Spirit Root Observation drawer), and no shared overlay chrome (title/sub/context-chips/wax-seal/close/vertical-tag/mountains). StatusObservatoryDrawers.tsx today only provides the meridian-vessel drawer and the support ledger drawer (build/work/recent/calc/source) — none is the astrolabe overlay.

**Build plan:** 1) Build a reusable StatusObservatoryOverlayShell.tsx (port of ovShell) as a full-screen modal: role=dialog aria-modal=true, useDialogFocusTrap (already in repo) + focus restore + Escape/✕ close. Props: title, sub, sealChars, tag, watermark, context (goal/bottleneck/route chips), children. Renders grain SVG, 4 gilt corners, ink mountains strip (reuse inkMountains), motes, vertical tag, watermark glyph, header chrome, and a position:relative .ovcontent body. Keep all decorative bits aria-hidden. Map raw hex to tokens: #8b3028->var(--paper-stamp)/--observatory-cinnabar, #0f6f5a->var(--paper-jade)/--paper-jade-bright, #a2712a->var(--paper-amber)/--paper-gold-deep, #f3ead7->var(--paper-parchment-soft). 2) Create StatusAstrolabeOverlay.tsx consuming surface.rootLawInstrument (astrolabe + heartLawSeal + bridge + spiritRootObservation). 3) Center hero: reuse the EXISTING StatusSpiritRootAstrolabe dial SVG (extract just the <svg> dial into a shared sub-component or render the component in a 'zoomed' variant) — it is already 1:1 with artifact astrolabe(); do not re-derive geometry. 4) Profile card (top-left): elementLabel + gradeLabel + purityLabel + fitLabel + totalMultiplierLabel + runValidityLabel, jade/cinnabar by fitTier. 5) 14 Major Roots legend rail: map astrolabe.notches, element disc via var(--root-{id}) + GLYPH. 6) Bridge block (center-right): reuse StatusRootLawBridge logic (state/broken -> fracture vs link) + InkWaxSeal 對沖/相合. 7) Heart Law seal scroll (right of dial): reuse StatusHeartLawSeal medallion + chapter beads + clarity/turbulence/dao-state + its routeAction button; add decorative brass rods/tassels via SCSS. 8) midA column: Run Validity card (runValidityLabel + InkWaxSeal 勉強 jade/cinnabar), Expression Cap card (expressionCapLabel + parseExpressionCap fill bar), Proc card (proc.name/statusLabel/cooldownLabel). 9) midB column: warning card (bridge.detail/fit.summary + decorative seal), Root Grade & Multiplier card (grade/purity/totalMultiplier; omit or compute Effective Output), Root Notes card (fit.summary). 10) Bottom diagnostic row: Synergy Map (bridge.label/detail/state), Current Impact (astrolabe.impactRows), How to Improve (spiritRootObservation.practiceRoutes.rows), Compatible Laws (spiritRootObservation.variants.rows). 11) Right-edge Spirit Root Observation drawer: vertical tab rail from spiritRootObservation.tabs (Profile/Fit/Effects/Variants/Routes/History), active-tab rows table, + 'Observe Spirit Root' route button (single observeAction). 12) Wire open/close: add an overlay-kind union to the host (StatusLivingStateObservatory) alongside existing setActiveDrawer state; trigger from a dedicated 'Open Astrolabe' affordance on the on-page instrument; forward onAction. 13) Layout: match the artifact via absolute regions inside a fixed-size content stage (the artifact uses a 1856-wide canvas) — but to respect the project's no-CLS single-layout rule, render the overlay at a fixed design size with overflow scaling rather than reflowing regions; reserve the modal box up front so opening it does not shift the page. 14) Bind data only; mark grain/corners/mountains/motes/watermark/tag/rods/tassels/wax-seal chars/qi-arcs as decorative (aria-hidden), no surface backing.

**Artifact regions:**
- **ovShell chrome (shared)** _(full-screen modal; grain layer + 4 gilt corners (.gc tl/tr/bl/br) + ink mountains strip bottom 260px + 5 drifting motes + watermark glyph top-right (right:54px top:110px '根骨鑑觀') + vertical tag column (.ovtag, chars of '根骨' stacked) + header (padding-left:74px) with .ovtitle 'Spirit Root Astrolabe' / .ovsub 'Root & Heart Law entity diagnostic' on the left and on the right: context chips (.ovctx Next Goal / Main Bottleneck / Primary Route) + a 52px '根骨' wax seal + a ✕ .ovclose. Body is .ovcontent, position:relative full size.)_ — Decorative chrome + the global decree context strip (goal/bottleneck/route). The '根骨' seal chars and watermark '根骨鑑觀' are decorative.
- **Profile card (top-left)** _(absolute left:18 top:6 width:300, .ovcard)_ — 34px root disc (jade gradient if aligned / cinnabar if opposed) with kai glyph '根'; '{active} Root' headline + 'Grade Common · Purity {purity}%'; divider; three rows: 'Root / Law Fit {fit}', 'Total Multiplier {Aligned +1.4x | Mismatched -2.32x}', 'Run Validity {run}'. Color jade vs cinnabar by opposed.
- **14 Major Roots legend (left rail)** _(absolute left:18 top:150 width:118 z-index:4)_ — Sub-label '14 Major Roots'; 14 rows, each a 15px element disc (filled with element color if active, else parchment/colored stroke) + kai glyph + element name. Active row bolded.
- **Astrolabe dial (center hero)** _(absolute left:142 top:120 width:430 height:430 (#ovAstroDial, astrolabe() draws at 420px) + 'Purity Ring' sub-label at left:300 top:108)_ — The full gold-rim astrolabe SVG: 84-tick spinning rim, purity arc (-58..58deg, color by purity band) with PURITY textPath + numeric purity bead, 14 element glyph ring, donut fit band(s) (OPPOSED+ALIGNED twin bands if opposed, else single ALIGNED/etc band) with textPath labels, quivering compass needle (cinnabar if opposed / jade otherwise) pointing to active root angle, breathing core disc with '{active}\nRoot' + ghost glyph + center pin.
- **Root/Law bridge + fit seal (center-right, over dial edge)** _(absolute left:520 top:256 width:216 z-index:5 pointer-events:none)_ — SVG chain links (left + right groups of brass ellipses); center is either a cinnabar fracture bolt + two animated qi-flow arcs + weak red pulse circle (opposed) OR an intact brass link + jade breathing circle (aligned). Below: 'Root / Law Fit: {fit}' label (jade/cinnabar) + a wax seal '對沖' (opposed) or '相合' (aligned).
- **Heart Law seal scroll (right of dial)** _(absolute left:680 top:104 width:280, .ovcard with top+bottom brass rods (rod(1)/rod(0)), bottom rod has swaying tassels)_ — Sub 'Heart Law' + '{law}' headline; the heartSeal() medallion (book-flame + chapter beads ring, cinnabar-tinted if opposed); 'Chapter {chapter} / {chapters}' + up to 9 bead dots (filled if i<chapter); Clarity {clarity} + Turbulence {turbulence} (cinnabar if Low/Present); 'Dao Heart State' pill (Steady/Needs Work); '.btn jade' button 'Open Dao Heart Sanctuary'.
- **midA column (Run Validity / Expression Cap / Proc)** _(absolute left:990 top:104 width:250 z-index:4, three stacked .ovcard)_ — Card 1 Run Validity: '{run}' (with 'but'-> linebreak) + wax seal '勉強' (jade if ok). Card 2 Expression Cap: big '{expressionCap}' + 218px progress bar (fill = parseInt(cap)%, cinnabar/jade) with 25/50/75 ticks + gold pointer + 'Cap suppressed by opposed fit' / 'Cap healthy'. Card 3 Proc: hourglass icon (flick anim if not opposed) + 'Proc' + '{proc name}' + '{Inactive|Ready} · CD 12s'.
- **midB column (warning / grade & multiplier / root notes)** _(absolute left:1262 top:104 width:248 z-index:4, three stacked .ovcard)_ — Card 1: '械鑑' wax seal + cinnabar warning sentence 'Current law fit reduces cultivation efficiency and breakthrough stability.' Card 2 'Root Grade & Multiplier': rows Grade/Common, Purity/{purity}%, Fit Multiplier (-2.32x|+1.4x), Effective Output (~0.43x|~1.4x base). Card 3 'Root Notes': flavor paragraph about Void roots & Ember laws.
- **Spirit Root Observation tabbed drawer (right edge)** _(absolute right:18 top:96 width:330 .ovcard padding:0 flex)_ — Vertical tab rail (46px, jade gradient) with 6 vertical tabs Profile/Fit/Effects/Variants/Routes/History (Profile active). Pane: 'Spirit Root Observation' sub-heading; a key/value table (Purity, Expression, Total Multiplier, Current Proc, Status Inactive, Run Validity, Cooldown 12s); '— Fit Analysis —' rows (Root/Law Fit, Fit Multiplier); '— Practice Guide —' Recommended sentence; a '.btn' '👁 Observe Spirit Root' button.
- **Diagnostic Detail band label** _(absolute left:18 top:578 z-index:5)_ — Sub label '— Diagnostic Detail —' marking the bottom row of cards.
- **Root / Law Synergy Map (bottom row, far-left)** _(absolute left:18 top:600 bottom:16 width:300 .ovcard)_ — Sub 'Root / Law Synergy Map'; SVG with jade '根' disc -> path (jagged red if opposed, straight jade if aligned) -> gold/cinnabar '法' disc; 'Bridge fractured — Opposed' / 'Bridge intact — Compatible' + explanatory sentence.
- **Current Impact (bottom row)** _(absolute left:340 top:600 bottom:16 width:300 .ovcard)_ — Sub 'Current Impact' (cinnabar); 5 fixed rows: Cultivation Efficiency Low↓, Breakthrough Stability Low↓, Qi Recovery Reduced, Skill Expression Suppressed, Breakthrough Risk Increased↑; footnote 'An opposed root suppresses every downstream effect until fit improves.'
- **How to Improve (bottom row)** _(absolute left:660 top:600 bottom:16 width:360 .ovcard)_ — Sub 'How to Improve' (jade); 5 fixed rows each ◆ action + small gold destination tag: Refine Heart Law comprehension/Dao Heart Sanctuary, Raise Purity/Cultivation, Align Root/Law synergy/Reincarnation reroll, Stabilize Dao Heart turbulence/Verses & seals, Increase Expression Cap/Training Hall.
- **Compatible Heart Laws / Variants (bottom row, far-right)** _(absolute left:1040 right:18 top:600 bottom:16 .ovcard)_ — Sub 'Compatible Heart Laws' (gold); 4 fixed rows: Hollow Void Canon Best fit, Abyssal Stillness Sutra Strong, Ember Thread Sutra Opposed (cinnabar), Iron Marrow Litany Neutral; footnote about switching to a Void-aligned law removing the -2.32x penalty.

**Surface mapping:**
- [y] Overlay title 'Spirit Root Astrolabe' ← `rootLawInstrument.astrolabe.title (const) / rootLawInstrument.title` — astrolabe.title literally 'Spirit Root Astrolabe'.
- [n] Overlay sub 'Root & Heart Law entity diagnostic' ← `(none — static copy)` — Decorative shell copy; hardcode in overlay.
- [y] Context chips Next Goal / Main Bottleneck / Primary Route ← `meta.visualState + bottleneckCanopy.centralEdict.{label,detail,primaryAction} / lifeDecree` — Shell-level. Derive from bottleneckCanopy.centralEdict + lifeDecree.hero, NOT from the astrolabe surface. CAUTION: the chip artifact label 'Primary Route' is FORBIDDEN — relabel (e.g. 'Suggested Next Step').
- [y] Profile headline '{Element} Root' ← `rootLawInstrument.astrolabe.spiritRoot.elementLabel` — = StatusSpiritRootSurface.elementLabel.
- [y] Profile 'Grade Common' ← `rootLawInstrument.astrolabe.gradeLabel` — Real grade label; do not hardcode 'Common'.
- [y] Profile / table 'Purity {n}%' ← `rootLawInstrument.astrolabe.purityLabel` — Nullable string; render conditionally. Numeric arc uses parseLeadingInt(purityLabel).
- [y] Root/Law Fit value ← `rootLawInstrument.astrolabe.fitLabel + .fitTier` — fitTier in aligned|compatible|strained|opposed|unknown drives jade/cinnabar; do NOT assume binary opposed.
- [y] Total Multiplier '+1.4x' / 'Mismatched -2.32x' ← `rootLawInstrument.astrolabe.totalMultiplierLabel` — Real label exists; artifact's literal '-2.32x'/'+1.4x' are fixture-only — bind to totalMultiplierLabel.
- [y] Run Validity value + 勉強 wax seal ← `rootLawInstrument.astrolabe.runValidityLabel` — Nullable; seal variant from fitTier (validityOk = aligned|compatible).
- [y] Astrolabe dial — active root, 14 glyph ring, fit bands, needle ← `rootLawInstrument.astrolabe.activeRootId + .notches[] (id,label,shortLabel,angleDeg,active,tone,ariaLabel) + .fitTier` — Reuse existing StatusSpiritRootAstrolabe dial SVG verbatim (already 1:1 with astrolabe()). 14 notches present with 360/14 angles.
- [y] Purity arc value/color + numeric bead ← `rootLawInstrument.astrolabe.purityLabel` — Same parseLeadingInt logic as current component.
- [y] 14 Major Roots legend rows ← `rootLawInstrument.astrolabe.notches[] (label/shortLabel + active + tone)` — Element color via existing var(--root-{id}); glyph via GLYPH map in component. No 'grade' per-root in surface (artifact had none either).
- [y] Expression Cap value + progress bar ← `rootLawInstrument.astrolabe.expressionCapLabel` — Nullable; reuse parseExpressionCap for fill width. 'Cap suppressed/healthy' note derived from fitTier.
- [y] Proc card name/status/cooldown ← `rootLawInstrument.astrolabe.proc {name,statusLabel,cooldownLabel}` — Whole proc object nullable. 'CD 12s' comes from cooldownLabel, not hardcoded.
- [y] Current Impact rows (5) ← `rootLawInstrument.astrolabe.impactRows (StatusLedgerFactRow[])` — Bind real impact rows; artifact's fixed 5-row list is fixture flavor. Map label/value/tone.
- [y] Spirit Root Observation drawer tabs + rows ← `rootLawInstrument.spiritRootObservation (SpiritRootObservationSurfaceV1): .tabs[] {id,label,rows[]}, .profile, .fit, .effects, .variants, .practiceRoutes, .history, .vfxRows` — Full tabbed source already exists. Tab labels Profile/Fit/Effects/Variants/Routes/History map to tabs[].label (practice_routes->Routes). Each tab .rows give the key/value pairs.
- [y] 'Observe Spirit Root' + 'Open Dao Heart Sanctuary' route buttons ← `rootLawInstrument.astrolabe.routeActions[] (observeAction filter) + rootLawInstrument.heartLawSeal.routeAction` — Reuse existing button components/markers; route via onAction. spiritRootObservation.route.openTarget.kind='status_observation'.
- [y] Heart Law name + medallion ← `rootLawInstrument.heartLawSeal.heartLawLabel + .chapterBeads[]` — Reuse StatusHeartLawSeal SVG; distress = fitTier==='opposed'.
- [y] Chapter '{c}/{total}' + bead dots ← `rootLawInstrument.heartLawSeal.chapterLabel + .chapterBeads[] (active/filled)` — Beads carry filled flag; artifact caps at 9, beads array already sized.
- [y] Clarity / Turbulence / Dao Heart State pill ← `rootLawInstrument.heartLawSeal.clarityLabel / .turbulenceLabel / .daoHeartStateLabel` — All nullable; cinnabar when clarity 'Low' / turbulence 'Present' / state 'Needs Work'.
- [y] Bridge center (fracture vs intact) + fit seal 對沖/相合 ← `rootLawInstrument.bridge {state,fitTier,label,detail,broken,tone,ariaLabel}` — bridge.state aligned|compatible|strained|opposed|unknown; broken flag picks fracture vs link; seal char by opposed.
- [n] Root Grade & Multiplier card rows ← `astrolabe.gradeLabel + .purityLabel + .totalMultiplierLabel; Effective Output = (no field)` — Grade/Purity/Fit-Multiplier available; 'Effective Output ~0.43x base' has NO dedicated field — see missingData.
- [y] How to Improve rows + destination tags ← `spiritRootObservation.practiceRoutes.rows / bottleneckCanopy.bestImprovements` — Partial — practiceRoutes.rows give the recommended-actions list; the artifact's 5 fixed 'X / Destination' pairs are fixture copy. Bind to practiceRoutes.rows (label+detail) rather than hardcoding.
- [y] Compatible Heart Laws / Variants rows ← `spiritRootObservation.variants.rows (id,label,value,detail,tone)` — Real variant list exists; artifact's 4 hardcoded laws (Hollow Void Canon etc.) are fixture — bind to variants.rows; tone gives jade/cinnabar/muted.
- [y] Root/Law Synergy Map verdict + sentence ← `rootLawInstrument.bridge.label + .detail + .state` — 'Bridge fractured/intact' = bridge.label; sentence = bridge.detail; jade/cinnabar from bridge.fitTier.
- [y] midB warning sentence + 械鑑 seal ← `rootLawInstrument.bridge.detail (when opposed) / spiritRootObservation.fit.summary` — Use bridge.detail or fit.summary instead of the hardcoded warning string; seal '械鑑' decorative.
- [y] Root Notes flavor paragraph ← `spiritRootObservation.fit.summary / profile.playstyleLabel` — Bind to fit.summary or a profile field; artifact's Void/Ember prose is fixture-only flavor.

**Missing data:** Effective Output multiplier ('~0.43x base' / '~1.4x base') — no field; only totalMultiplierLabel exists. Either omit the row or compute display-only from purity x fitTier. | Per-root 'Grade' in the 14-root legend — surface notches carry no grade/purity per element (only the active root's gradeLabel). Render legend without per-row grade (artifact also omitted it). | Fixed 'Current Impact' wording (Qi Recovery Reduced, Skill Expression Suppressed, Breakthrough Risk Increased) — impactRows are real but their exact labels/values come from the surface, not these hardcoded strings; treat the artifact strings as fixture. | Variant verdict words 'Best fit / Strong / Neutral' — variants.rows.value/tone supply equivalents but the exact 4 law names are fixture content, not guaranteed by the surface. | Hourglass/'CD 12s' literal and 'Cooldown 12s' table row — only proc.cooldownLabel is available; if null there is no cooldown to show. | Decorative-only: ovShell grain/corners/mountains/motes/watermark/vertical-tag, wax-seal chars (根骨/對沖/相合/勉強/械鑑), brass rods+tassels on the seal scroll, qi-flow arcs — no surface backing, purely diegetic.

**Fidelity risks:** FORBIDDEN LABELS: ovShell renders a 'Primary Route' context chip and the drawers use 'Source:' patterns — the repo's proofSurfaceNormalization/statusRouteActions contracts ban literal 'Primary Route' and 'Source:'. Relabel the route chip (e.g. 'Suggested Next Step') and use sourceLabel without a 'Source:' prefix. | BUTTON-IN-BUTTON: the overlay has multiple route buttons (Observe Spirit Root, Open Dao Heart Sanctuary, per-row actions, tab controls). Keep each <button> a leaf; tabs as <button>/role=tab must not wrap action buttons; existing layoutStability/statusV2Layout contracts will flag nested interactives. | RAW HEX AUDIT: artifact uses #8b3028/#0f6f5a/#a2712a/#f3ead7 and many rgba()/element hexes. The repo enforces near-zero raw hex in observatory components (W6 net -2). Route every color through var(--observatory-*)/--paper-*/--root-* tokens; the inline element hexes in the 14-root legend must use var(--root-{id}). | CLS / single-layout: project decision is single-layout no-CLS (V7 re-ground). A full-screen modal that mounts on open can shift focus/scroll; reserve the overlay container and use the existing focus-trap/restore (useDialogFocusTrap) pattern, and do not reflow underlying regions. | A11Y: dial + medallion are role=img with aria-label (keep ariaLabel from surface). Vertical-text tabs need accessible names; the spinning rim/needle/flame motion must be gated by prefers-reduced-motion (W8 a11y precedent). Provide aria-live only where the existing announcer expects it; avoid duplicate live regions. | FIXTURE-AS-DATA TRAP: many artifact strings (-2.32x, +1.4x, 0.43x base, the 4 law names, the 5 impact rows, 'CD 12s', 'Grade Common') are fixture literals. Binding them verbatim would diverge from live data — bind to totalMultiplierLabel/impactRows/variants.rows/proc.cooldownLabel/gradeLabel instead, and gracefully omit nullable fields (purityLabel, expressionCapLabel, proc, runValidityLabel, daoHeart/clarity/turbulence) when null. | VOCAB: heartLawSeal title const is 'Heart Law Seal' and astrolabe.title is 'Spirit Root Astrolabe'; do not invent new titles. The spiritRootObservation route forbids the global 'spiritRoot' tab (route.forbiddenGlobalTabId) — keep routing through status_observation, not the global Spirit Root tab.

**Proposed SCSS classes:** statusAstrolabeOverlay, statusAstrolabeOverlay__profile, statusAstrolabeOverlay__rootsLegend, statusAstrolabeOverlay__rootsLegendRow, statusAstrolabeOverlay__dial, statusAstrolabeOverlay__bridge, statusAstrolabeOverlay__bridgeSeal, statusAstrolabeOverlay__sealScroll, statusAstrolabeOverlay__sealRod, statusAstrolabeOverlay__metricsColumn, statusAstrolabeOverlay__runValidityCard, statusAstrolabeOverlay__expressionCard, statusAstrolabeOverlay__expressionTrack, statusAstrolabeOverlay__expressionFill, statusAstrolabeOverlay__procCard, statusAstrolabeOverlay__notesColumn, statusAstrolabeOverlay__warningCard, statusAstrolabeOverlay__gradeCard, statusAstrolabeOverlay__rootNotesCard, statusAstrolabeOverlay__observationDrawer, statusAstrolabeOverlay__observationTabs, statusAstrolabeOverlay__observationTab, statusAstrolabeOverlay__observationPane, statusAstrolabeOverlay__diagnosticBand, statusAstrolabeOverlay__synergyMap, statusAstrolabeOverlay__impact, statusAstrolabeOverlay__improve, statusAstrolabeOverlay__variants, statusAstrolabeOverlay__card (shared .ovcard analogue), statusObservatoryOverlayShell, statusObservatoryOverlayShell__grain, statusObservatoryOverlayShell__corner, statusObservatoryOverlayShell__tag, statusObservatoryOverlayShell__head, statusObservatoryOverlayShell__title, statusObservatoryOverlayShell__sub, statusObservatoryOverlayShell__context, statusObservatoryOverlayShell__close, statusObservatoryOverlayShell__mountains, statusObservatoryOverlayShell__watermark, statusObservatoryOverlayShell__mote

---

## vessel → `StatusMeridianVesselOverlay.tsx`  (surface 17/19)

**Current app:** Today the app renders the vessel as the always-visible hero instrument StatusMeridianVesselCompass (figure SVG + 6 clickable organ seal buttons + StatusMeridianFocusLens + a separate StatusMeridianSharedCauseStamps ribbon), plus a simple expanded modal StatusMeridianVesselDrawer (role=dialog, aria-modal, focus-trapped) that lists the selected organ's detailRows, shared-cause stamps, and one Route button. There is no full-screen painterly overlay that wraps the figure + flanking notes/legend/cards/lens/shared-causes in the shared observatory chrome (title/sub/context-chips/seal/close/tag/mountains) — that is what this spec adds.

**Build plan:** 1) Build a SHARED observatory overlay-shell first (StatusObservatoryOverlayShell.tsx + .scss) that reproduces ovShell: header (title/sub on left padded ~74px; context chips + cinnabar InkWaxSeal + close ✕ on right), decorative grain (reuse InkGrain/InkObservatoryDefs), 4 gilt corner brackets, vertical stacked tag, watermark, ink-mountains band (new InkMountains React port — no existing component; port the generator path or a static SVG), and 5 motes. Props: title, sub, sealChars, tag, watermark, contextChips (Next Goal/Bottleneck/Route — relabel 'Primary Route'->'Route'), onClose, children. role=dialog, aria-modal, Esc + backdrop close, focus-trap via useDialogFocusTrap. Reuse for all 10 overlays. 2) Create StatusMeridianVesselOverlay.tsx consuming surface.meridianVessel + visualState + onAction. Maintain selectedOrganId state (reuse meridianVesselReducer + defaultSelectedOrganId from the compass) so organ clicks swap the focus lens (no page nav). 3) Lay out the __stage with absolute regions matching the artifact: left notes column (196px, 4 cards: Overview[static], Current Focus[focusLens.label], Inspection Mode[static], Vessel State[derived from organs.some danger]); right legend column (Legend from meridianVessel.legend + How This Works[static]); center figure (REUSE the existing statusMeridianVesselCompass figure SVG verbatim — already a 1:1 compass() port); full-bleed thread layer (per-organ bezier colored by meridianThreadColor(state)); 6 organ cards positioned left/right at y=[80,330,560] (reuse organ button markup: ordinal medallion, title, valueLabel, State label, decorative aria-hidden route pill); focus lens pinned center-bottom (REUSE StatusMeridianFocusLens/StatusFocusLens — already renders eyebrow/value/consequence/route/Sources with forbidden-label-safe labels); shared-causes ribbon pinned bottom (REUSE StatusMeridianSharedCauseStamps fed by sharedCauseStamps). 4) Data-bound bits: Current Focus, organ cards (ordinal/title/valueLabel/state/route), focus lens, vessel-state derivation, legend, shared causes. Decorative-only: figure anatomy, aura/halo/qi-core, motes, grain, corners, tag, watermark, mountains, organ glyph rings, route PILL (real route action stays in the lens). 5) Map raw hex to tokens: #8b3028->var(--observatory-cinnabar), #0f6f5a->var(--observatory-jade), #a2712a->var(--observatory-gold-deep, var(--observatory-gold)), #f3ead7->var(--observatory-paper); grey #69717c->var(--observatory-muted). 6) Wire close + onAction(route) through props; ensure single-layout (no responsive reflow) per the no-CLS decree.

**Artifact regions:**
- **Overlay shell — header (ovShell)** _(top band, full width; title block padding-left:74px)_ — Title 'Meridian Vessel Compass' + sub 'Full-state inner ledger of mind, body, root, path, preparation, and current work'. Right side: context chips (ovctx: Next Goal / Main Bottleneck / Primary Route) + cinnabar wax seal '內視' (waxSeal(52,-4)) + close ✕ (closeOverlay()). Decorative chrome: grain SVG overlay, 4 gilt corner brackets (gc tl/tr/bl/br), vertical tag '內視' (chars stacked), watermark '內視臟腑' (gcol, right:54px top:110px font 52px), ink mountains band (bottom, 1856x260, z0), 5 drifting motes.
- **Left notes column** _(position:absolute; left:18px; top:8px; width:196px; z-index:4)_ — 4 stacked ovcards: (1) 'Overview' descriptive paragraph; (2) 'Current Focus' showing fo.title in cinnabar bold 11px; (3) 'Inspection Mode' = gold ring+core SVG + 'Reserved Lens / One organ in focus — tap any organ seal'; (4) 'Vessel State' = dashed ring SVG (weakP if any organ atrisk else breatheA) + 'Strained/Stable' (cinnabar/jade) + 'Multiple risks detected'/'Functioning normally'.
- **Right legend column** _(position:absolute; right:18px; top:8px; width:196px; z-index:4)_ — 2 ovcards: (1) 'Legend' = 4 swatch rows: jade #0f6f5a 'Stable — functioning normally', gold #a2712a 'Needs Work — improvement required', cinnabar #8b3028 'At Risk — significant weakness', grey #69717c 'Locked — not yet unlocked'; (2) 'How This Works' explanatory paragraph.
- **Central vessel figure (fig)** _(position:absolute; left:50%; top:30px; transform:translateX(-50%); width:660px; height:620px; viewBox 0 0 700 640; z-index:1)_ — Painterly seated cultivator: gold radial aura ellipse (breatheA), head halo circle, robe path, base/sash path, central spine line (350,172→490) + glowing qiflow channel, 5 meridian-point rings at y=[130,200,280,360,440] (y=360 larger, carries qiCore disc), gold radial point cores (mp pulse, staggered delay).
- **Organ→body qi-thread layer (threads)** _(position:absolute; inset:0; z-index:2; viewBox 0 0 1856 912; preserveAspectRatio none; pointer-events:none)_ — 6 curved bezier threads from each organ card anchor (left=470/right=1386, y∈[126,376,606]) to a spine meridian source point (srcY=[156,224,301,379,456][o.src], bx≈928±20). Under-stroke colored by thcol(state); bright over-stroke (#ffe9b0 or #f0b9a0 if atrisk) animated qiflow; ring node at card end + pulsing bead at spine end.
- **6 organ cards (cards)** _(position:absolute; left:220px (i<3) / right:220px (i>=3); top:[80,330,560][i%3]; width:250px; border-left/right:4px solid stcol(state); z-index:4; clickable -> VLENS(i))_ — Each card: circular state-colored glyph chip (38px, o.glyph, dashed twk halo ring) + gold ordinal medallion bubble (o.n) + title (o.title) + sub (o.sub); footer row: 'State: {o.stateLabel}' colored + a '.btn' route pill ({o.action}). Selected card gets gold ring/glow box-shadow. Default-selected = organ whose title matches fo.title.
- **Focus Lens card (lens / vLensHTML)** _(position:absolute; left:50%; top:618px; transform:translateX(-50%); width:580px; cinnabar 2px border; torn clip-path polygon; z-index:5)_ — Eyebrow 'Focus Lens — Active Inspection' (cinnabar) + cinnabar wax seal '檢視' (waxSeal 38,7); big cinnabar title; 'Value:' line; 'Consequence:' line (muted); 'Route:' line (gold-deep bold + → medallion); 'Sources:' row of 1-3 cinnabar disc chips (cinnDisc, kai glyph + caption). Updated by VLENS(i) on organ click; default derived from state focus (fo.body split on '. ').
- **Shared Causes ribbon (shared)** _(position:absolute; left:18px; right:18px; bottom:8px; z-index:4)_ — One ovcard, flex space-around: label 'Shared Causes (All Organs)' + 6 fixed cause cells, each = small cinnabar twk ring SVG + caption (ink-45) + bold cinnabar value. Artifact-hardcoded cells: 'Heart Law parity'=Weak, 'Root / Law fit'=Opposed, 'Gate readiness'=Not secure, 'Expression cap'=Low, 'Foreground gain'=Low, 'Breakthrough risk'=High.

**Surface mapping:**
- [y] Shell title 'Meridian Vessel Compass' ← `meridianVessel.title` — Literal type 'Meridian Vessel Compass' — exact match. Sub-text is artifact-static copy (no surface field); hardcode as a presentation constant.
- [n] Shell context chip 'Next Goal' ← `(parent) lifeDecree.hero / milestone (not on meridianVessel slice)` — ovShell pulls Next Goal/Main Bottleneck/Primary Route from STATE.decree. meridianVessel slice does NOT expose decree; the shared overlay shell must take these from the parent surface (lifeDecree/bottleneckCanopy.centralEdict) or omit. NOTE: artifact label 'Primary Route' is FORBIDDEN vocab in this app — must be relabeled (e.g. 'Route').
- [y] Left 'Current Focus' value ← `meridianVessel.focusLens.label` — Artifact uses fo.title; map to focusLens.label (the selected-organ headline). focusLens.selectedOrganId picks the organ.
- [y] Left 'Vessel State' Strained/Stable + ring color ← `meridianVessel.organs[].state` — Derive 'any organ at danger' from organs.some(o=>o.state==='danger'). Artifact uses state==='atrisk'; app state enum is healthy|attention|danger|locked|unknown (map atrisk->danger).
- [y] Right Legend 4 rows ← `meridianVessel.legend` — legend: ReadonlyArray<{state,label,detail}>. Already provides Stable/Needs Work/At Risk/Locked with detail strings; render swatch from state via STATE_LABELS tone tokens.
- [n] Right 'How This Works' paragraph ← `(none)` — Artifact-static explanatory copy; hardcode as presentation constant (decorative/help text).
- [y] Central figure anatomy + meridian points + spine ← `(decorative; geometry constants)` — Pure painterly decoration. Reuse existing StatusMeridianVesselCompass figure SVG (MERIDIAN_MER, MERIDIAN_VIEW_W/H, organStyle) — already a 1:1 port of the compass() generator. No data binding beyond per-organ thread color.
- [y] Per-organ qi-thread color + selected/atrisk over-stroke ← `meridianVessel.organs[].state` — Thread under-stroke via meridianThreadColor(state) (already exists); bright over-stroke cinnabar-tint when state==='danger'. Source spine point index = STATUS_OBSERVATORY_MERIDIAN_ORGAN_GEOMETRY[id].src.
- [y] Organ card glyph chip ← `meridianVessel.organs[].icon` — organs[].icon is an IconId. Artifact uses o.glyph (a CJK char). App has no per-organ CJK glyph atlas; current compass renders ordinal number in disc (W4-deferred glyph atlas). Use icon or ordinal — true CJK glyph is MISSING.
- [y] Organ card ordinal medallion ← `meridianVessel.organs[].ordinal` — ordinal:1..6 — exact (artifact o.n).
- [y] Organ card title ← `meridianVessel.organs[].title` — Exact (artifact o.title).
- [y] Organ card sub-line ← `meridianVessel.organs[].valueLabel` — Artifact o.sub is a short descriptor; closest is valueLabel. A dedicated 'sub' string is not separately exposed — valueLabel is the value, not a subtitle. Partial gap.
- [y] Organ card 'State: {stateLabel}' ← `meridianVessel.organs[].state -> STATUS_OBSERVATORY_MERIDIAN_STATE_LABELS[state].label` — organs[].state + presentation STATE_LABELS gives Stable/Needs Work/At Risk/Locked (artifact o.stateLabel).
- [y] Organ card route pill ({o.action}) ← `meridianVessel.organs[].route` — organs[].route: StatusLedgerActionSurface | null. Pill text = route.label. In current app this pill is decorative (aria-hidden) and the real focusable route lives in the focus lens to avoid button-in-button.
- [y] Focus Lens eyebrow / value / consequence / route / sources ← `meridianVessel.focusLens.{label,value,consequence,route,sources}` — focusLens.label/value/consequence/route(StatusLedgerActionSurface|null)/sources(string[]). Reuse existing StatusMeridianFocusLens + StatusFocusLens components verbatim. On organ click, lens swaps to that organ via organ.valueLabel/consequence/route/detailRows (current StatusMeridianFocusLens already does this fallback).
- [y] Focus Lens 'Sources:' chips ← `meridianVessel.focusLens.sources / organs[].detailRows[].sourceSystem` — sources:string[]; fallback to detailRows.sourceSystem. CAUTION: artifact label text is 'Sources:' with a trailing colon — app vocab forbids 'Source:'; use plural 'Sources' label already defined in STATUS_OBSERVATORY_MERIDIAN_FOCUS_LABELS.sourcesLabel.
- [y] Shared Causes ribbon cells ← `meridianVessel.sharedCauseStamps[] (id,label,value,detail,tone,sourceLabel,ariaLabel)` — App ALREADY renders these via StatusMeridianSharedCauseStamps (data-bound). Artifact's 6 cells are hardcoded; sharedCauseStamps is the typed replacement (label + value). Also sharedCauseRows (StatusCauseRowSurface[]) carries fuller cause data.
- [y] Wax seal '內視' / '檢視' ← `(decorative)` — Reuse InkWaxSeal (chars,'內視'/'檢視', variant='cinnabar'). Decorative chrome, not data-bound.
- [y] buffDebuffRows / nextBottleneck ← `meridianVessel.buffDebuffRows, meridianVessel.nextBottleneck` — Available on the slice but the artifact vessel overlay does NOT visibly render buffs/debuffs or a nextBottleneck band (nextBottleneck.primary feeds the shell 'bottleneck' chip / drawer detail rows). Treat as drawer/expanded-detail data, not a primary overlay region.

**Missing data:** Per-organ CJK glyph (artifact o.glyph e.g. 神/體/根): meridianVessel.organs[] exposes icon:IconId + ordinal but no CJK organ glyph atlas (W4-deferred). Render ordinal medallion or IconId glyph as a fidelity stand-in. | Organ card short subtitle (artifact o.sub): no dedicated 'sub' field on StatusMeridianOrganSurface; valueLabel is the value not a descriptor. Would need a presentation-derived subtitle. | Shell context chips (Next Goal / Main Bottleneck / Primary Route) come from STATE.decree in the artifact, which is NOT part of the meridianVessel slice — the overlay shell must source them from the parent surface (lifeDecree / bottleneckCanopy.centralEdict) and relabel 'Primary Route' (forbidden vocab). | Right 'How This Works' + left 'Overview' / 'Inspection Mode' copy: static explanatory help text with no surface field — must live as presentation constants. | Shared Causes ribbon in the artifact is 6 hardcoded cells (Heart Law parity=Weak … Breakthrough risk=High); the typed sharedCauseStamps may have a different count/labels — exact cell count is data-driven, not the artifact's fixed 6. | VLENS default-organ resolution by string-matching fo.title against organ titles is artifact-only; app uses focusLens.selectedOrganId + organStateRank fallback (already implemented) — no extra data needed but the selection rule differs.

**Fidelity risks:** FORBIDDEN VOCAB: artifact shell chip literally reads 'Primary Route' and the focus-lens label is 'Sources:'. App contracts forbid 'Primary Route' and 'Source:' — relabel chip to 'Route' and keep the existing plural 'Sources' label (STATUS_OBSERVATORY_MERIDIAN_FOCUS_LABELS.sourcesLabel). Audit test will fail on the raw strings. | BUTTON-IN-BUTTON: organ cards are clickable (select organ) AND carry a route pill + the card sits inside the overlay. Keep the route pill decorative (aria-hidden, non-button) exactly like the current compass; route the real focusable action only through the focus-lens button. Do not nest a <button> route inside the card <button>. | RAW-HEX AUDIT: the artifact uses raw hex everywhere (#8b3028/#0f6f5a/#a2712a/#f3ead7/#69717c, #ffe9b0/#f0b9a0 thread glows, gradient stops). The SCSS gate forbids raw hex — every color must map to var(--observatory-*) tokens (cinnabar/jade/gold/gold-deep/paper/muted) or existing rgba helpers; thread-glow tints need a token or color-mix, not literal hex. | CLS / NO-CLS DECREE: this overlay must render at a single fixed layout (mockup-native units, uniform-scaled) — no media-query reflow of the absolute regions. Reserve fixed sizes for figure/cards/lens so opening the modal causes zero layout shift; layoutStabilityContract + statusV2LayoutContract will assert this. | A11Y: full-screen modal needs role=dialog + aria-modal + focus-trap + focus-restore + Esc/backdrop close (reuse useDialogFocusTrap, mirror existing drawer). Organ seals need roving tabindex + aria-pressed + aria-label (reuse compass pattern). Focus-lens is aria-live=polite; vessel-state ring should expose at-risk via aria, decorative SVGs aria-hidden. | SOURCE-OF-TRUTH duplication: the figure SVG, organ geometry, thread math and focus-lens already exist 1:1 in StatusMeridianVesselCompass — reuse/extract them rather than re-porting, or the two renderers will drift and break the S4 'class string co-located with renderer' contract. | SHELL CONTEXT CHIPS not on the meridianVessel slice (decree lives in lifeDecree/bottleneckCanopy). Pulling them requires the parent surface; if unavailable, omit chips rather than fabricating — fabricated goal/bottleneck/route values risk vocab + correctness audits. | SHARED CAUSES count: artifact hardcodes 6 cells; sharedCauseStamps length is data-driven. Do not pad/truncate to 6 — render the actual array (proofSurfaceNormalizationContract / statusLedger contracts assert surface-faithful counts).

**Proposed SCSS classes:** statusMeridianVesselOverlay (full-screen modal root, role=dialog), statusMeridianVesselOverlay__shell (shared observatory overlay-shell host), statusMeridianVesselOverlay__stage (relative positioning canvas for absolute regions), statusMeridianVesselOverlay__notes (left 196px column), statusMeridianVesselOverlay__notesCard, statusMeridianVesselOverlay__legend (right 196px column), statusMeridianVesselOverlay__legendRow, statusMeridianVesselOverlay__figure (reuse statusMeridianVesselCompass__figure markup), statusMeridianVesselOverlay__threadLayer (full-bleed organ qi-thread SVG), statusMeridianVesselOverlay__organCards (absolute-positioned card grid), statusMeridianVesselOverlay__organCard (--side-left/--side-right, data-organ-state, data-selected), statusMeridianVesselOverlay__organCardGlyph, statusMeridianVesselOverlay__organCardRoute (decorative route pill), statusMeridianVesselOverlay__focusLens (reuse statusFocusLens, torn clip-path), statusMeridianVesselOverlay__sharedCauses (bottom ribbon, reuse statusMeridianVesselCompass__sharedCauseStamps), statusObservatoryOverlayShell + __head / __title / __sub / __ctx / __ctxItem / __seal / __close / __tag / __grain / __corner / __mountains / __mote (shared shell BEM, new)

---

## canopy → `StatusCanopyOverlay.tsx`  (surface 21/23)

**Current app:** No dedicated full-screen overlay exists. The canopy currently renders INLINE inside the main observatory grid (obsRegion--canopy) via StatusBottleneckTalismanCanopy.tsx, split into three `part` variants ('cluster' = central edict + 4 pinned slips + brass rail + cords + causal-thread layer; 'charms' = best-improvement charm rod; 'safety' = mercy/safety-net seal), with the StatusBottleneckInspector rendered in an `obsVisuallyHidden` wrapper (slip selection updates it but it is not visible). There is no modal/overlay-shell mechanism in the app at all — only support drawers (StatusObservatoryDrawers: buildPreparation/currentWork/recentChanges/calculation/sourceCoverage) and a non-modal whole-stage StatusObservatoryStateOverlays (scorch/ritual atmosphere only). The 'Open Canopy' affordance is just a hint; clicking a region does not open a focused canopy view.

**Build plan:** 1) Build a SHARED overlay shell first (StatusObservatoryOverlayShell, used by all detail overlays) that ports ovShell: header (title/sub + context chips + InkWaxSeal + ✕ close), vertical tag, InkGrain, 4 corner brackets, InkMountains bottom band, watermark, motes. It must be a focus-trapped role=dialog aria-modal=true (reuse useDialogFocusTrap) with Esc-to-close and focus restore. Context chips take decree {goal, bottleneck, route} as props (shell scope). RELABEL the route chip away from 'Primary Route'. 2) Create StatusCanopyOverlay.tsx consuming surface.bottleneckCanopy + onAction + open/onClose + decree props; wrap children in the shell with title=surface.title, subtitle=surface.subtitle, sealChars=瓶頸, tag=厄結因緣. 3) Reuse existing logic from StatusBottleneckTalismanCanopy: selectedSlipId state, useObservatorySelection, edictChop/edictEyebrow, parseProgressPips, canopyModeForVisualState — lift shared helpers rather than duplicate. 4) Lay out the body with a single stable absolute/grid map keyed to the artifact (threads left 18/96 w230; edict center top128 w330; 4 slips at talismanSlips[].geometry x/y/rotationDeg; inspector right 18/96 w300; charm rail bottom-center; safety bottom-right; summary right 556 w300; diagnosis chop bottom-left). Decide a no-CLS layout: a fixed-aspect viewport with the same single-layout scaling the inline screen uses (useObservatoryScale) so positions never reflow. 5) Data-bind: edict ← centralEdict (label/detail/sourceLabel-no-prefix/primaryAction); slips ← talismanSlips[] (replace SLIPS) wired to inspector; inspector ← reuse StatusBottleneckInspector (now visible, not obsVisuallyHidden) fed by selectedSlip ?? inspector; charms ← routeCharms[] reusing the existing charm-medallion markup + charm.order; safety ← safetySeal reusing the mercy-ring + pips markup + action; causal panel ← causalThreads[] (label/detail/tone) and also pass to StatusCausalThreadLayer for the decorative fan. 6) Decorative-only (no binding): fan/header-arc SVG, diagnosis 診斷 chop, mountains, grain, corners, motes, pin dots, brass roller caps, tassels — render via Ink* primitives + raw SVG using var(--observatory-*) tokens. 7) Resolution Reading: compose from causalThreads or omit — do not hard-code artifact copy. 8) Wire an 'Open Canopy' affordance in StatusLivingStateObservatory: add overlay open state + a focusable trigger on obsRegion--canopy that sets it open, render <StatusCanopyOverlay> alongside StatusObservatoryDrawers, route onAction through the existing handler. 9) Map every raw hex to tokens (#8b3028→--observatory-cinnabar, #0f6f5a→--observatory-jade, #a2712a→--observatory-gold/--observatory-gold-deep, #f3ead7→parchment/paper token) in SCSS; tones via data-tone, never inline hex. 10) Add data-testid='status-canopy-overlay' and per-region testids mirroring inline (status-bottleneck-edict/slip/route-charm/safety-seal/inspector) for contract continuity.

**Artifact regions:**
- **ovShell chrome (shared)** _(Full-bleed overlay wrapper. Header band ovhead padding-left:74px (left: title 'Bottleneck Talisman Canopy' .ovtitle + sub 'The node of obstruction and the paths to resolution' .ovsub; right cluster: .ovctx context chips, waxSeal('瓶頸',52,-4), .ovclose ✕ at far right). Vertical tag .ovtag '厄結因緣' (chars stacked, left edge). Grain .grain full-bleed; 4 corner brackets .gc tl/tr/bl/br; ink mountains bottom band left:0 right:0 bottom:0 height:260px z:0; right watermark .gcol right:54px top:110px 52px '厄結因緣'; 5 floating .mote particles. ovcontent holds the body.)_ — Shared overlay frame. .ovctx renders THREE chips: 'Next Goal'=dc.goal, 'Main Bottleneck'=dc.bottleneck (color dc.bottleneckCol|#8b3028), 'Primary Route'=dc.route (color #0f6f5a). NOTE the literal label 'Primary Route' — forbidden in this codebase; must be relabeled (e.g. 'Route' / 'Next Route').
- **Causal Threads panel (threads)** _(position:absolute; left:18px; top:96px; width:230px; z-index:4)_ — Sub label 'Causal Threads (from Status)'. Three .ovcard rows, each = cinnabar disc SVG (twk spin) with a Kai glyph + bold title + small detail line + cinnabar chip badge. Artifact hard-codes: ['備','Build / Prep','Below minimum','AT RISK'], ['根','Root / Law Fit','Opposed','OPPOSED'], ['兵','Weapon Intent','Lowest weapon stat','WEAK LINK']. Footer .ovcard prose: 'Threads run from each cause into the edict. Cut the cause and the knot loosens.'
- **Central Edict card** _(position:absolute; left:50%; top:128px; transform:translateX(-50%); width:330px; text-align:center; border:2px solid rgba(139,48,40,.45); z-index:5; box-shadow drop)_ — Gold pin dot at top. Sub eyebrow 'Current Bottleneck'. Big cinnabar serif title = c.bottleneck ('Realm edge not reached', <br> stripped). Divider rule. Attribution line 'Source: '+c.source ('Status Analysis') — NOTE literal 'Source:' prefix is forbidden. 'Primary Action' label + bold c.action ('Refine Weapon'). waxSeal('瓶頸',46,7) bottom-right.
- **Pinned talisman slips (x4)** _(Four .ovcard.clickable absolutely positioned from SLIPS geometry: slip0 (452,118) rot -2; slip1 (1188,118) rot 2; slip2 (452,352) rot 1; slip3 (1188,352) rot -1. width:196px; z-index:4; gold pin dot top; cursor pointer; onclick=CLENS(i).)_ — Each slip: priority disc '!' chip in tone color (prc), bold title s.t, rotated priority badge s.pr ('PRIORITY 1'/'RISK'/'PRIORITY 2') outlined in prc, and 'Route: '+s.route (jade). Selected slip gets gold ring/glow box-shadow (CLENS). Titles: 'Current State blocked', 'Loadout / pouch warning', 'Root / Law opposed', 'Heart Law clarity low'.
- **Expanded Inspector card** _(position:absolute; right:18px; top:96px; width:300px; border:1px solid; z-index:6; overflow:visible. Initialized to SLIPS[1] then CLENS(1) on load.)_ — cLensHTML(s): brass roller-bar caps top & bottom (SVG, gold end-knobs). Header sub '👁 Expanded Inspector' + waxSeal('檢視',38,6). '!' tone disc + cinnabar serif slip title. Body s.body prose. s.rows[] as label/bold-value dashed rows (e.g. Stage progress 1/9, Stability 0/100, Realm edge Not reached, Foreground Idle). 'Route:' jade s.route. Footer: jade .btn s.route + jade disc with s.glyph.
- **Route charms rail (x4)** _(position:absolute; left:50%; top:600px; transform:translateX(-50%); width:720px; z-index:4. Charms at board-x 642/768/1062/1188 (local left = x-608). Each width:80px swayH.)_ — Brass spear-rod across (in threadSvg, y=568, 'BEST IMPROVEMENT ROUTES' plaque centered, end spear-tips, 4 hanging nub lines). Four charms: ['兵','Refine Weapon','jade'], ['療','Restock Healing','jade'], ['術','Upgrade Technique','gold'], ['心','Practice Dao Heart','gold']. Each = ellipse medallion (jadeRad/goldRad) + Kai glyph + swaying tassel + bold label + 'Route charm' caption.
- **Diagnosis chop (decorative)** _(position:absolute; left:26px; bottom:22px; z-index:4; opacity:.85; 92x92 breatheA)_ — Two-ring ink stamp with stacked Kai '診斷' (Diagnosis). Purely decorative chrome.
- **Safety Net (Mercy Path) seal** _(position:absolute; right:30px; bottom:18px; width:280px; z-index:5; text-align:center)_ — 200x150 SVG: large parchment circle + dashed gold ring, two swaying tassels, small lotus/heart glyph, text 'Inactive / unavailable', 'Progress 0 / threshold', 'Cost hidden or disabled'. Sub label below 'Safety Net (Mercy Path)'.
- **Resolution Reading summary** _(position:absolute; right:18px; top:556px; width:300px; z-index:4)_ — .ovcard, sub 'Resolution Reading' (gold-deep). Prose: 'Four sources feed this bottleneck. Clearing Build / Prep and the weapon floor first unblocks the realm edge; the opposed root is a longer-term reroll.' (artifact hard-codes this narrative).
- **Threads/fan SVG backdrop (decorative)** _(absolute inset:0; z-index:1; viewBox 0 0 1856 912; pointer-events:none)_ — Fan skeleton from pivot [915,1015] (radial spokes ±54°, 3 arcs r=445/585/725), top brass header arc M120,64→1710,64 with gold end caps, dashed cinnabar 'thread' paths from threads panel into edict, charm spear-rod with plaque. Decorative geometry; the data is conveyed by the cards, not the SVG.

**Surface mapping:**
- [y] Overlay title / subtitle ← `bottleneckCanopy.title ('Bottleneck Talisman Canopy') / bottleneckCanopy.subtitle` — Literal strings present on the surface; use them rather than hard-coding.
- [y] Context chip 'Next Goal' ← `lifeDecree (decree goal) — surface.lifeDecree; in artifact STATE.decree.goal. Confirm exact field on lifeDecree block.` — Comes from the shared life-decree block, not bottleneckCanopy. Shell-level prop; thread decree goal/bottleneck/route into the overlay shell.
- [y] Context chip 'Main Bottleneck' ← `bottleneckCanopy.centralEdict.label (+ .visualState for color)` — Edict label is the bottleneck text; tone via visualState data-attr, never raw #8b3028.
- [y] Context chip route (artifact 'Primary Route') ← `bottleneckCanopy.centralEdict.primaryAction.label / .destinationLabel` — RELABEL: do not emit literal 'Primary Route' (forbidden). Use 'Route' or 'Next Route'. Color via tone token, not #0f6f5a.
- [y] Edict eyebrow ('Current Bottleneck') ← `derived from canopyMode (presentation.canopyMode) via CANOPY_EDICT_EYEBROW map in existing component` — Section chrome, not a surface field — reuse edictEyebrow(resolvedCanopyMode).
- [y] Edict title (big cinnabar) ← `bottleneckCanopy.centralEdict.label` — Direct.
- [y] Edict detail/prose ← `bottleneckCanopy.centralEdict.detail` — Artifact has no detail line in edict; surface provides one — render as supporting line.
- [y] Edict 'Source:' attribution ← `bottleneckCanopy.centralEdict.sourceLabel` — RELABEL: render sourceLabel WITHOUT the 'Source:' prefix (forbidden literal). Existing component already renders it as a bare __edictSource line.
- [y] Edict 'Primary Action' button ← `bottleneckCanopy.centralEdict.primaryAction (StatusLedgerActionSurface: label/destinationLabel/target/disabled/disabledReason/tone)` — Real route action with disabled/disabledReason. 'Primary Action' label is artifact chrome; existing edictActionLabel() composes label / destinationLabel.
- [y] Edict wax seal chop 瓶頸 ← `derived from centralEdict.visualState via CANOPY_EDICT_CHOP map` — Decorative; InkWaxSeal variant keyed by visualState (blocked→瓶頸 cinnabar, healthy→順遂 jade, etc.).
- [y] Pinned slips (title/priority/state/route/tone/geometry) ← `bottleneckCanopy.talismanSlips[] → .title, .priorityLabel, .stateLabel, .routeLabel, .routeAction, .tone, .geometry.{x,y,rotationDeg,anchor}, .sourceFamily, .sourceLabel, .ariaLabel` — Fully data-bound — replaces hard-coded SLIPS[]. Geometry x/y/rotationDeg map directly to absolute positioning (artifact 452/1188 × 118/352).
- [y] Slip '!' priority disc color ← `talismanSlips[].tone` — data-tone attr → token, not slip.prc raw hex.
- [y] Slip selection / inspector sync ← `bottleneckCanopy.inspector.selectedSlipId + local selection state (useObservatorySelection)` — Existing component already manages selectedSlipId + shared selection; CLENS(i) equivalent = onClick setSelectedSlipId + select('talisman', id).
- [y] Expanded inspector card (title/detail/source/priority/rows/route) ← `bottleneckCanopy.inspector.{title,detail,sourceLabel,sourceFamily,priorityLabel,stateLabel,tone,routeAction,detailRows} OR selectedSlip fields` — StatusBottleneckInspector already consumes this; promote it from obsVisuallyHidden into a visible overlay region. detailRows = artifact s.rows[].
- [y] Inspector row label/value/source ← `inspector.detailRows[] / selectedSlip.detailRows[] (StatusLedgerRequirementRow|StatusLedgerFactRow|StatusCauseRowSurface)` — rowValue()/rowSource() helpers already handle the union. NOTE inspector's existing 'Source' label is a meta heading; ensure no 'Source:' prefix on a value.
- [y] Inspector jade route button + glyph ← `selectedSlip.routeAction ?? inspector.routeAction (.label/.destinationLabel/.target/.disabled/.tone)` — Real action. Artifact s.glyph (功/藥/根/心) is decorative — NOT on surface (see missingData).
- [y] Route charms rail (label/detail/action/order/geometry) ← `bottleneckCanopy.routeCharms[] → .label, .detail, .action (StatusLedgerActionSurface), .order, .geometry.{x,y}` — Fully data-bound — replaces hard-coded charm array. order drives the medallion number; geometry.x/y for rail placement. Artifact glyphs 兵/療/術/心 are decorative.
- [y] Charm plaque 'BEST IMPROVEMENT ROUTES' ← `bottleneckCanopy.bestImprovements.title ('Best Improvements')` — Use surface title; avoid coining a new 'Best Improvement Routes' literal if a vocab contract restricts it. Charms themselves come from routeCharms (derived from bestImprovements actions).
- [y] Safety Net seal (label/state/progress/tone/rows/action) ← `bottleneckCanopy.safetySeal.{label,stateLabel,progressLabel,tone,action,rows[]}` — Existing 'safety' part renders this. progressLabel parsed into grayscale pips. Artifact texts 'Inactive/unavailable','Progress 0/threshold','Cost hidden or disabled' map to stateLabel/progressLabel/rows.
- [y] Causal Threads panel (3 rows) ← `bottleneckCanopy.causalThreads[] → .label, .detail, .tone, .fromId/.fromFamily, .toId/.toFamily` — Replaces artifact's hard-coded 備/根/兵 trio. The Kai glyph + AT RISK/OPPOSED chip text is decorative/derived; label+detail+tone are the data. Same array also feeds StatusCausalThreadLayer SVG.
- [y] Mission requirements (gate context) ← `bottleneckCanopy.missionRequirements.{rows[] (StatusLedgerRequirementRow), emptyState}` — Available but artifact does not surface a distinct mission-requirements card in the canopy overlay; can feed the edict/inspector or a 'requirements' sub-list. Existing inline component anchors mission-requirements testid on the edict.
- [n] Resolution Reading summary prose ← `(none — narrative is artifact-authored)` — No surface field carries this synthesized sentence. Either omit, or compose from causalThreads labels; do NOT hard-code the artifact's literal copy as if data.
- [n] Diagnosis 診斷 chop / fan SVG / mountains / motes / grain / corners ← `(none — decorative chrome)` — Decorative-only. Reuse InkGrain, InkMountains, corner brackets, InkWaxSeal; no surface binding.

**Missing data:** Resolution Reading summary sentence — no surface field; artifact hard-codes the four-sources narrative. Compose from causalThreads or omit. | Per-slip glyph (功/藥/根/心) and per-charm glyph (兵/療/術/心) — artifact decorative Kai marks; surface exposes no glyph field on talismanSlips or routeCharms (existing charm renders charm.order as a number instead). | Causal-thread Kai glyph (備/根/兵) and uppercase chip text (AT RISK/OPPOSED/WEAK LINK) — StatusCausalThreadSurface has label/detail/tone but no glyph or short badge string; badge must be derived from tone or omitted. | Edict gold pin / brass roller caps / spear-rod plaque geometry — purely decorative SVG; no data drives them. | Safety-net literal lines 'Cost hidden or disabled' / 'Progress 0 / threshold' — must come from safetySeal.rows[]/progressLabel; if a row is absent the artifact line cannot be reproduced 1:1. | Context-chip 'Next Goal' value lives on lifeDecree (shell scope), not on bottleneckCanopy — overlay must receive decree goal/bottleneck/route as shell props.

**Fidelity risks:** Forbidden literal 'Primary Route': artifact ovShell context chip and any route labeling use it — MUST relabel (vocab contract daoMandateP8VocabularyDecommissionContract bans /Primary Route/i). Use 'Route'/'Next Route'. | Forbidden literal 'Source:': artifact edict prints 'Source: '+source and inspector has a 'Source' heading — render sourceLabel as a bare attribution with no 'Source:' prefix (CultivationExact contract bans 'Source:' / 'Primary Route'). | CLS risk: artifact uses many position:absolute cards at fixed px on a 1856-wide canvas. The codebase is no-CLS / single-layout (V7 re-ground decision). Must use the established fixed-aspect viewport + useObservatoryScale, not media-query reflow; layoutStability/statusV2Layout contracts will flag shifts. | Button-in-button: edict route, each slip (button), inspector route, each charm (button), and safety-net action are all interactive. The slip is itself a <button> — its route must NOT nest another button; keep route as the inspector's button or the slip's onClick-only (as the inline component does). Charm rail is buttons on a decorative rod — keep rod aria-hidden, do not wrap charms in an outer button. | Raw-hex audit: artifact is saturated with raw hex and rgba (#8b3028/#0f6f5a/#a2712a/#f4dccb + rgba spokes). Net-raw-hex audits expect tokens; map all to var(--observatory-*) and data-tone. SVG fan/thread strokes are the biggest offenders — use currentColor/token stroke vars. | a11y: overlay must be role=dialog aria-modal=true with focus trap + restore + Esc (reuse useDialogFocusTrap); slips need roving tabindex (existing handler) and aria-pressed; inspector aria-live=polite; decorative SVG/chops aria-hidden; close button labeled. Announce slip selection without duplicating. | Decorative vs data confusion: Resolution Reading prose, per-slip/charm glyphs, causal badge text (AT RISK/OPPOSED) are NOT on the surface — do not present invented strings as data; derive from tone/label or omit to avoid fabricating evidence (combatRouteLocalLens-style forbidden-copy patterns: no 'TODO/deferred/Packet'). | Charm plaque text: avoid coining a new 'BEST IMPROVEMENT ROUTES' all-caps literal if it diverges from surface vocab — prefer bestImprovements.title ('Best Improvements'). | Selection-state divergence: overlay and inline canopy both manage selectedSlipId — ensure shared useObservatorySelection so opening the overlay reflects the inline selection and vice-versa (statusRouteActions / selection contracts).

**Proposed SCSS classes:** statusCanopyOverlay, statusCanopyOverlay__shell, statusCanopyOverlay__head, statusCanopyOverlay__title, statusCanopyOverlay__sub, statusCanopyOverlay__ctx, statusCanopyOverlay__ctxChip, statusCanopyOverlay__seal, statusCanopyOverlay__close, statusCanopyOverlay__tag, statusCanopyOverlay__content, statusCanopyOverlay__threadsPanel, statusCanopyOverlay__causalRow, statusCanopyOverlay__edict, statusCanopyOverlay__edictEyebrow, statusCanopyOverlay__edictSource, statusCanopyOverlay__edictRoute, statusCanopyOverlay__slips, statusCanopyOverlay__slip, statusCanopyOverlay__slipBadge, statusCanopyOverlay__inspector, statusCanopyOverlay__inspectorRow, statusCanopyOverlay__charms, statusCanopyOverlay__charmRod, statusCanopyOverlay__charm, statusCanopyOverlay__charmPlaque, statusCanopyOverlay__safety, statusCanopyOverlay__safetyPips, statusCanopyOverlay__summary, statusCanopyOverlay__diagnosisChop, statusCanopyOverlay__fan, statusCanopyOverlay__mountains

---

## constellation → `StatusConstellationOverlay.tsx`  (surface 18/20)

**Current app:** No dedicated full-screen overlay exists for the constellation. Today the app renders only the compact in-grid instrument `StatusStatMeridianConstellation.tsx` (in obsRegion--constellation): a 100x100 viewBox SVG atlas with 28 selectable node buttons across Universal/Heaven/Earth/Martial branches, branch wash/brush atmosphere, the universal spine glow path, bridge sockets, weak-link threads, a hidden plaque/legend/strip (kept in DOM for no-loss), and an `Open Constellation` text hint. The hidden `StatusStatBeadLens` provides the per-bead inspector. No modal is wired; clicking the hint does nothing.

**Build plan:** 1) Extract a shared overlay-shell first: create StatusObservatoryOverlayShell.tsx (+ .scss) that ports ovShell — props {title, subtitle, sealChars, tag, watermark, contextChips, onClose, children}. It renders the header band (title/sub, context-chips, wax seal via SealStamp/InkWaxSeal, .ovclose), left vertical tag, watermark, grain, 4 corner brackets, ink mountains strip, motes (motion-gated). Wire as a role='dialog' aria-modal='true' full-screen modal using useDialogFocusTrap (already used by drawers) for focus-trap/restore + ESC=onClose. Context chips relabel 'Primary Route'->'Route' and drop any 'Source:' prefix. This shell is reusable by all overlays. 2) Build StatusConstellationOverlay.tsx consuming surface.statConstellation (+ lifeDecree.hero for chips, meta.currentPath/visualState). Render the shell, then a single full-bleed canvas. 3) Canvas core: reuse the compact instrument's SVG approach but scale the 100x100 atlas to fill the overlay (single layout — no responsive grid reflow to preserve no-CLS). Draw branch-path edges (STATUS_OBSERVATORY_STAT_BRANCH_PATHS), spine glow, then 28 nodes from surface.nodes positioned by STATUS_OBSERVATORY_STAT_NODE_GEOMETRY, each a <button> (NOT nested in another button) with data-node-state/data-contribution-state/data-weak-link, aria-label from ariaLabelForNode, onClick selecting into useObservatorySelection + local lens state. 4) Atmosphere (atmosphere group, aria-hidden, motion-gated via useRitualMotion/useObservatoryMotion): gold brush behind Martial, ink clouds behind Heaven, ridges under Earth, faint figure linework behind spine — decorative only. 5) Branch banner tablets from branchPaths.title + PATH_LABELS.detail (data-bound text, decorative frame). UNIVERSAL SPINE card from PATH_LABELS.universal + foundation legend copy (static). 6) Bridge sockets from surface.bridgeSockets + STATUS_OBSERVATORY_STAT_BRIDGE_GEOMETRY (positions decorative, label/detail/state data-bound). 7) Legend card: iterate surface.legend[] -> swatch keyed by nodeState. 8) Explanation card: static prose + decorative '星訣' seal. 9) Plaque card (absolute left/bottom): Current Path (currentPath label), Harmony (derive from weakLinks.length + visualState label map), Bridge Integration (bridgeSockets[].state), Realm (lifeDecree.hero.realmName+stageText); footer counts from branchCounts. Decorative brass rod/tassels (motion-gated sway). 10) Weak-link layer: if weakLinks.length>0 draw cinnabar thread + 'Current Weak Link' callout (weakReason) + 'Connected to Current Bottleneck' card driven by causalThreads (fromFamily statConstellation -> toFamily bottleneckCanopy); hide entirely when no weak link. 11) Lens slot (absolute right/bottom): mount existing StatusStatBeadLens with selected node (default selectedLensDefaultStatId -> weakLinks[0] -> first lit), placeholder text when none; lens routeAction wired through onAction (single button only). 12) Wire open/close: add overlay state to StatusLivingStateObservatory (like activeDrawer) and make the compact instrument's 'Open Constellation' hint a real button that opens it; render the overlay as a portal/sibling so it floats above obsStage without shifting layout. Decorative-only: all atmosphere, mountains, grain, corners, motes, seals, tag, watermark, spine glyph, plaque rod, dashed connectors. Data-bound: nodes, edges, branch banners/counts, legend, bridge sockets, weak-link callout, plaque text, lens.

**Artifact regions:**
- **ovShell chrome (shared wrapper)** _(Full-screen overlay. Header band top (padding-left:74px): ovtitle 'Path-Adaptive Stat Meridian Constellation' + ovsub '28 named stats · all paths visible · current path: Martial'. Header right: .ovctx context-chips block (Next Goal / Main Bottleneck / Primary Route) + wax seal '束竅' (52px, rot -4) + .ovclose ✕. Left edge vertical tag '星脈' (.ovtag). Watermark '周天星圖' at right:54px top:110px (.gcol 52px). Decorative: grain overlay, 4 corner brackets (.gc tl/tr/bl/br), ink mountains strip bottom (height 260px z0), 5 floating motes.)_ — Shared overlay chrome wrapping the constellation body; title/sub/seal/tag/watermark are the only data-ish bits (sub embeds current path).
- **Constellation canvas (#ovConstSvg)** _(position:absolute; inset:0; z-index:1 — fills the whole overlay content area. Hosts constellationFull() SVG (viewBox 0 0 1456 740, preserveAspectRatio xMidYMid meet).)_ — The full star-map: painterly atmosphere (gold blade-swoosh behind Martial ~1085,470 & 1010,350; ink clouds behind Heaven ~330,290/250,170/430,400; ink ridges under Earth ~340,690/180,700; faint meditating-figure linework behind spine at cx728), 3 branch banner tablets (HEAVEN BRANCH @300,112; EARTH BRANCH @258,494; MARTIAL BRANCH @1156,240 each with EN title + sub tagline), UNIVERSAL SPINE card (rect @806,50 212x84, jade breathing glyph @990,98), vertical spine glow line (x728 y60->682), branch edges (C_EDGES per branch, lit/grey/jade coloring), dashed spine->hub connectors, 4 bridge sockets (@838,300 / 606,352 / 606,532 / 838,452) + 'bridge socket' label, 28 stat nodes (lit gold / spine jade / weak cinnabar / socket dashed / grey), node id text + full-name text, weak-link thread + 'CURRENT WEAK LINK' callout (@1066,118) + 'Connected to Current Bottleneck' card (@1284,432) toward the right edge.
- **Node State Legend card (.ovcard)** _(position:absolute; right:18px; top:8px; width:268px.)_ — Title 'Node State Legend'. Six rows each with a 20x20 swatch circle: Lit (goldRad) — current path, contributes now; Grey (paper-deep) — other path, unlocked but idle; Unlit socket (none, dashed) — future realm / path stat; Spine (jadeRad) — shared foundation, always on; plus Bridge socket (double dashed ring) — dormant dual-path possibility.
- **Constellation Explanation card (.ovcard)** _(position:absolute; left:74px; top:8px; width:312px.)_ — Title 'Constellation Explanation' + 4 explanatory lines (each node = a core aspect; Universal Spine always contributes; Path branches unlock per path; Bridge sockets allow future hybrid). A small wax seal '星訣' (32px) is pinned top-right of the card. Static prose — decorative/explanatory.
- **Plaque summary card (.ovcard)** _(position:absolute; left:18px; bottom:14px; width:344px; padding-left:30px. A brass hanging-scroll SVG rod (18x118) pinned left:5px top:8px with swaying tassels.)_ — Lines: 'Current Path <b>Martial</b>', 'Constellation Harmony <b>{Strained|Harmonious|Mastered}</b>' (color cinnabar if Strained else jade), 'Bridge Integration <b>{Dormant|Forming}</b>', 'Realm <b>{realm} {stage}</b>'. Footer dashed line: 'Universal <b>{count}</b> · always · Martial <b>{count}</b> · lit · Other paths <b>{28-U-path}</b> · idle / locked'.
- **Stat Bead Lens slot (#ovStatLens .ovcard)** _(position:absolute; right:18px; bottom:14px; width:300px; min-height:120px. Default centered placeholder 'Select a meridian bead to inspect'.)_ — On node click STATLENS() fills it: header 'Stat Bead Lens' + state label (Active/Foundation/Weak Link/Idle/Locked, colored); stat name (15px serif); 'Branch <b>{Universal|Heaven|Earth|Martial}</b> · Value <b>{x / 16}</b>'; a progress bar (width by state); rotated chops (CORE STAT if Universal, CURRENT PATH if on-path, WEAK LINK if weak); Effect / Source / Why-now copy block; two action buttons '.btn' (Open Training Hall) + '.btn jade' (Refine Weapon).

**Surface mapping:**
- [y] Overlay title 'Path-Adaptive Stat Meridian Constellation' ← `statConstellation.title ('Stat Meridian Constellation')` — Surface title differs slightly; use a static overlay title or compose from title + 'Path-Adaptive' prefix. subtitle field = '28 Named Stats - all paths visible'.
- [y] Overlay subtitle current-path suffix ← `statConstellation.currentPath (PathId|null) via STATUS_OBSERVATORY_STAT_PATH_LABELS[path].shortLabel` — meta.currentPath also available. Compose '28 named stats · all paths visible · current path: {shortLabel|None}'.
- [y] ovShell context-chips (Next Goal / Main Bottleneck / Primary Route) ← `lifeDecree.hero.nextMajorGoalLabel; lifeDecree.hero.mainBottleneckLabel; bottleneckCanopy.centralEdict / lifeDecree.hero.primaryAction.destinationLabel` — Same chips ObsPanel/decree already render. CRITICAL: artifact label text 'Primary Route' and 'Source:' are FORBIDDEN; relabel the route chip (e.g. 'Route') and use sourceLabel without a 'Source:' prefix.
- [y] 28 stat nodes (position, id number, name, state, click) ← `statConstellation.nodes[] (StatusObservatoryStatNodeSurface: id, displayName, shortLabel, nodeState, contributionState, weakLink, tone, currentRating, cap, capPct, routeAction, ariaLabel)` — Positions come from STATUS_OBSERVATORY_STAT_NODE_GEOMETRY (0-100 space) in presentation.ts — NOT from the surface and NOT the artifact's 1456x740 pixel coords. Reuse the compact instrument's geometry/branch-path constants for the full overlay; scale up the 100x100 atlas. Node numeric id labels (1-28) are artifact-only; surface ids are slugs (e.g. 'weapon_intent').
- [y] Node fill/stroke by state (lit gold / spine jade / weak cinnabar / socket dashed / grey) ← `statConstellation.nodes[].nodeState ('lit'|'foundation'|'lifeless'|'unlit_socket'|'future'|'inactive') + .weakLink + .contributionState` — Map nodeState->visual via existing data-node-state CSS in StatusLivingStateObservatory.scss. Artifact 'spine'=foundation, 'grey'=lifeless, 'socket'=unlit_socket/future/inactive, 'weak'=weakLink.
- [y] Branch edges (Universal/Heaven/Earth/Martial connector arcs) ← `statConstellation.branchPaths[] (StatusStatBranchSurface id/title/role/expectedCount/actualCount) + STATUS_OBSERVATORY_STAT_BRANCH_PATHS geometry` — Edge path 'd' geometry is presentation-side (C_EDGES in artifact, branch path strings in app). lit/grey coloring derives from currentPath===branchId. branchPaths gives per-branch counts/titles for banner tablets.
- [y] Branch banner tablets (HEAVEN/EARTH/MARTIAL + tagline) ← `statConstellation.branchPaths[].title/role + STATUS_OBSERVATORY_STAT_PATH_LABELS[branch].label/detail` — Taglines ('High Sky · Laws · Celestial Way' etc.) match STATUS_OBSERVATORY_STAT_PATH_LABELS detail. Banner positions are decorative geometry.
- [y] UNIVERSAL SPINE card + jade breathing glyph ← `statConstellation.branchPaths (id==='universal') + STATUS_OBSERVATORY_STAT_PATH_LABELS.universal.detail; legend entry for foundation` — Copy 'Shared foundation for all paths / always contribute' is static/labels. Breathing glyph decorative (motion-gated).
- [y] Node State Legend card (5-6 rows) ← `statConstellation.legend[] (StatusStatConstellationLegendEntry: id, label, detail, nodeState|'bridge_socket')` — Direct 1:1 — iterate legend; render swatch by entry.nodeState. Bridge-socket legend row keyed by 'bridge_socket'.
- [y] Bridge sockets on the map + 'bridge socket' label ← `statConstellation.bridgeSockets[] (StatusNamedStatBridgeSocket: id, label, detail, state, sourceStatIds) + STATUS_OBSERVATORY_STAT_BRIDGE_GEOMETRY` — Geometry/positions are presentation-side; surface supplies label/detail/state/source for tooltips and dormant/empty styling.
- [y] Weak-link thread + 'CURRENT WEAK LINK' callout ← `statConstellation.weakLinks[] (StatusObservatoryStatNodeSurface; .weakReason, .displayName, .tone) + nodes[].weakLink` — weakLinks[0] anchors the cinnabar thread to the bottleneck edge. weakReason fills 'This stat is limiting current growth' copy. Empty weakLinks (healthy/prestige states) => hide callout.
- [y] 'Connected to Current Bottleneck' card (links to canopy) ← `statConstellation.causalThreads[] (StatusCausalThreadSurface: fromFamily 'statConstellation' -> toFamily 'bottleneckCanopy', label, detail, tone)` — Use a causal thread targeting bottleneckCanopy for the 'See Bottleneck Talisman Fan' card; ties into shared useObservatorySelection highlight.
- [y] Plaque: Current Path / Harmony / Bridge Integration / Realm ← `statConstellation.currentPath; statConstellation.weakLinks.length (Harmony Strained/Stable); statConstellation.bridgeSockets[].state (Dormant/Forming); lifeDecree.hero.realmName + stageText (Realm)` — Compact instrument already computes Harmony='Strained' if weakLinks>0 else 'Stable' and Bridge='Dormant' if sockets exist. Artifact's 'Harmonious/Mastered/Forming' wording comes from visualState (meta.visualState) — derive a label map rather than hardcode.
- [y] Plaque footer counts (Universal N / path N / other N) ← `statConstellation.branchCounts {universal, heaven, earth, martial}` — Universal=branchCounts.universal; path count=branchCounts[currentPath]; other=28-universal-path. Direct.
- [y] Stat Bead Lens slot (default + selected) ← `statConstellation.selectedLensDefaultStatId + drawers.selectedStatLens (StatusObservatoryStatNodeSurface) / local selected node` — Reuse StatusStatBeadLens.tsx wholesale — it already renders name/path/state/value/contribution/effect/source/why-now/routeAction. Default selection via selectedLensDefaultStatId then weakLinks[0].
- [y] Lens state label (Active/Foundation/Weak Link/Idle/Locked) ← `nodes[].nodeState via STATUS_OBSERVATORY_STAT_LENS_STATE_LABELS + .weakLink` — Existing label maps in presentation.ts cover this.
- [y] Lens Value 'x / 16' ← `nodes[].currentRating + nodes[].cap (capPct)` — Artifact uses fixed mock values (11/16, 8/9, 0/16); real surface uses currentRating/cap. Progress bar width = capPct.
- [y] Lens CORE STAT / CURRENT PATH / WEAK LINK chops ← `nodes[].contributionState ('foundation'==Core) ; path===currentPath==Current Path ; .weakLink==Weak Link` — Derive chips from contributionState/path/weakLink; existing data-contribution-state styling available.
- [n] Lens action buttons (Open Training Hall / Refine Weapon) ← `nodes[].routeAction (StatusLedgerActionSurface) — single route` — Surface exposes ONE routeAction per node, not two distinct buttons. Render the single routeAction; the artifact's second '.btn jade' has no backing field (potential missing data).
- [n] Wax seals '束竅' / '星訣' / vertical tag '星脈' / watermark '周天星圖' ← `none — decorative constants` — Hardcoded glyphs/seals; decorative-only (mirror SealStamp/InkWaxSeal usage). Not data-bound.

**Missing data:** Per-node numeric ids 1-28 shown as the bead glyph label in the artifact — surface ids are slugs (e.g. 'weapon_intent'); the node short number is presentation-only. Render shortLabel/displayName instead of inventing numbers. | Second lens action button ('Refine Weapon' / '.btn jade') — surface gives a single nodes[].routeAction; no field for a secondary per-stat action. | Explicit 'Constellation Harmony' wording (Strained/Harmonious/Mastered/Forming): surface has no harmony string; must be derived from weakLinks.length + meta.visualState via a presentation label map. | Lens fixed value scale '/16' and progress percentages are artifact mock numbers; real values come from currentRating/cap/capPct (different denominators per stat). | Spine->branch-hub dashed connectors and the 4 fixed bridge-socket map positions (838,300 etc.) are artifact pixel geometry with no surface coordinates; must reuse presentation geometry constants. | ovShell context-chip 'Primary Route' value semantics: surface route info lives on lifeDecree.hero.primaryAction / bottleneckCanopy.centralEdict.primaryAction — no single 'route' string field; compose from primaryAction.destinationLabel.

**Fidelity risks:** FORBIDDEN LABELS: artifact ovShell renders 'Primary Route' and STATLENS renders 'Source:' — both are banned by contract. Relabel route chip to 'Route' and render sourceLabel without a 'Source:' prefix (StatusStatBeadLens already does this correctly — reuse it, do not re-port the raw artifact lens). | RAW HEX: artifact uses #8b3028 cinnabar, #0f6f5a jade, #a2712a/#6e4c16 gold-deep, #f3ead7 parchment, url(#goldRad)/url(#jadeRad)/url(#cinnDisc) and many rgba() literals. Repo enforces a zero-raw-hex audit — map all to var(--observatory-*) / existing InkObservatoryDefs gradient ids; do not introduce new hex. | CLS: overlay must be single-layout full-screen (no responsive grid reflow). Mount as a fixed/portal layer above obsStage so opening it does not shift the underlying grid; keep node geometry fixed (presentation constants) so the layoutStabilityContract / statusV2LayoutContract tests pass. | BUTTON-IN-BUTTON: each stat node and lens action must be a standalone <button>; do not nest a clickable node inside another interactive element, and do not place the lens route button inside a node button. The 'Open Constellation' trigger must be a real <button> not a span with onClick. | A11Y: dialog needs role='dialog' aria-modal='true', focus-trap + focus-restore (reuse useDialogFocusTrap), ESC to close, accessible close button label, and aria-live announce of selection changes (StatusStatBeadLens already has aria-live polite). Roving tabindex on nodes like the compact instrument. | VOCAB/CONTRACT: keep data-testid='status-stat-constellation' lineage and rootTestId; do not rename surface-driven text. Harmony wording must be derived (no hardcoded 'Strained' that contradicts state) to satisfy proofSurfaceNormalizationContract / statusRouteActionsContract. | MOTION: all atmosphere/glow/sway/motes must be gated by reduced-motion (useRitualMotion/useObservatoryMotion + data-animate) and grayscale-safe, matching W3-W8 gating, to pass the motion/a11y contracts. | DATA INVENTION: do not render fabricated numeric node ids (1-28) or a fixed '/16' scale or a phantom second lens button — use slugs/displayName, currentRating/cap, and the single routeAction so the surface stays the source of truth.

**Proposed SCSS classes:** statusConstellationOverlay, statusConstellationOverlay__canvas, statusConstellationOverlay__svg, statusConstellationOverlay__atmosphere, statusConstellationOverlay__banner, statusConstellationOverlay__banner--heaven, statusConstellationOverlay__banner--earth, statusConstellationOverlay__banner--martial, statusConstellationOverlay__spineCard, statusConstellationOverlay__spineGlow, statusConstellationOverlay__edge, statusConstellationOverlay__node, statusConstellationOverlay__nodeAura, statusConstellationOverlay__nodeBead, statusConstellationOverlay__bridgeSocket, statusConstellationOverlay__weakThread, statusConstellationOverlay__weakCallout, statusConstellationOverlay__bottleneckLink, statusConstellationOverlay__legend, statusConstellationOverlay__explanation, statusConstellationOverlay__plaque, statusConstellationOverlay__plaqueRod, statusConstellationOverlay__lensSlot, statusObservatoryOverlayShell, statusObservatoryOverlayShell__head, statusObservatoryOverlayShell__title, statusObservatoryOverlayShell__sub, statusObservatoryOverlayShell__ctx, statusObservatoryOverlayShell__seal, statusObservatoryOverlayShell__close, statusObservatoryOverlayShell__tag, statusObservatoryOverlayShell__watermark, statusObservatoryOverlayShell__mountains, statusObservatoryOverlayShell__grain, statusObservatoryOverlayShell__corner

---

## build → `StatusBuildPreparationOverlay.tsx`  (surface 9/24)

**Current app:** Today the "build" overlay is `StatusSupportDrawer` (kind=`buildPreparation`) in `src/ui/status/observatory/StatusObservatoryDrawers.tsx` — a generic right-docked `aside.statusObservatoryDrawer` (position:fixed, width min(620px), bronze border) that renders two flat `EvidenceRow` lists under `<h4>Build Floors</h4>` and `<h4>Reserve Jars</h4>` from `surface.drawers.buildPreparation.buildRows` / `reserveRows`. It is opened from the on-stage `StatusBuildPreparationScales` (brass balance) and `StatusReserveJars` (glass-jar shelf) instruments. There is no full-screen 3-column "Build & Preparation Inspector" matching the artifact, no readiness gauge, no loadout-coverage row, no consumption forecast, and no shared overlay-shell chrome (title/sub/context-chips/wax-seal/close/vertical-tag/mountains).

**Build plan:** 1. First extract a SHARED overlay-shell component (StatusObservatoryOverlayShell.tsx) since every overlay (astrolabe/build/etc.) wraps the same ovShell chrome. Props: title, sub, sealChars, tag, watermark, contextChips (fed from surface.lifeDecree.hero: nextMajorGoalLabel, mainBottleneckLabel+tone, primaryAction — NOT labelled 'Primary Route'), onClose, children. It renders role=dialog aria-modal=true, focus-trap via existing useDialogFocusTrap, a single fixed full-bleed container (position:fixed; inset:0; z-index above stage), the InkWaxSeal (reuse src/ui/ink/InkWaxSeal) for the 備械 seal, the vertical tag, the ink-mountains backdrop + corners + grain + motes as decorative pointer-events:none layers, and a ✕ close button. 2. Create StatusBuildPreparationOverlay.tsx consuming surface.buildPreparation (+ rawLedger.bestImprovements / milestone / meta.visualState for the readiness column). It wraps StatusObservatoryOverlayShell with title 'Build & Preparation Inspector', sub 'Floors, reserves, and readiness thresholds', sealChars/tag '備械', watermark '備械秤量'. 3. Layout the three regions to match the artifact using a single non-CLS layout — a 3-column flex/grid (430 / 560 / remainder) inside the shell content; do NOT use absolute px positioning that risks CLS — use a stable grid sized to the design canvas (per V7 single-layout no-CLS law). 4. Region A (.statusBuildPreparationOverlay__floors): map the deduped low-state+rows list (reuse StatusBuildPreparationScales.uniqueWeightRows/rowMark) to floor rows — status disc (data-mark ok/warn/bad), label, derived verdict word (display-only from tone), value as 'current', detail as description, and a route chip ONLY where FactRow.action exists (render its destinationLabel, wired to onAction — never the word 'Primary Route'/'Source:'). Add the readiness pill from derived stateStamp, the static Floor Doctrine callout, an optional Recent Floor Checks list bound to filtered recentChanges (or omit if decorative), and the decorative have/need balance SVG tilted via scaleTilt(). 5. Region B (.statusBuildPreparationOverlay__jars): map surface.buildPreparation.reserveJars to jar rows reusing jarFillFromText + the existing JAR_GLYPH map; show jar.label, value, derived state word, fill track (--jar-fill), and jar.sourceLabel as the source micro-line (NOT prefixed 'Source:'). Mark Target and per-jar restock route as decorative/absent (no surface field) — render only what the surface provides. Render the static Pouch Doctrine prose. The Trial Consumption Forecast and Apothecary Shelf SVG are decorative-only (no surface data) — either omit or render with a 'illustrative' aria treatment; do NOT fabricate numbers as if data-bound. 6. Region C (.statusBuildPreparationOverlay__readiness): gauge shows milestone.readinessLabel (label, not a fabricated %); prose synthesised from top lowStateRow + hero.mainBottleneckDetail; Loadout Coverage and Readiness Components bars + gate-threshold scalars are MISSING from the surface — render as decorative placeholders or omit (flag clearly), never invent the 18/55-style numbers as truth. The 'Last gate attempt' callout is gated on meta.visualState==='postFailure'. The Primary Routes button column is DATA-BOUND to rawLedger.bestImprovements.primary + rows (StatusLedgerActionSurface[]), each a single real <button> wired to onAction with action.label. 7. Wiring: mount the overlay from StatusLivingStateObservatory alongside StatusObservatoryDrawers (or replace the buildPreparation drawer branch), opened by the existing onOpenDrawer({kind:'buildPreparation'}) path from the on-stage Scales/Jars instruments; keep data-testid='status-ledger-build-preparation' present so statusV2LayoutContract still finds it. 8. SCSS: add a new block in StatusLivingStateObservatory.scss using only var(--observatory-*) tokens — map artifact hex #8b3028→--observatory-cinnabar, #0f6f5a→--observatory-jade, #a2712a→--observatory-gold-deep (fallback --observatory-gold), #f3ead7→--observatory-paper, deeps via --observatory-bronze; reuse rgba helpers already present. Decorative-only: mountains/grain/motes/corners/balance SVG/shelf SVG/gauge ring/loadout glyphs. Data-bound: floor rows, readiness pill, jar rows, route buttons, context chips, attempt callout.

**Artifact regions:**
- **Overlay shell — header (ovShell)** _(top band, full width; ovhead padding-left:74px)_ — Title 'Build & Preparation Inspector', sub 'Floors, reserves, and readiness thresholds'; right side: context-chips (ovctx) Next Goal / Main Bottleneck / Primary Route from STATE.decree, wax seal 備械 (52px), ✕ close (ovclose → closeOverlay).
- **Overlay shell — vertical tag (ovtag)** _(far-left vertical strip)_ — Stacked characters 備械 (tag arg, each char on its own line).
- **Overlay shell — watermark (gcol)** _(absolute right:54px top:110px font-size:52px)_ — Faint giant glyph 備械秤量 (wm arg).
- **Overlay shell — atmosphere** _(behind content; mountains bottom height:260px z-index:0; 5 drifting motes; grain filter; 4 gold corners (gc tl/tr/bl/br))_ — Decorative only: inkMountains(1856,260), .mote particles, .grain SVG, corner brackets.
- **Region A — Build Floors scale panel (.ovcard)** _(position:absolute left:18px top:14px width:430px)_ — Sub-heading 'Build Floors — Current vs Required' (cinnabar). 6 floor rows (one per st.scales.rows): status disc ([y] jade / ! gold / ✕ cinnabar) + bold label + verdict word ('Met'/'Marginal'/'Below floor'); second line 'Current <x> · Required <y>' + route chip '<Route> ›' (gold-deep); third line micro-description. Rows from a hardcoded reqMap keyed by row label (Path Alignment / Empty Slots / Mastery Floor / Rank Floor / Rune Floor / Policy Fit).
- **Region A — Build Readiness verdict pill** _(inside scale panel, margin-top:10px, centered, cinnabar-tint bg)_ — 'Build Readiness: ' + st.scales.verdict (serif 13.5px cinnabar).
- **Region A — Floor Doctrine callout** _(inside scale panel, dashed border box)_ — Static prose: 'Floor Doctrine. Floors are minimums the gate checks. Any red floor blocks readiness regardless of totals.'
- **Region A — Recent Floor Checks list** _(inside scale panel, sub-heading 'Recent Floor Checks')_ — 3 hardcoded history rows: 'Gate trial — Stage 9 / Failed', 'Pre-check — manual / 3 floors red', 'Loadout review / Policy loose' with tone colours.
- **Region A — have/need balance SVG** _(inside scale panel, 150x92 svg centered)_ — Decorative brass balance beam with 'have' pan (jade) and heavier 'need' pan (cinnabar, .twk wobble class).
- **Region B — Reserve Jars detail panel (.ovcard)** _(position:absolute left:462px width:560px top:14px)_ — Sub-heading 'Reserve Jars — Detail' (jade). One row per st.jars (j): small jar SVG tinted j.col (.twk if j.bad) + jar name (hardcoded jarNames[i]) + 150px progress track filled to j.level% in j.col + value j.val (cinnabar if j.bad); second line 'Target: <jarTargets[i]>' + 'Restock via <jarRoutes[i]> ›'.
- **Region B — Pouch Doctrine + Restock All** _(inside jar panel, margin-top:11px flex row)_ — Static prose 'Pouch Doctrine. Reserves drain during trials; the gate assumes a full pouch and punishes shortfalls mid-attempt.' + jade button 'Restock All'.
- **Region B — Trial Consumption Forecast list** _(inside jar panel, sub-heading 'Trial Consumption Forecast — per gate attempt')_ — 4 hardcoded rows: 'Healing charges −12 → 0 remaining', 'Medicine doses −8 → 4 short', 'Spirit Stones −150 → covered', 'Merit (entry writ) −25 → covered' (tone-coloured outcomes).
- **Region B — Apothecary Shelf SVG** _(inside jar panel, 360x86 svg)_ — Decorative shelf with 5 jars filled to hardcoded fractions [0.3,0.85,0.18,0.5,0.16]; label 'APOTHECARY SHELF'.
- **Region C — Gate Readiness Reading panel (.ovcard)** _(position:absolute left:1036px right:18px top:14px)_ — Sub-heading 'Gate Readiness Reading' (gold-deep). Radial gauge SVG showing hardcoded '34% ready' (cinnabar arc) + prose 'Preparation is the primary blocker. Healing and weapon floor are unmet…'.
- **Region C — Loadout Coverage row** _(inside readiness panel, sub-heading 'Loadout Coverage', flex row of 6)_ — 6 hardcoded SLOTS: 兵 Weapon/Set, 甲 Armor/Set, 符 Rune/Empty, 丹 Pill/Empty, 策 Policy/Loose, 飾 Charm/Empty — each a circle (jade fill if ok, gold if warn, dashed outline if empty) + Kai-ti glyph + slot name + state word.
- **Region C — Last gate attempt callout** _(inside readiness panel, cinnabar-tint box)_ — Static 'Last gate attempt: Failed — preparation below floor.'
- **Region C — Readiness Components bars** _(inside readiness panel, sub-heading 'Readiness Components')_ — 4 hardcoded progress bars: Build floors 18/55, Reserves 9/25, Loadout 12/15, Stability 0/15; plus 'Gate threshold 75 · current total 39'.
- **Region C — Primary Routes button column** _(inside readiness panel, sub-heading 'Primary Routes')_ — 3 stacked buttons: 'Restock Healing' (jade), 'Forge Weapon +5', 'Open Apothecary'.

**Surface mapping:**
- [y] Overlay title 'Build & Preparation Inspector' ← `buildPreparation.title ('Build & Preparation') + static suffix; or scales.title (StatusBuildPreparationScaleSurface.title)` — buildPreparation.title is 'Build & Preparation'; the ' Inspector' suffix and the sub-line are overlay copy, not surface fields.
- [y] Context chips: Next Goal ← `rawLedger.hero.nextMajorGoalLabel (via lifeDecree.hero.nextMajorGoalLabel)` — Shell-level; same source the on-stage Life Decree uses. Reuse a shared overlay shell fed from surface.lifeDecree.hero.
- [y] Context chips: Main Bottleneck ← `rawLedger.hero.mainBottleneckLabel / mainBottleneckDetail` — Available; colour by tone, not raw hex.
- [y] Context chips: Primary Route (chip) ← `rawLedger.hero.primaryAction (StatusLedgerActionSurface.label/destinationLabel)` — Available BUT the literal heading 'Primary Route' is a forbidden copy token (see daoOmenCopyGuard). Render the route value without that label, or use a neutral header like 'Route'.
- [y] Region A floor rows (label / status disc / verdict) ← `buildPreparation.scales.lowStateRows[] + buildPreparation.rows[] (StatusLedgerFactRow: label, value, detail, tone); existing StatusBuildPreparationScales.uniqueWeightRows() dedup logic` — Rows exist as FactRows with tone → map tone to ok/warn/bad disc (existing rowMark()). NO explicit 'verdict word' per row — derive from tone (Met/Marginal/Below floor) as display-only.
- [n] Region A 'Current x · Required y' + micro-description + route chip ← `FactRow.value (current), FactRow.detail (description), FactRow.action.destinationLabel (route)` — Artifact's reqMap hardcodes Current/Required pairs and route names per label. Surface FactRow has a single value+detail, no separate current-vs-required pair and no per-row 'Required' threshold. PARTIAL — show value as 'current', detail as description; 'Required' threshold is missing.
- [n] Region A 'Build Readiness: <verdict>' pill ← `buildPreparation.scales.fulcrumLabel OR scales.build.headline / scales.preparation.headline` — No dedicated scalar 'verdict' string. scales.fulcrumLabel is the closest balance-state label; existing instrument derives a stateStamp() (Balanced/Needs support/Floor broken) from tones. Use that derived stamp, not a literal 'verdict'.
- [n] Region A Floor Doctrine prose ← `(none — static educational copy)` — Decorative/educational; ship as static overlay copy. Not data-bound.
- [n] Region A Recent Floor Checks list ← `drawers.recentChanges.rows[] (StatusLedgerFactRow) filtered to build/prep family` — Surface exposes recentChanges globally but not a build-floor-specific history. Artifact's 3 rows are hardcoded. Either bind to filtered recentChanges or treat as decorative.
- [y] Region A have/need balance SVG ← `buildPreparation.scales (tilt from lowStateRows danger count — existing scaleTilt())` — Decorative; tilt direction can be driven by existing scaleTilt()/heavyPan logic. Display-only.
- [y] Region B jar rows (name / fill / value / tone) ← `buildPreparation.reserveJars[] (StatusReserveJarSurface: id, label, value, detail, tone, sourceLabel)` — Core data-bound list. Fill% via existing jarFillFromText(value, tone); jar tint via tone→token; bad = tone 'danger'. jarNames/jarTargets/jarRoutes in artifact are hardcoded — prefer jar.label/jar.detail/jar.sourceLabel instead.
- [n] Region B 'Target: <x>' per jar ← `reserveJars[].detail (free-text)` — No structured target value. detail may contain it as prose. Artifact hardcodes ('10,000','5,000','80%'…). PARTIAL — surface only via detail text.
- [n] Region B 'Restock via <Route> ›' per jar ← `reserveJars[] has NO routeAction (StatusReserveJarSurface lacks an action field)` — Reserve jars carry sourceLabel but no per-jar StatusLedgerActionSurface. Routing per jar is not available; would need to map to the shared drawer or omit the route chip.
- [n] Region B Pouch Doctrine prose ← `(none — static)` — Decorative educational copy.
- [n] Region B 'Restock All' button ← `no aggregate restock action on buildPreparation surface` — No surface action for 'restock all'. Could map to rawLedger.bestImprovements.primary or safetyNet.action, but no exact field. Likely decorative or wire to drawer.
- [n] Region B Trial Consumption Forecast list ← `(none)` — Entirely hardcoded in artifact (charges −12, doses −8, etc.). No consumption-forecast surface exists. MISSING.
- [n] Region B Apothecary Shelf SVG ← `(decorative; fractions hardcoded)` — Decorative. Could echo reserveJars fills but artifact uses fixed values.
- [n] Region C readiness gauge '34% ready' ← `rawLedger.milestone.readinessLabel / milestone.tone` — milestone.readinessLabel is a label string (e.g. 'Not ready'), not a percent. No numeric readiness % on the build/prep surface. PARTIAL — show label, not a precise arc %.
- [y] Region C readiness prose ← `buildPreparation.scales.lowStateRows[0] / rawLedger.hero.mainBottleneckDetail` — Can summarise from top low-state row + bottleneck detail. Display-only synthesis.
- [n] Region C Loadout Coverage (6 slots) ← `(none structured)` — Artifact's 兵/甲/符/丹/策/飾 slots with set/loose/empty states are hardcoded. No equipment-slot coverage array on the surface. MISSING — closest is FactRows in buildPreparation.rows mentioning equipment, but not a 6-slot model.
- [n] Region C 'Last gate attempt' callout ← `meta.visualState ('postFailure') / rawLedger.recentChanges` — visualState='postFailure' signals a failed attempt but the exact 'preparation below floor' phrasing is not a field. PARTIAL — gate on visualState, copy is static.
- [n] Region C Readiness Components bars (4) ← `(none — values 18/55, 9/25, 12/15, 0/15 hardcoded)` — No readiness-component breakdown surface. MISSING.
- [n] Region C 'Gate threshold 75 · current total 39' ← `(none)` — No numeric gate threshold/total on surface. MISSING.
- [y] Region C Primary Routes buttons (Restock Healing / Forge Weapon +5 / Open Apothecary) ← `rawLedger.bestImprovements.primary + bestImprovements.rows[] (StatusLedgerActionSurface[]) and/or buildPreparation FactRow.action entries` — Real route actions exist in bestImprovements and on individual FactRows (action.label/destinationLabel/target). Bind the button column to these instead of hardcoded labels; labels like 'Open Apothecary'/'Forge Weapon' are allowed per copy-guard allowlist.

**Missing data:** Per-floor Current-vs-Required threshold pair (artifact reqMap hardcodes e.g. 'Avg mastery 0.0 vs ≥ 4.0'). Surface FactRow gives only a single value+detail, no required-threshold field. | Per-jar structured Target value (artifact: 10,000 / 5,000 / 80% …). Only free-text reserveJars[].detail exists. | Per-jar restock route action — StatusReserveJarSurface has sourceLabel but no StatusLedgerActionSurface. | 'Restock All' aggregate action — no field on buildPreparation surface. | Trial Consumption Forecast (per-gate-attempt drain table: healing −12, doses −8, stones −150, merit −25). Entirely absent from the surface. | Numeric readiness percentage for the gauge (artifact '34%'). Only milestone.readinessLabel (string) exists. | Loadout Coverage 6-slot model (Weapon/Armor/Rune/Pill/Policy/Charm with set/loose/empty). No equipment-slot array on the surface. | Readiness Components breakdown (Build floors 18/55, Reserves 9/25, Loadout 12/15, Stability 0/15) and the 'Gate threshold 75 · total 39' scalars. No such decomposition exists. | Per-row build-floor history (Recent Floor Checks). Only a global recentChanges list exists, not build-scoped. | Build Readiness verdict scalar — no dedicated 'verdict' string; must derive from tones.

**Fidelity risks:** Forbidden copy: the shell's third context chip is literally 'Primary Route' in the artifact — that string is banned by tests/contracts/daoOmenCopyGuard.test.ts (and route-led/Best Next Action). Do NOT render the heading 'Primary Route'; render the route value under a neutral/empty header. Likewise never prefix sources with 'Source:' — emit the bare sourceLabel (existing factSourceText pattern). | Fabricated data: the artifact's Region C (34% gauge, Loadout slots, Readiness Components 18/55…, Gate threshold 75/total 39) and Region B Trial Consumption Forecast are hardcoded with NO backing surface field. Rendering them as if data-bound would be dishonest (status honesty contracts e.g. prestigeScreenHonesty / readinessDriftGuard ethos). Mark them decorative/placeholder or omit — never invent numbers. | CLS: per the V7 re-ground single-layout no-CLS law (memory: observatory uses one layout, no card grid swap, locked renderer anchors). The artifact uses absolute px positions; do NOT animate or reflow region sizes on data change. Use a stable grid and reserve space for optional rows (e.g. route chips) so presence/absence of a FactRow.action doesn't shift layout. | button-in-button: route chips inside floor rows and the Primary Routes column must be standalone <button>s — do not nest a clickable route button inside another clickable row/button (the on-stage Scales already makes each weight a button that opens the drawer; in the overlay, a floor ROW that is itself a button must not also contain a route button). Keep one interactive element per cell. | Raw-hex audit: artifact uses #8b3028/#0f6f5a/#a2712a/#f3ead7 and many literal hex in SVGs. The SCSS no-raw-hex gate requires var(--observatory-*) tokens; map all colours. Inline SVG fills that must stay (decorative gradients like url(#brass), url(#goldRad)) should reference shared <defs> already used by Scales/Jars rather than new hex. | Testid/contract preservation: statusV2LayoutContract expects 'status-ledger-build-preparation' to exist; ensure the overlay (or the retained on-stage instrument) keeps that testid and the data-s7-instrument anchors so layoutStability/statusV2 contracts and the no-loss family 'buildPreparation' mapping stay green. | a11y: full-screen overlay must be role=dialog, aria-modal=true, focus-trapped and focus-restored (reuse useDialogFocusTrap), with an aria-label and an accessible ✕ close (aria-label 'Close …'); decorative mountains/grain/motes/SVGs must be aria-hidden and pointer-events:none. Provide reduced-motion gating for motes/balance wobble (.twk) consistent with existing observatory motion hooks. | Glyph/font: Kai-ti slot glyphs (兵甲符丹策飾) and seal chars rely on --kai/--serif font vars; ensure fallback so missing CJK fonts don't break layout. These slots are decorative since no loadout surface exists.

**Proposed SCSS classes:** statusBuildPreparationOverlay, statusBuildPreparationOverlay__shell, statusBuildPreparationOverlay__floors, statusBuildPreparationOverlay__floorRow, statusBuildPreparationOverlay__floorMark, statusBuildPreparationOverlay__floorVerdict, statusBuildPreparationOverlay__floorMeta, statusBuildPreparationOverlay__readinessPill, statusBuildPreparationOverlay__doctrine, statusBuildPreparationOverlay__historyRow, statusBuildPreparationOverlay__balanceSvg, statusBuildPreparationOverlay__jars, statusBuildPreparationOverlay__jarRow, statusBuildPreparationOverlay__jarGlyph, statusBuildPreparationOverlay__jarTrack, statusBuildPreparationOverlay__jarFill, statusBuildPreparationOverlay__forecastRow, statusBuildPreparationOverlay__shelfSvg, statusBuildPreparationOverlay__readiness, statusBuildPreparationOverlay__gauge, statusBuildPreparationOverlay__loadout, statusBuildPreparationOverlay__loadoutSlot, statusBuildPreparationOverlay__attemptCallout, statusBuildPreparationOverlay__components, statusBuildPreparationOverlay__componentBar, statusBuildPreparationOverlay__routes, statusBuildPreparationOverlay__routeButton, statusObservatoryOverlayShell, statusObservatoryOverlayShell__head, statusObservatoryOverlayShell__title, statusObservatoryOverlayShell__sub, statusObservatoryOverlayShell__ctx, statusObservatoryOverlayShell__tag, statusObservatoryOverlayShell__seal, statusObservatoryOverlayShell__close, statusObservatoryOverlayShell__content, statusObservatoryOverlayShell__mountains

---

## work → `StatusWorkWheelOverlay.tsx`  (surface 6/13)

**Current app:** Today this overlay is not a bespoke screen at all: the in-grid instrument is StatusCurrentWorkTimeWheel.tsx (a 212x212 brass dial + 4 corner chips), and clicking its hub/chips opens the GENERIC StatusSupportDrawer (StatusObservatoryDrawers.tsx, drawer.kind==='currentWork'). That generic aside renders an "Expanded Ledger Detail / Current Work Ledger" header plus two plain stacked sections - "Wheel Spokes" (WorkSpokeRow over surface.drawers.currentWork.spokes) and "Current Work Rows" (EvidenceRow over surface.drawers.currentWork.rows) - with no activity wheel, no offline/forecast card, no session-timeline, no work-log, and none of the artifact's absolute 4-region layout or xianxia chrome.

**Build plan:** 1) First build a SHARED overlay-shell component (StatusObservatoryOverlayShell.tsx) reused by all detail overlays, porting ovShell: props { title, sub, sealChars, tag, watermark, context (chips), children }. It renders grain, 4 corners, vertical tag, padded-left header (title + sub on left; context chips + waxSeal + close ✕ on right), the ink-mountains band, watermark, motes, and a relative .statusObservatoryOverlayShell__content host. Mount as a role='dialog' aria-modal full-screen modal and reuse useDialogFocusTrap (already used by the existing drawers) for focus trap/restore + Escape->onClose; close ✕ is the only header button. 2) Build StatusWorkWheelOverlay.tsx consuming workWheel (and widen props to also accept drawers.recentChanges + optionally vitalsRibbon so Regions C/D are data-bound rather than fixture). Wrap children in the shell with title=workWheel.title, sealChars='行功', tag='行功', watermark='行功時輪'. 3) Region A: reuse the EXISTING StatusCurrentWorkTimeWheel dial SVG geometry (212 viewBox, #brass/#jadeRad/#soft paint servers from the shared <InkObservatoryDefs/>) at the larger 380px slot, hub fed by foregroundRead(workWheel), satellites mapped from the first 4 spokes (label+value+idle styling via toneToSegmentState). Keep the existing spin/breath motion gated by useRitualMotion/reduced-motion. 4) Region B: iterate ALL workWheel.spokes (plus a leading foreground row) into __systemRow entries: dot (data-tone), name, value (idle styling), consequence=spoke.detail; .twk twinkle gated on non-idle + motion. 5) Region C: render workWheel.rows generically into __offlineRow label/value pairs; source qi/s from vitalsRibbon if threaded else '—'; render queue slots only if a queue figure exists in rows (otherwise omit) - never hardcode 2/3 or +229.5K. 6) Region D: render drawers.recentChanges.rows into __logRow (dot data-tone, danger=bad); Session Timeline / Earlier-This-Life / System-Ownership seals are DECORATIVE chrome (mark aria-hidden, do not present as live data) unless a typed source is threaded; footnote is a static literal. 7) SCSS: single absolute-position 4-region layout matching the artifact's exact left/top/width/height numbers inside the shell content; one fixed layout (no responsive reflow) to honor the no-CLS rule. Map raw artifact hex to tokens: #0f6f5a->var(--observatory-jade), #8b3028->var(--observatory-cinnabar), #a2712a->var(--observatory-gold-deep, var(--observatory-gold)), #f3ead7->var(--observatory-paper), ink-70/ink-45->var(--observatory-ink)/var(--observatory-muted), 行/修/空 via var(--kai). Azure/brown system accents derive from data-tone (info/muted) - no new hex. 8) Wire: add a 'work' overlay route in StatusLivingStateObservatory so the wheel hub/chips open this overlay (replacing the generic currentWork drawer path) and pass onAction through.

**Artifact regions:**
- **Overlay shell chrome (ovShell)** _(Full-screen modal: header padded left 74px (title 'Current Work Time Wheel' + sub 'Foreground activity, queues, and idle systems'); right cluster = context-chips (.ovctx Next Goal / Main Bottleneck / Primary Route) + wax seal '行功' (52px) + close ✕; left vertical tag '行功' (.ovtag); watermark '行功時輪' top-right ~54px/110px 52px; grain overlay; 4 gold corners (.gc tl/tr/bl/br); ink-mountains band bottom 260px z0 opacity .9; 5 drifting motes.)_ — Decorative + cross-cutting decree context. Title/sub/seal/tag/watermark are static literals for this overlay. Context chips read STATE decree (goal/bottleneck/route).
- **Region A - Activity Wheel card** _(position:absolute; left:18px; top:14px; width:470px; column flex, centered.)_ — Sub-label 'Activity Wheel'; a 380x380 wheel() SVG (id ovWorkWheel): brass outer dashed ring (64s spin) + brass rim + 24 reverse-spinning ticks (110s) + jade breathing core showing 行 / 'FOREGROUND' / foreground value (a.fg); 4 brass satellites at 45/135/225/315deg = Combat / Exped / Prof / Bounty each with label+value (jade if active, ink-45 if None/Idle). Caption: 'Hub shows the foreground occupation; satellites show queued systems. Brass ring turns while any system works.'
- **Region B - Active & Queued Systems card** _(position:absolute; left:500px; top:14px; width:430px; height:330px.)_ — Jade sub-label 'Active & Queued Systems'. 5 dashed-divider rows (one per seg entry): Foreground / Combat / Bounty / Expeditions / Professions. Each row = colored status dot (jade/cinnabar/gold/azure/brown; .twk twinkle class when NOT idle) + bold system name (left) + value (right, jade if active else ink-45) + an 8.8px sub-line consequence ('Idle - no yield accruing', 'Suppressed after failed trial', 'No contract active', 'No party deployed', 'Workshop unmanned').
- **Region C - Offline & Yield card** _(position:absolute; left:500px; top:360px; width:430px.)_ — gold-deep sub 'Offline & Yield'; 5 label/value rows: Last offline duration (—), Qi gained offline (—), Foreground yield / s (st.vitals.qis), Foreground since ('Idle after failure'), Queue slots free ('2 / 3'). Sub 'Queue Slots' + 3 small 34x34 SVG circles (filled=jadeRad+修, empty=dashed+空) + caption '1 queued · 2 open. Everything except combat progresses offline.'. Sub 'Offline Forecast' + 3 rows: Projected Qi in 1h (+229.5K), in 8h (+1.83M), Combat while offline ('Paused - by law').
- **Region D - Recent Work Log card** _(position:absolute; left:942px; right:18px; top:14px (right rail).)_ — cinnabar sub 'Recent Work Log' + one dot-row per st.ledgers.changes entry (cinnabar dot if c.bad else gold, c.t text). Sub 'Session Timeline' + a 14px stacked horizontal bar (Cultivate 38% gold / Gate trial 8% cinnabar with .thrP pulse / Idle 54% grey) with % legend beneath. Sub 'Earlier This Life' + 4 static dot-rows (Reached Qi Condensation Stage 9 / Learned Iron Palm Rank 1 / First gate writ purchased / Joined Pinewind Hamlet). Sub 'System Ownership' + 5 seal medallions (功 Cultivation / 兵 Combat / 賞 Bounty / 旅 Expedition / 工 Profession). Footer: 'The work wheel records foreground activity, queued systems, and idle yield. Switch foreground in Cultivation or Training Hall.'

**Surface mapping:**
- [y] Region A wheel hub - foreground value (a.fg / 行 FOREGROUND) ← `workWheel.foreground (StatusLedgerFactRow: .value ?? .detail, .label, .tone)` — foreground is StatusLedgerFactRow | null. Reuse foregroundRead() fallback from StatusCurrentWorkTimeWheel when null. value->hub big text, label->'FOREGROUND' caption.
- [y] Region A satellites (Combat / Exped / Prof / Bounty) + Region B 5 system rows ← `workWheel.spokes[] (StatusWorkWheelSpokeSurface: id,label,value,detail,tone,route)` — Spokes are the typed equivalent of the artifact's hardcoded seg[] (which assumes exactly Foreground/Combat/Bounty/Expeditions/Professions). Map spoke.value->right value, spoke.detail->consequence sub-line, spoke.tone->dot color + idle/active styling, spoke.route->optional routing. DO NOT hardcode 5 systems; iterate spokes (count is data-driven). Foreground row in Region B = workWheel.foreground.
- [y] Region A/B per-system status dot color + .twk active twinkle ← `workWheel.spokes[].tone (StatusLedgerTone) and foreground.tone` — Artifact uses raw per-system hex (#0f6f5a jade / #8b3028 cinnabar / #a2712a gold / #2a668d azure / #8d642d brown). No azure/brown observatory token exists - derive dot color from tone via data-tone attr + CSS (jade/cinnabar/gold/info/muted). Twinkle gate = NOT (value idle/None) - reuse toneToSegmentState() idle detection.
- [y] Region B/A 'value (jade if active else ink-45)' ← `workWheel.spokes[].value + computed idle state` — Idle when value is null/'None'/'Idle'; reuse the existing toneToSegmentState() heuristic for active vs idle styling.
- [n] Region C 'Foreground yield / s' ← `vitalsRibbon.metrics[] / vitalsRibbon.seals[] (NOT workWheel)` — Artifact reads st.vitals.qis - a vitals figure, not exposed on workWheel. The overlay receives only workWheel + drawers.currentWork per the prop contract. Either thread vitalsRibbon in, or source the qi/s figure from workWheel.rows if present (search rows by label). Treat as conditionally available; render '—' when absent.
- [n] Region C label/value rows (Last offline duration, Qi gained offline, Foreground since, Queue slots free) + Offline Forecast (1h/8h/combat) ← `workWheel.rows[] (StatusLedgerFactRow) / drawers.currentWork.rows[]` — workWheel.rows is the only typed bag of extra current-work facts. The artifact's specific offline/forecast/queue figures (+229.5K, +1.83M, '2 / 3', 'Idle after failure', 'Paused - by law') are NOT modeled as named fields. Render whatever rows exist generically; the named forecast numbers are fixture-only and must NOT be hardcoded as truth.
- [n] Region C Queue Slots SVG (修 filled / 空 empty, '1 queued · 2 open') ← `(none) - artifact literal [1,0,0]` — No queue-slot count/capacity field on the surface. Decorative-only unless a queue figure is found in workWheel.rows by label; otherwise omit or render from rows count - do NOT fabricate 2/3.
- [n] Region D 'Recent Work Log' dot-rows ← `ledgerRail.recentChanges.rows[] / drawers.recentChanges.rows[] (StatusLedgerFactRow; tone='danger' => artifact c.bad)` — Artifact reads st.ledgers.changes. The overlay prop contract (workWheel + drawers.currentWork) does NOT include recentChanges. To populate Region D authentically, widen the overlay props to also accept drawers.recentChanges (already exists on surface). c.bad maps to row.tone==='danger'.
- [n] Region D Session Timeline bar (Cultivate 38 / Gate trial 8 / Idle 54) ← `(none)` — Hardcoded tl[] in artifact. No session time-split exists on the surface. Decorative-only / fixture; do not present as live data.
- [n] Region D 'Earlier This Life' 4 rows ← `(none) / partially lifeDecree.milestone` — Artifact uses 4 string literals. Closest typed source is lifeDecree.milestone (not in this overlay's props). Treat as decorative unless milestone history is threaded; do not hardcode the 4 literals as truth.
- [y] Region D 'System Ownership' 5 seals (功兵賞旅工) ← `workWheel.spokes[] labels (decorative glyph map)` — The 5 CJK ownership chops are decorative; glyph<->system can be aligned to spoke labels, but the glyph set itself is static chrome.
- [n] Shell context chips (Next Goal / Main Bottleneck / Primary Route) ← `bottleneckCanopy.centralEdict / lifeDecree (decree) - cross-cutting` — Artifact reads STATE.decree. Not in this overlay's workWheel props. If chips are kept, source them from a shared shell fed by bottleneckCanopy.centralEdict (label/detail/primaryAction) - and the chip label 'Primary Route' is FORBIDDEN vocabulary (see fidelityRisks); relabel to a route/path-neutral term.
- [y] Shell title / sub / seal '行功' / tag '行功' / watermark '行功時輪' ← `workWheel.title ('Current Work Wheel') for the visible title; CJK chrome static` — workWheel.title supplies the English title. The artifact's literal 'Current Work Time Wheel' differs slightly; prefer surface.title. CJK seal/tag/watermark are decorative literals.

**Missing data:** Foreground yield-per-second (st.vitals.qis) - lives on vitalsRibbon, not on workWheel; overlay prop contract does not currently receive it. | Offline metrics: last offline duration, Qi gained offline, foreground-since reason - no named fields; only generic workWheel.rows. | Offline forecast figures (projected Qi in 1h '+229.5K' / 8h '+1.83M', 'Combat while offline - Paused by law') - fixture literals, no typed source. | Queue-slot model: filled/open count and capacity ('1 queued · 2 open', '2 / 3 free', 修/空 slots) - no queue field on the surface. | Recent Work Log entries with a bad/good flag - exists as ledgerRail.recentChanges.rows (tone) but is NOT in this overlay's prop slice (workWheel + drawers.currentWork). | Session Timeline split (Cultivate/Gate trial/Idle %) - no time-allocation field anywhere. | 'Earlier This Life' history list - closest is lifeDecree.milestone, not threaded into this overlay. | Per-system consequence strings in the artifact ('Suppressed after failed trial', etc.) are partly redundant with spoke.detail but the artifact's exact wording is fixture; use spoke.detail as the truth. | Per-system accent hues #2a668d (expedition azure) and #8d642d (profession brown) have no observatory token - must be derived from tone, not added as raw hex.

**Fidelity risks:** FORBIDDEN VOCAB: the artifact's shell context chip literally renders 'Primary Route' and 'Source:'-style source captions appear throughout; releaseVocabularyAudit/statusLedgerRowBudget contracts ban 'Primary Route' and 'Source:'. The shared shell must NOT emit 'Primary Route' - relabel the route chip (e.g. 'Recommended Path' / route-neutral) and use the existing factSourceText() pattern (no 'Source:' prefix). | CLS: artifact uses absolute px positioning per region. Keep ONE fixed single-layout (no breakpoints/reflow) per the no-CLS decision in MEMORY (V7 re-ground); avoid late-loading wheel SVG causing reflow - reserve the 380px dial box. | BUTTON-IN-BUTTON: do not nest the spoke route button inside the wheel hub button or a row that is itself a button; the existing wheel already exposes a hub button + chip buttons - in the overlay, make the close ✕ and any route buttons siblings, never nested. The Region B rows should be non-interactive (or a single button each), not buttons containing buttons. | RAW-HEX AUDIT: the artifact is full of raw hex (#0f6f5a,#8b3028,#a2712a,#f3ead7,#2a668d,#8d642d, rgba ink). The repo enforces zero raw hex in observatory SCSS - every color must route through var(--observatory-*) tokens (or gate-allowed rgba); azure/brown have no token so must come from data-tone, not literal hex. | FABRICATED DATA: forecast numbers (+229.5K/+1.83M), queue '2/3', timeline %s, and 'Earlier This Life' items are fixture literals with no typed source - presenting them as live values is a truthfulness violation; render '—'/omit or mark decorative. | A11Y: full-screen modal needs role='dialog' + aria-modal + labelled title, focus trap + restore (reuse useDialogFocusTrap) and Escape-to-close; decorative SVG (wheel, seals, timeline, mountains, motes) must be aria-hidden; spin/breath/twinkle/.thrP pulse must be gated by reduced-motion (useRitualMotion). | PROP-CONTRACT SCOPE: Regions C/D need vitalsRibbon + drawers.recentChanges which are NOT in the current currentWork prop slice - widening the overlay props is required to make them data-bound; otherwise these regions are fixture-only and risk drift from the live ledger. | DATA-DRIVEN COUNT: artifact hardcodes exactly 5 systems and 4 satellites; spokes count is dynamic - iterate spokes (with a 4-chip cap for satellites like WORK_WHEEL_MAX_CHIPS) and route overflow through the drawer, do not assume 5.

**Proposed SCSS classes:** statusWorkWheelOverlay, statusWorkWheelOverlay__wheelCard, statusWorkWheelOverlay__wheelCaption, statusWorkWheelOverlay__systemsCard, statusWorkWheelOverlay__systemRow, statusWorkWheelOverlay__systemDot, statusWorkWheelOverlay__systemName, statusWorkWheelOverlay__systemValue, statusWorkWheelOverlay__systemConsequence, statusWorkWheelOverlay__offlineCard, statusWorkWheelOverlay__offlineRow, statusWorkWheelOverlay__queueSlots, statusWorkWheelOverlay__queueSlot, statusWorkWheelOverlay__forecastRow, statusWorkWheelOverlay__logCard, statusWorkWheelOverlay__logRow, statusWorkWheelOverlay__timeline, statusWorkWheelOverlay__timelineSeg, statusWorkWheelOverlay__timelineLegend, statusWorkWheelOverlay__history, statusWorkWheelOverlay__ownership, statusWorkWheelOverlay__ownershipSeal, statusWorkWheelOverlay__footnote, statusObservatoryOverlayShell, statusObservatoryOverlayShell__head, statusObservatoryOverlayShell__title, statusObservatoryOverlayShell__sub, statusObservatoryOverlayShell__ctx, statusObservatoryOverlayShell__tag, statusObservatoryOverlayShell__watermark, statusObservatoryOverlayShell__close, statusObservatoryOverlayShell__corner, statusObservatoryOverlayShell__mountains, statusObservatoryOverlayShell__content

---

## ledger → `StatusFoldedLedgersOverlay.tsx`  (surface 17/23)

**Current app:** Today the app has no full-screen "ledger" overlay. The folded ledger appears two ways: (1) StatusFoldedLedgerRail.tsx (footer rail) renders the Recent Changes fold (up to 5 row labels + a count + a 記 cinnabar chop) and a "How calculated" fold (calc row labels + decorative "View formula ›" text), each a single <button> opening a drawer; (2) StatusObservatoryDrawers.tsx renders a side <aside role="dialog"> StatusSupportDrawer for drawer.kind 'recentChanges' (EvidenceRow list of recent.rows / emptyState) and 'calculation' (calculation.title + calculation.summary + calculation.rows as EvidenceRows). Neither reuses an ovShell-style chrome (title/sub/context-chips/wax-seal/close/vertical-tag/mountains); there is no shared overlay-shell component in the codebase yet.

**Build plan:** 1. First build a shared StatusObservatoryOverlayShell.tsx (no equivalent exists today) that ports ovShell: role='dialog' aria-modal='true', full-screen scaled to the same design canvas, with grain (reuse ObsPanelChrome grain/corner pieces), vertical tag, header (title + sub + context chips + wax seal via SealStamp/waxSeal port + ✕ close), inkMountains band (reuse ObsInkMountains), glyph watermark, and motes. Wire useDialogFocusTrap (already used by drawers) for focus trap/restore + Escape→onClose, and aria-label from title. Context chips fed by surface.rawLedger.hero (Next Goal=hero.nextMajorGoalLabel, Main Bottleneck=hero.mainBottleneckLabel, third chip from hero.primaryAction) — RELABEL the third chip away from the forbidden 'Primary Route'. 2. Build StatusFoldedLedgersOverlay.tsx consuming surface.ledgerRail (recentChanges, details, noLossFamilies) + surface.rawLedger.hero for chips. Render inside the shell with title 'Folded Ledgers'. 3. Left card (Recent Changes): top/bottom rodbar decorations (pure CSS, decorative), card head from recentChanges.title; iterate recentChanges.rows → disc (tone-driven gold/cinnabar, derive glyph from icon/tone or use a tone dot) + label + (timestamp column omitted — missing). Empty state → recentChanges.emptyState.label. Provenance + fold-footer as static prose; footer wax seal 歷 decorative. Filter chips: either omit or render visibly-decorative non-interactive chips (no onClick) to avoid implying behavior. 4. Right card (How Calculated): rodbars + head from details.title; iterate details.rows → metric block: name(label) + owner(row.sourceLabel, no 'Source:' prefix) + a real route button when row.action exists (single button, NOT nested) or plain text otherwise; render row.value/row.detail as the evidence line. The artifact per-line breakdown sub-table is MISSING data — render row.detail in its place (do not fabricate factor rows). Note line from details.summary. Honor details.closedByDefault if the fold should start collapsed. 5. Wire opening: extend StatusObservatoryDrawerKind/route so the recentChanges + calculation folds in StatusFoldedLedgerRail open this single full-screen overlay (one overlay covering both cards) instead of (or in addition to) the side StatusSupportDrawer; keep aria-expanded/aria-controls on the fold buttons pointing at the overlay id. Preserve data-testid='status-ledger-details' and data-s7/s8 instrument hooks. 6. All colors via existing tokens: cinnabar #8b3028→var(--observatory-cinnabar), jade #0f6f5a→var(--observatory-jade), gold-deep #a2712a→var(--paper-gold-deep)/var(--observatory-gold-deep), parchment #f3ead7→var(--observatory-paper); ink-70/ink-45→var(--paper-ink-70)/var(--paper-ink-45); fonts var(--kai)/var(--serif). 7. Decorative-only: rodbars, end-cap discs, filter chips, watermark, motes, mountains, wax seals, glyph discs, fold-footer 'Records' pointer. Data-bound: card headings, change rows + tone, empty state, metric names + owner(sourceLabel) + value/detail + action, summary note, context chips.

**Artifact regions:**
- **ovShell chrome — grain + corners** _(Full overlay; .grain covers inset:0 (SVG grainF filter rect); four .gc corner ticks tl/tr/bl/br.)_ — Decorative parchment grain texture + 4 gold corner brackets. Pure chrome, no data.
- **ovShell chrome — vertical tag (ovtag)** _(Far left edge of overlay, vertical strip.)_ — tag='歷記' split per-character stacked vertically (歷<br>記). Decorative title chop column.
- **ovShell chrome — header (ovhead, padding-left:74px)** _(Top band of overlay.)_ — Left: .ovtitle 'Folded Ledgers' + .ovsub 'Recent changes and calculation evidence'. Right cluster (flex gap 16): context chips (ovctx), wax seal 歷記 (waxSeal size 52 rot -4, cinnabar disc), and .ovclose ✕ close button (onclick closeOverlay).
- **ovShell chrome — context chips (ovctx)** _(Header right cluster, before the seal.)_ — Three stacked <i>label/<b>value pairs pulled from STATE decree: 'Next Goal'→dc.goal; 'Main Bottleneck'→dc.bottleneck (colored dc.bottleneckCol, default #8b3028 cinnabar); 'Primary Route'→dc.route (color #0f6f5a jade). NOTE: literal label 'Primary Route' is a FORBIDDEN vocabulary string.
- **ovShell chrome — content backdrop (ovcontent)** _(Below header, fills remaining overlay; position:relative wrapper width/height 100%.)_ — Ink-wash mountains band (inkMountains, bottom, height 260, z-index 0); faint vertical glyph watermark wm='摺記簿冊' (.gcol right:54px top:110px font-size:52px); 5 floating .mote particles. All decorative.
- **Region A — Recent Changes Ledger card (.ovcard 'recent')** _(position:absolute; left:18px; top:22px; width:40%; overflow:visible.)_ — A folded-ledger card. Carries top+bottom rodbar() brass scroll rods (gradient bar + two end-cap discs each). Sub-heading 'Recent Changes Ledger' (font 10px, color #8b3028 cinnabar).
- **Region A.1 — Filter chips row** _(Top of card A, flex gap 6, margin-bottom 8.)_ — 5 .chip pills: 'All' (active: bg #243a30 / color #e9efe2 / border #0f6f5a), 'Cultivation','Combat','Prep','Root & Law'. cursor:pointer but NO click handler wired — decorative/non-functional filter affordance.
- **Region A.2 — Change entry rows** _(Stacked under filters; each row flex justify-between, padding 7px 0, dashed bottom border rgba(120,90,46,.2).)_ — Per entry: left = a 22x22 SVG glyph disc (circle r9.5 fill url(#goldRad) or url(#cinnDisc) when c.bad, stroke gold/cinnabar; centered kai glyph c.g) + the change text c.t (font 11px, color var(--ink-70)); right = relative timestamp from times[] ('just now','2m ago',...) by index. Rows = baseCh (st.ledgers.changes) each given a cycling glyph from ['功','根','備','兵','心'], CONCATENATED with 3 hardcoded extra entries (術 'Technique rank checked vs floor', 策 'Loadout policy mismatch noted', 藥 'Mercy-path progress +1').
- **Region A.3 — Provenance note** _(Below entries, margin-top 10, font 9.3px color var(--ink-70).)_ — Static text: 'Every gameplay event that changes a Status value is recorded here in source order. Status never edits the record.'
- **Region A.4 — Fold footer** _(Bottom of card A; flex justify-between, top border 1px rgba(120,90,46,.25), padding-top 7, margin-top 8.)_ — Left: 'Older entries folded — open Records for the full chronicle.' (Records bolded gold-deep). Right: small wax seal 歷 (waxSeal '歷' size 26 rot -5). Decorative pointer to a Records chronicle.
- **Region B — How Calculated card (.ovcard 'how')** _(position:absolute; right:18px; top:22px; width:55%; overflow:visible.)_ — Folded-ledger card with top+bottom rodbar() brass rods. Sub-heading 'How Calculated — Formula Sources' (font 10px, color #0f6f5a jade).
- **Region B.1 — Per-metric formula rows** _(Stacked in card B; each block padding 7px 0, dashed bottom border.)_ — One block per metric name c in calc (st.ledgers.calc). Header row: <b> metric name (12px) on left; right cluster = 'Owner: <b>OWNER</b>' (8.5px, owner gold-deep) + a .btn.jade 'View formula' pill. Owner from hardcoded map owner{} ('Qi / s'→Cultivation, 'Stability'→Dao Heart, 'Combat Strength'→Training Hall, 'Breakthrough Chance'→Cultivation, 'All sources'→All systems).
- **Region B.2 — Breakdown sub-table** _(Under each metric header; margin-top 5, padding 6px 10px, bg rgba(120,90,46,.06), border-radius 3.)_ — Rows of label/value from hardcoded breakdown{} map keyed by metric name. e.g. 'Qi / s': [['Base cultivation','+92 / s'],['Heart Law parity','×0.74'],['Root fit penalty','×0.62'],['Foreground bonus','+0 (idle)']]; 'Stability','Combat Strength','Breakthrough Chance' each have their own 4 rows. Value is bold var(--serif). Only renders if breakdown[metric] exists.
- **Region B.3 — How-calculated note** _(Bottom of card B; font 9.3px color var(--ink-70), margin-top 8.)_ — Static text: 'Cultivation base, build, preparation, safety net, heart law, spirit root, equipment, and active effects all contribute. Status reads these values; it never recomputes gameplay truth.'

**Surface mapping:**
- [y] Overlay title 'Folded Ledgers' ← `ledgerRail.title ('Folded Ledger Rail')` — Surface title differs from artifact 'Folded Ledgers'; reconcile (hardcode artifact copy or relabel surface). Both acceptable as static title.
- [n] Overlay subtitle 'Recent changes and calculation evidence' ← `(none) static copy` — Subtitle is fixed artifact prose; render as a constant. Optionally derive from recentChanges.title + details.title.
- [y] Context chip 'Next Goal' value ← `rawLedger.hero.nextMajorGoalLabel (StatusLedgerSurfaceV1.hero)` — Use the real hero goal label rather than artifact STATE.decree.goal. Available via surface.rawLedger.hero.
- [y] Context chip 'Main Bottleneck' value ← `rawLedger.hero.mainBottleneckLabel / mainBottleneckDetail` — Available; color cinnabar like artifact. Shell shared with other overlays so chips come from hero, not a ledger-local field.
- [y] Context chip third chip (artifact 'Primary Route') ← `rawLedger.hero.primaryAction (StatusLedgerActionSurface) → label/destinationLabel` — Data available via hero.primaryAction, BUT the artifact LABEL 'Primary Route' is FORBIDDEN vocabulary — relabel the chip (e.g. 'Recommended Route' / 'Route') in the shared shell.
- [y] Recent Changes sub-heading ← `ledgerRail.recentChanges.title ('Recent Changes')` — Artifact uses literal 'Recent Changes Ledger'; surface title is 'Recent Changes'. Append ' Ledger' or use surface title.
- [y] Recent change entry text ← `ledgerRail.recentChanges.rows[].label (StatusLedgerFactRow.label)` — Maps to c.t. Use real rows; do NOT append the 3 hardcoded extra (術/策/藥) artifact entries — those are fabricated demo data.
- [y] Recent change entry value / detail ← `ledgerRail.recentChanges.rows[].value | .detail` — Artifact only shows the change text + timestamp; value/detail can feed a title/tooltip or be omitted to match artifact density.
- [n] Recent change glyph disc character ← `(none) — artifact cycles ['功','根','備','兵','心'] by index` — Surface has no per-row glyph. Either derive a deterministic glyph from row.icon/tone or drop the glyph for a tone dot (rail already uses __dot). Decorative.
- [y] Recent change disc tone (gold vs cinnabar 'bad') ← `ledgerRail.recentChanges.rows[].tone (StatusLedgerTone)` — Map tone danger/warning → cinnabar disc (artifact c.bad → cinnDisc), else gold (goldRad). Replaces artifact's absent c.bad flag.
- [n] Recent change timestamp ('2m ago' …) ← `(none)` — No timestamp on StatusLedgerFactRow. times[] is hardcoded artifact array. Omit the timestamp column or add a field to the surface; treat as missing data.
- [n] Filter chips (All/Cultivation/Combat/Prep/Root & Law) ← `(none)` — No filter taxonomy in surface; chips have no handler in artifact. Render as decorative-only OR omit to avoid implying interactivity. Not data-bound.
- [y] Recent Changes empty state ← `ledgerRail.recentChanges.emptyState (StatusLedgerFactRow)` — Render emptyState.label when rows.length === 0 (mirrors current rail/drawer behavior).
- [n] Fold footer 'open Records' note ← `(none) static copy` — Static artifact prose; 'Records' is a decorative pointer. No surface field; render as constant text (no real navigation target exposed here).
- [y] How Calculated sub-heading ← `ledgerRail.details.title ('How calculated')` — Artifact 'How Calculated — Formula Sources'; surface title 'How calculated'. Use surface title (+ optional ' — Formula Sources' suffix).
- [y] How-calculated summary note ← `ledgerRail.details.summary` — Real summary string available; render as the B.3 note instead of the hardcoded artifact sentence.
- [y] Per-metric formula block name ← `ledgerRail.details.rows[].label (StatusLedgerFactRow.label)` — Maps to calc[] metric names. Drive blocks from details.rows, not artifact's STATE.ledgers.calc.
- [y] Per-metric value / current reading ← `ledgerRail.details.rows[].value | .detail` — Surface gives a single value+detail per row — NOT the artifact's multi-line breakdown sub-table. Show value/detail as the row evidence.
- [y] Per-metric 'Owner: X' ← `ledgerRail.details.rows[].sourceLabel (StatusLedgerFactRow.sourceLabel)` — Closest real field is sourceLabel (the owning system). Use it INSTEAD of artifact hardcoded owner{} map, and do NOT prefix with the forbidden 'Source:' string — use a neutral label or just render sourceLabel.
- [y] 'View formula' jade pill ← `ledgerRail.details.rows[].action (StatusLedgerActionSurface | null)` — If row.action present, render a real route button (DrawerAction-style) NOT a button-in-button; if null, render plain decorative text. closedByDefault:true means details fold starts collapsed.
- [n] Breakdown sub-table rows (Base cultivation +92/s …) ← `(none)` — breakdown{} is entirely hardcoded artifact demo data. Surface exposes only one row per metric. Render row.detail instead; the per-line factor breakdown is MISSING.
- [y] Wax seals (歷記 header, 歷 footer) ← `(none) decorative` — Decorative chrome; render via shared SealStamp/waxSeal port (cinnabar disc). No data binding.
- [y] No-loss source coverage ← `ledgerRail.noLossFamilies[] (StatusObservatoryNoLossFamilySurface: family/sourcePath/defaultHome/exactHome/represented/rowCount/actionCount)` — Artifact ledger overlay does NOT visualize noLoss families, but the surface bundles them here; keep for source-coverage/no-loss contract (could anchor a coverage strip or stay drawer-only). Not in artifact regions.

**Missing data:** Per-row glyph characters for recent-change discs (artifact cycles 功/根/備/兵/心) — no glyph field on StatusLedgerFactRow. | Relative timestamps ('just now','2m ago',…) for recent changes — no time/age field on StatusLedgerFactRow or recentChanges. | The 3 extra hardcoded recent entries (術 Technique rank checked vs floor, 策 Loadout policy mismatch noted, 藥 Mercy-path progress +1) — fabricated demo data, not in surface. | Filter taxonomy (All / Cultivation / Combat / Prep / Root & Law) and per-row category — no filter/category field exists. | Per-metric breakdown sub-tables (e.g. Qi/s: Base cultivation +92/s, Heart Law parity ×0.74, Root fit penalty ×0.62, Foreground bonus +0; plus Stability / Combat Strength / Breakthrough Chance factor rows) — surface gives only one value+detail per metric row, not the factor decomposition. | Per-metric 'Owner' map (Cultivation / Dao Heart / Training Hall / All systems) — closest is row.sourceLabel; the artifact's explicit owner mapping is not a dedicated field. | 'open Records for the full chronicle' navigation target — no Records route/destination exposed on the ledger surface.

**Fidelity risks:** FORBIDDEN LABEL 'Primary Route': the artifact ovctx third chip literally renders 'Primary Route', which is decommissioned vocabulary (daoMandateP8VocabularyDecommissionContract / CultivationExactNoLegacyPanels / daoOmenSharedComponents contracts grep source for /\bPrimary Route\b/). The shared shell MUST relabel it (e.g. 'Recommended Route' / 'Route') and source text must not contain the string. | FORBIDDEN PREFIX 'Source:': artifact 'Owner:' is fine, but do NOT introduce a 'Source:' label when rendering row.sourceLabel — several contracts forbid the 'Source:' string and 'Source <field>' patterns. | Fabricated/demo data fidelity trap: the artifact concatenates 3 hardcoded recent entries (術/策/藥), a hardcoded times[] timestamp array, hardcoded owner{} and breakdown{} maps, and 5 decorative filter chips. Reproducing these as if real would render non-truthful Status data and break the 'Status reads, never recomputes' provenance promise — render only surface-backed rows and treat the rest as decorative or omitted. | CLS / no-CLS law: overlay must mount on the same fixed design canvas with stable sizing (the screen is single-layout, no css grid per V7 re-ground notes). Decorative rodbars/discs/motes must not shift the data layout; reserve space so opening the overlay does not reflow the underlying observatory. | button-in-button: the fold buttons in StatusFoldedLedgerRail are already <button>; the per-metric 'View formula' route must be a sibling/standalone button (DrawerAction pattern), never nested inside another button, and the shell ✕ close is its own button. | Raw-hex audit: artifact uses #8b3028/#0f6f5a/#a2712a/#f3ead7 and rodbar/disc gradients with many raw hex (#caa15a,#7a5b26,#4a330e,#f0d99e,#9a6f24, etc.). Map all to var(--observatory-*)/var(--paper-*) tokens; net raw-hex must not increase (W6 note tracked net raw-hex −2). Inline-style SVG fills like url(#goldRad)/url(#cinnDisc) reuse existing defs — keep tokenized. | a11y: dialog needs aria-modal, labelled by title, focus trap + restore + Escape (reuse useDialogFocusTrap); decorative discs/seals/rods/motes aria-hidden; the non-interactive filter chips must not be focusable/announce as actionable; timestamp/glyph omissions must not leave empty announced cells. | Vocabulary/source-text contracts (statusV2Layout / statusLedgerLayout / proofSurfaceNormalization) assert on instrument data-testids and copy — preserve data-testid='status-ledger-details' / rootTestId and avoid introducing banned strings; keep recentChanges count + details summary truthful. | Surface-title vs artifact-copy drift: ledgerRail.title='Folded Ledger Rail' vs artifact 'Folded Ledgers'; recentChanges.title='Recent Changes' vs 'Recent Changes Ledger'; details.title='How calculated' vs 'How Calculated — Formula Sources' — pick one source of truth so a copy/string contract does not fail on the mismatch.

**Proposed SCSS classes:** statusFoldedLedgersOverlay, statusFoldedLedgersOverlay__recentCard, statusFoldedLedgersOverlay__howCard, statusFoldedLedgersOverlay__rodbar, statusFoldedLedgersOverlay__rodbar--top, statusFoldedLedgersOverlay__rodbar--bottom, statusFoldedLedgersOverlay__rodCap, statusFoldedLedgersOverlay__cardHead, statusFoldedLedgersOverlay__filters, statusFoldedLedgersOverlay__filterChip, statusFoldedLedgersOverlay__filterChip--active, statusFoldedLedgersOverlay__change, statusFoldedLedgersOverlay__changeDisc, statusFoldedLedgersOverlay__changeText, statusFoldedLedgersOverlay__changeTime, statusFoldedLedgersOverlay__provenance, statusFoldedLedgersOverlay__foldFooter, statusFoldedLedgersOverlay__metric, statusFoldedLedgersOverlay__metricHead, statusFoldedLedgersOverlay__metricOwner, statusFoldedLedgersOverlay__viewFormula, statusFoldedLedgersOverlay__breakdown, statusFoldedLedgersOverlay__breakdownRow, statusFoldedLedgersOverlay__note, statusObservatoryOverlayShell, statusObservatoryOverlayShell__head, statusObservatoryOverlayShell__title, statusObservatoryOverlayShell__sub, statusObservatoryOverlayShell__ctx, statusObservatoryOverlayShell__tag, statusObservatoryOverlayShell__close, statusObservatoryOverlayShell__content, statusObservatoryOverlayShell__mtn, statusObservatoryOverlayShell__watermark

---
