
##### DECREE  (match 68) #####
  [H] goal glyphs
      FIX: TSX StatusLifeDecreeScroll add span statusLifeDecree goalGlyph before each goal label color bottleneck cinnabar others gold
  [H] goal label width
      FIX: SCSS line 702 change 84px to 96px
  [H] wax seal
      FIX: TSX seal size 60 SCSS stateSeal top 50pct translateY minus 50pct right 14px
  [M] roller spines
      FIX: SCSS rollerCap left 62 right 84 remove verticalSeal 458 to 467
  [M] chip medallions and route color
      FIX: SCSS medallion 26px and add disabled strong color gold deep opacity 1

##### VITALS  (match 72) #####
  [H] 9th tile — FOREGROUND / Idle (glyph 行)
      FIX: Root cause is data, not CSS. In src/systems/ui/status/statusObservatoryFixtures.ts, the hand-written `metrics:` array (lines 526-535) has 8 entries and omits foreground; add a 9th entry e.g. `fixtureFact('foreground', 'Foreground', 'Idle', 'Current foreground activity.', 'jade', 'hourglassProgress')` so ledger.metrics.map(metricSeal) (statusObservatorySurface.ts:905) yields 9 seals. ALSO add `foreground: '行'` to the VITALS_GLYPH map in src/ui/status/observatory/StatusVitalsSealRibbon.tsx (lines 12-23) so the medallion renders the 行 glyph rather than a bare disc.
  [H] STABILITY medallion disc color (danger/at-risk tone)
      FIX: In src/ui/status/observatory/StatusVitalsSealRibbon.tsx line 49, change `variant={seal.tone === 'danger' ? 'cinnabar' : 'gold'}` to always `variant="gold"`. Keep the cinnabar value (SCSS .statusVitalsRibbon__seal[data-tone='danger'] .statusVitalsRibbon__value, line 975-977) as the at-risk signal; optionally retain the danger ring rule (lines 970-973) for grayscale a11y but it should not change the disc fill to red.
  [M] Label (.statusVitalsRibbon__label) letter-spacing
      FIX: In StatusLivingStateObservatory.scss .statusVitalsRibbon__label (line 939) change `letter-spacing: 0.05em;` to `letter-spacing: 0.16em;` to match the artifact .sub tracking.

##### ROOTLAW  (match 38) #####
  [H] Overall rendered STATE (whole instrument)
      FIX: The capture rendered the wrong state. The fixture `blocked-realm-edge` sets opposed via `/opposed/i.test(seed.lifeDecree.spiritRootLabel)` and seed.lifeDecree.spiritRootLabel='Void Root - Opposed' (statusObservatoryFixtures.ts:60, :350, :369 totalMultiplierLabel '-2.32x', :372 tone 'danger'). Verify obs-preview.html actually applies ?obsFixture=blocked-realm-edge to surface.rootLawInstrument; if the preview falls back to live state, wire the fixture loader so StatusRootLawCoupledInstrument receives the fixture surface. Re-capture once opposed renders, then re-diff. Until the same state renders, all color/label drifts below are confounded by this mismatch.
  [H] Heart Law Seal — medallion color variant
      FIX: Driven by the `distress` prop in StatusRootLawCoupledInstrument.tsx:67 (`distress={surface.bridge.fitTier === 'opposed'}`) and the `red` branch in StatusHeartLawSeal.tsx:21,28-37. This is correct code — it is firing gold because fitTier is not 'opposed' in the rendered (wrong) state. No SCSS/TSX change needed beyond fixing the state load (drift #1). Once fitTier='opposed', bookFill→'url(#cinnDisc)', flame→parchment-soft, crack path renders. Confirm after re-capture.
  [H] Astrolabe title — duplicated word 'Root'
      FIX: StatusSpiritRootAstrolabe.tsx line 129: `<strong>{surface.spiritRoot.elementLabel} Root</strong>`. elementLabel already contains 'Root' (fixtures.ts:366 `${fixtureTitleCase(rootElement)} Root`; surface builder passes spiritRoot.elementLabel through). Change line 129 to `<strong>{surface.spiritRoot.elementLabel}</strong>` (drop the literal ' Root'). Same double-append affects the core (lines 225-226 render elementLabel then a second 'Root' on line 226) — collapse to a single line rendering elementLabel only, or split elementLabel on whitespace if two-line stacking is desired.
  [H] Astrolabe core text — raw element id leaking
      FIX: The core renders surface.spiritRoot.elementLabel (StatusSpiritRootAstrolabe.tsx:225) plus a literal 'Root' (line 226). The value contains an underscore, so either elementLabel itself is 'Void_Root Root' or the field used resolves to the raw id. Fix at the surface/fixture source: in statusObservatoryFixtures.ts:366 ensure fixtureTitleCase(rootElement) converts 'void_root' → 'Void Root' (replace underscores with spaces before title-casing). And in the TSX, stop appending the extra literal 'Root' on line 226 (mirror of drift #3) so the core shows exactly 'Void Root'.
  [H] Bottom data quad + TOTAL MULE strip (instrument footer rows)
      FIX: Two issues compound here. (1) State (drift #1): the values must come from the opposed fixture (totalMultiplierLabel '-2.32x', expressionCapLabel '43 / 100', proc 'Hollow Step', runValidity 'Playable but strained'). (2) totalMultiplierLabel is captured into the surface (statusObservatorySurface.ts:262 `totalMultiplierLabel: spiritRoot.totalMultiplierLabel`) but the astrolabe summary block (StatusSpiritRootAstrolabe.tsx:232-237) renders only Grade/Purity/Fit — it never renders totalMultiplierLabel. Add a 'Total Mule' cell to the `.statusSpiritRootAstrolabe__summary` grid rendering `{surface.totalMultiplierLabel}` so the strip appears. Confirm the data row uses opposed-state labels after the state fix.
  [M] Heart Law title text truncation
      FIX: heartLawLabel is 'Ember Thread Sutra' in the fixture (statusObservatoryFixtures.ts:59). The seal column is too narrow so the strong title wraps/clips. Widen the right column or allow wrap: in StatusLivingStateObservatory.scss `.statusRootLawCoupledInstrument__body` (line 1023) the right column is `minmax(190px, 0.92fr)` — increase the min to e.g. `minmax(210px, 0.96fr)`, and ensure `.statusHeartLawSeal__title strong` (line 1051) does not have nowrap/overflow clipping (it doesn't currently, so the cause is column width / the gold medallion forcing min-width). Also verify the eyebrow `.statusHeartLawSeal__title span` is not being letter-spaced past the column width.
  [M] Bridge (middle column) content + treatment
      FIX: Bridge state comes from surface.bridge.state; markerCount=3 cracks only when state==='opposed' (StatusRootLawCoupledInstrument.tsx:11-15). The cracked-cord CSS exists and is correct (StatusLivingStateObservatory.scss:1229-1273, 1269 opposed line). This is a consequence of drift #1 (wrong state → bridge resolves to neutral/aligned). After the opposed state loads, the bridge will render `data-bridge-state='opposed'`, the dashed cinnabar line, and 3 cracks. No code change needed beyond the state fix; verify the bridge label/detail then read 'Opposed' / opposed copy.
  [M] Footer route buttons visibility
      FIX: Buttons are conditional on `action` existing: observe button on StatusSpiritRootAstrolabe.tsx:275-290 (needs surface.routeActions with a status_observation target / 'Observe Spirit Root' label) and the Dao Heart button on StatusHeartLawSeal.tsx:101-116 (needs surface.heartLawSeal.routeAction). In the rendered (wrong) state these route actions are likely absent. After the fixture loads, confirm both routeActions are populated (fixtures.ts fixtureAction 'observe-spirit-root' at :373 and the heart-law route). If buttons still clip, the instrument min-height (StatusLivingStateObservatory.scss:1012 `min-height:430px`) may be too short for the populated footer — bump to fit, matching the artifact's taller panel.
  [M] Horizon / mountain landscape band
      FIX: The region is `obsRegion--mtn` (StatusLivingStateObservatory.tsx:115) and ObsPanelChrome takes `mtn="root-law"`, so a mountain layer is intended. Verify the `obsRegion--mtn` / mtn='root-law' background layer is actually painted — grep ObsPanelChrome / `obsRegion--mtn` SCSS; the band may be clipped because `.statusRootLawCoupledInstrument` (SCSS:1014-1018) paints its own opaque `linear-gradient(...rgba(255,250,232,.96)...)` over the chrome's mtn layer. Reduce the instrument background opacity (e.g. drop the near-opaque white linear-gradient stop) or move the mtn layer above it so the horizon shows through.

##### VESSEL  (match 62) #####
  [H] Organ card badge content (體/心/根/武/備/功 glyph vs ordinal number)
      FIX: In src/ui/status/observatory/StatusMeridianVesselCompass.tsx the medallion <text className="statusMeridianVesselCompass__organMark"> renders {organ.ordinal} (line ~410). Replace with a per-organ glyph lookup, e.g. const ORGAN_GLYPH:Record<id,string>={cultivation:'體',daoHeart:'心',spiritRoot:'根',training:'武',buildPrep:'備',activeWork:'功'} and render {ORGAN_GLYPH[organ.id]}. Bump .statusMeridianVesselCompass__organMark font-size from 13px to ~14px and keep font-family serif so the CJK glyph reads (SCSS line 1718-1723). This is the W4-deferred 'glyph atlas' noted at line 392.
  [H] Organ card title ordinal prefix
      FIX: In StatusMeridianVesselCompass.tsx line 414 change <strong>{organ.title}</strong> to <strong>{organ.ordinal}. {organ.title}</strong> (or `${organ.ordinal}. ${organ.title}`). Keep the -webkit-line-clamp:1 in SCSS line 1756-1765; the prefix is short so it won't wrap.
  [H] Organ card subtitle/value overlaps the title
      FIX: In StatusLivingStateObservatory.scss give the subtitle its own row. Change grid-template-rows so row 1 = title, row 2 = subtitle, row 3 = state+route, and make the badge span rows 1-2. Concretely: on .statusMeridianVesselCompass__organSeal (line 1611) add grid-template-rows: auto auto auto; on .__organSeal > .__organSealInner (line 1650) set grid-row: 1 / span 2; on .__organSeal > small (line 1664-1670) set grid-row: 2 and REMOVE margin-top:16px and align-self:end; and move <em>/<route> to grid-row: 3 (lines 1674-1686). Also bump row-gap from 5px to ~3px (line 1628).
  [H] Focus lens structure: prose + inline Route vs stacked VALUE/CONSEQUENCE labels
      FIX: This is a structural mismatch in src/ui/status/observatory/StatusFocusLens.tsx. To match the artifact: drop the "VALUE" __copy row (merge value into the title or omit), render consequence as a plain paragraph without the "CONSEQUENCE" span, and replace the .statusFocusLens__route button (lines 65-80) with an inline line: <p><strong>Route: </strong><a/span class=gold>{routeButtonText(action)}</a></p>. Style the route text with color:var(--observatory-gold). If the SOURCES rail isn't in the artifact slip, hide .statusFocusLens__sources (SCSS line 1950) within .statusMeridianVesselCompass__focusLens. Note: do NOT introduce 'Source:'/'Primary Route' labels.
  [M] Focus lens eyebrow text
      FIX: In src/systems/ui/status/statusObservatoryPresentation.ts change STATUS_OBSERVATORY_MERIDIAN_FOCUS_LABELS.eyebrow (line 473) from 'Focus Lens - Active Inspection' to 'Focused Inspection'. Uppercase + 0.74rem muted styling already match (SCSS line 584-597).
  [M] Focus lens title text
      FIX: In StatusMeridianFocusLens.tsx titleFor() (StatusMeridianFocusLens uses lens.label) — pass a composed title like `${lens.label} ${lens.value}` (e.g. 'Build / Prep below minimum') when matching the artifact's single-headline form, OR adjust the fixture lens.label. The h3 styling (1.16rem serif, SCSS line 1898-1903) already matches.
  [M] Per-organ states are uniform 'Needs Work'/gold (fixture data)
      FIX: Fixture-driven, not CSS. In src/systems/ui/status/statusObservatoryFixtures.ts FIXTURE_ORGAN_STATES.blocked (line 208) sets all to 'attention'. To match the artifact set cultivation:'healthy' and activeWork:'healthy' (and verify daoHeart/training intended states). The em/badge/rail colors already key off data-organ-state (SCSS 1725-1744).
  [M] Card route pills are all 'Open Route' (fixture data)
      FIX: Fixture-driven. In statusObservatoryFixtures.ts a single sharedRoute=fixtureAction('open-owner','Open Route',...) (line 376) is assigned to every block's route (line 413). Give makeBlock a per-organ route label map to reproduce the distinct pills. Pill geometry/style (SCSS .statusMeridianVesselCompass__organRoute line 1790-1807) already matches the artifact .btn.
  [M] Card subtitle strings differ (fixture data)
      FIX: Fixture-driven content (statusObservatoryFixtures.ts valueLabelFor map, line 387-394, and stageText). Update daoHeart:'Clarity steady'->'Ember Thread Sutra' and training:'Path 11 / 16 avg.'->'Martial 0/16 avg.', and the cultivation stageText to 'Qi Condensation 1/9' to match. Note the artifact uses an em-dash in 'Void Root — Opposed' vs hyphen in mine.

##### CANOPY  (match 52) #####
  [H] Satellite slip placement (4 corner cards)
      FIX: In StatusBottleneckTalismanCanopy.scss .statusBottleneckTalismanCanopy__slip (line 2439): the artifact corner geometry is ~ TL 13.7%/16.4%, BL 14.6%/39.5%, TR 86.2%/16.4%, BR 85.5%/39.5% of the 608x560 board (left = (x+w/2)/608, top = (y+h/2)/560). Either feed those into the surface slip.geometry x/y, or clamp width to clamp(116px,24%,150px) (already) and set height to ~16.1% (90/560) instead of 14% and verify the fixture geometry pushes slips to corners. Net: slips must reach the four outer corners, not ring the edict.
  [H] Safety-net seal material/shape (bottom-right)
      FIX: In .statusBottleneckTalismanCanopy__safetySeal (line 2701) drop the filled background: remove the conic-gradient + radial fill and the inset 9px ring box-shadow so the disc is transparent; rely on the inner __mercyRing SVG (line 2898) as the visual. Make __mercyRingOuter fill transparent (line 2905 fill: rgba(...) -> none) and bump its stroke-width to ~1.8-2.2 to match the artifact's bold cinnabar ring. Keep border:none on the wrapper or reduce to a thin single ring so the seal reads as an open talisman, not a coin.
  [H] Central edict body content/density
      FIX: Edict card .statusBottleneckTalismanCanopy__edict (line 2353): reduce height (26% -> ~22-24%) and tighten gap (6px -> 4px). The verbose <p> detail (line 2390) should be a short single line; cap its line-clamp to 2 and shrink to ~0.62rem. Restyle the route action so it reads as the artifact's 'Primary Action / value' caption rather than a filled pill: in __edictRoute (line 2424) drop the rounded 999px green capsule (background rgba(47,127,98,.12), border) to a borderless text/label, or shrink min-height 34px -> ~26px and padding 7px 11px -> 4px 8px so it does not dominate the card.
  [M] 'Best Improvement Charms' section label
      FIX: The cluster board (part default, StatusBottleneckTalismanCanopy.tsx return at line 310) renders no charms heading; the charms live in the separate part='charms' instance (line 179) which also lacks the 'Best Improvement Charms' caption. Add a centered caption element above .statusBottleneckTalismanCanopy__routeCharms (line 190) e.g. <span class='...__charmsLabel'>Best Improvement Charms</span>, styled font:Georgia 10px, letter-spacing .1em, color var(--observatory-muted), centered, margin-bottom ~6px.
  [M] Charm medallion count + tone mix
      FIX: Count is fixture-driven (surface.routeCharms has 5 in this fixture vs 4 in artifact) — flag as data. For tone: .statusBottleneckTalismanCanopy__charmDisc[data-charm-tone='jade'/'success'] -> jadeRad (line 2885) already exists, so verify the fixture sets the first two charms' action.tone to 'jade'/'success' (currently they appear to resolve gold). The glyph gap (artifact shows 兵/療/術/心; mine shows numbers) is the documented W5-deferred glyph atlas — note as a known gap, not a quick fix.
  [M] Causal threads (red dashed crossing lines)
      FIX: In .statusBottleneckTalismanCanopy__thread (line 2311) the default opacity .76 / stroke-width 1.1 is too prominent for the blocked board. Lower default opacity to ~0.28-0.35 and stroke-width to ~0.7 so threads recede unless data-related='true' (which keeps the brighter 0.95/1.5 at line 2332). This restores the artifact's calm, cord-led hierarchy.

##### CONSTELLATION  (match 42) #####
  [H] Off-path (lifeless) node beads — heaven & earth clusters
      FIX: StatusLivingStateObservatory.scss — make the hit-target airy: in .statusStatConstellation__hitTarget (line ~3698) drop the box-shadow and lighten/flatten the base (background: transparent or rgba(255,247,220,0.25); border: 1px solid rgba(120,90,46,0.45)). In .statusStatConstellation__node[data-node-state='lifeless'] .statusStatConstellation__hitTarget (line ~3742) remove border-style:double and the grey radial-gradient — use border:1px solid rgba(120,90,46,0.5); background: rgba(255,247,220,0.18); no gradient. Reduce .statusStatConstellation__hitTarget width/height from 30px to ~22px to match the artifact bead size.
  [H] Connecting branch lines (heaven heptagon ring / earth web)
      FIX: StatusLivingStateObservatory.scss .statusStatConstellation__branchPath--heaven, --earth (line ~3425): change stroke-dasharray to a solid/large value (e.g. remove it or use 'none'), raise opacity by using stroke rgba(120,90,46,0.55), and bump stroke-width from 0.9 to ~0.7-0.9 solid. Also remove/soften filter: url('#statusStatBrushSoft') on .statusStatConstellation__branchPath (line ~3417) which blurs the thin strokes into nothing.
  [H] SVG branch name labels leaking into compact view
      FIX: Hide the branch <text> in compact. Either gate the branchEntries label map in StatusStatMeridianConstellation.tsx (lines 363-379) behind `expanded` (render only when variant==='expanded'), OR in StatusLivingStateObservatory.scss add `.statusStatConstellation[data-variant='compact'] .statusStatConstellation__label { display:none; }` near line 3436. Keep them for the expanded overlay only.
  [M] Visible bead vs hit-target vertical misalignment
      FIX: StatusLivingStateObservatory.scss .statusStatConstellation__node (line 3681): change transform to translate(-50%, -50%) so the hit-target centers on the geometry point (matching the SVG bead). The name caption, which needed the downward room, is opacity:0 in compact so this is safe; verify the expanded caption still positions acceptably.
  [M] Spine beads (central jade column)
      FIX: This is the same root cause — the 30px __hitTarget at translate(-16px) overlaps vertically given the 10-unit y spacing (≈ atlas_height/10 ≈ 46px per step at min-height 466px). After fixing __hitTarget size to ~22px and translate to -50%,-50% (drifts above), the foundation beads will separate. If still tight, reduce foundation hit-target to ~20px via .statusStatConstellation__node[data-node-state='foundation'] .statusStatConstellation__hitTarget.
  [M] Martial cluster nodes & gold brush ring
      FIX: StatusLivingStateObservatory.scss .statusStatConstellation__node[data-node-state='lit'] .statusStatConstellation__hitTarget (line 3736): flatten to a solid-ish gold (background: radial-gradient(circle, rgba(255,243,200,0.9), rgba(163,108,33,0.55))) and reduce the 0 0 12px glow to ~0 0 4px. The __branchPath--martial fix (gold, solid) from drift 2 restores the connecting lines. Optionally raise .statusStatConstellation__brush--martial opacity from 0.42 toward 0.5 and confirm its 'M62 30 …' path matches the artifact swirl radius.
  [M] Weak-link (cinnabar) node rendering — drawn twice & oversized
      FIX: Pick ONE crack representation. In StatusLivingStateObservatory.scss remove the ::after slash on .statusStatConstellation__node[data-weak-link='true'] .statusStatConstellation__hitTarget (lines 3761-3771) so only the SVG __crack shows, and reduce its visual weight; make the weak hit-target a hollow cinnabar ring (border:2px solid var(--observatory-cinnabar); background:transparent) to match. Keep the SVG weakPulse small.

##### SCALES  (match 34) #####
  [H] Checklist row content
      FIX: StatusBuildPreparationScales.tsx lines 44-50,77,100: stop using uniqueWeightRows(); render the six fixed readiness criteria with Title-Case labels and per-row ok/warn/risk status from the build/prep signal. Caps must be removed at the data source (rowLabel scss 4272 has no text-transform).
  [H] Status dot glyphs
      FIX: StatusBuildPreparationScales.tsx lines 113-115: render glyph for all marks: mark==='ok'?'✓':mark==='warn'?'!':'✕'. Backgrounds already match (scss 4267/4269/4270).
  [H] MAIN GAP warning row
      FIX: Remove the firstLowRow warning block in StatusBuildPreparationScales.tsx lines 175-187; scss .statusBuildPreparationScales__warning rules 4418-4425 become dead.
  [H] Open Build / Prep Ledger footer button
      FIX: Remove openLedger block in StatusBuildPreparationScales.tsx lines 189-197; replace with a small bottom-right 'Open Inspector' pill. Change scss .statusBuildPreparationScales grid (lines 4219-4220) from 'minmax(0,1fr) auto' to single 'minmax(0,1fr)'. Avoid forbidden 'Source:'/'Primary Route' labels.
  [M] Scale vertical placement/size
      FIX: After removing warning/footer rows, single-row grid lets .statusBuildPreparationScales__balance (scss 4283-4290) center the full SVG; ensure panel top matches artifact 28px inset so finial at y=30 clears banner.
  [M] Arm tilt + pan-hang behavior
      FIX: Set blocked tilt to -9deg (scaleTilt tsx 36-42 or surface). Counter-rotate each pan group by calc(-1*var(--scale-tilt)) so cords hang vertical, or port the artifact trig endpoint math.

##### JARS  (match 44) #####
  [H] Per-jar text block (name + value + state)
      FIX: src/ui/status/observatory/StatusReserveJars.tsx lines 142-144: remove the <span className="statusReserveJars__label">{jar.label}</span> and the <small>{state}</small>; keep only the single bold value <strong>{jar.value ?? state}</strong>. In SCSS .statusReserveJars__jar (line 4484) change grid-template-rows from 'minmax(0,1fr) auto auto' to 'minmax(0,1fr) auto' to drop the third row.
  [H] Spirit-Stones value content
      FIX: Root cause is the 3-line render plus the bound surface value; once StatusReserveJars.tsx renders only <strong>{jar.value ?? state}</strong> (see drift above) verify the bound buildPreparation.reserveJars[1].value resolves to 'Sufficient' for the blocked-realm-edge fixture. If the fixture supplies a number, the per-jar truth must be the qualitative word to match the artifact's 'Sufficient'.
  [H] Reserve Ledger dashed trigger row
      FIX: src/ui/status/observatory/StatusReserveJars.tsx lines 150-158: remove the entire <button className="statusReserveJars__ledgerTrigger"> block. In SCSS .statusReserveJars (line 4452) change grid-template-rows from 'minmax(0,1fr) auto' to 'minmax(0,1fr)' so the shelf fills the cell. Keep onOpenDrawer reachable via the jar buttons (already wired on each jar).
  [M] Glyph set for jars 4 and 5
      FIX: src/ui/status/observatory/StatusReserveJars.tsx JAR_GLYPH map (lines 63-70): change 'pouch-fit': '契' to '囊' and 'healing-reserve': '癒' to '療'. (Confirm jarKind() maps the 4th label to 'pouch-fit' and 5th to 'healing-reserve'; adjust kind keys if needed.)
  [M] Spirit-Stones liquid color (blue)
      FIX: src/ui/status/observatory/StatusLivingStateObservatory.scss after line 4551 add a rule: .statusReserveJars__jar[data-tone='info'] .statusReserveJars__liquid { background: linear-gradient(180deg, rgba(90,139,176,.86), rgba(74,115,150,.88)); } and ensure the spirit-stones jar surface carries data-tone='info' (or gold/blue). Without a blue tone bound, the artifact's distinctive blue stone jar cannot render.
  [M] Glyph ink color (per-jar vs tone-bucket)
      FIX: src/ui/status/observatory/StatusLivingStateObservatory.scss lines 4585-4605: drive glyph color from a per-jar CSS var instead of tone buckets, e.g. set --jar-glyph-col on the jar style in StatusReserveJars.tsx (#c79a45/#5a8bb0/#a44731/#8d642d/#9c4a3e) and change .statusReserveJars__glyph color to var(--jar-glyph-col, var(--observatory-jade)). Removes the green-石 mismatch.

##### WHEEL  (match 38) #####
  [H] Satellite chip placement (all four corners)
      FIX: In StatusLivingStateObservatory.scss the corner rules at lines 4789-4792 anchor chips to the dial's hard edges (top:0/bottom:0/left:0/right:0). Inset them toward the rim and vertically center the pairs: change to e.g. .__chip[data-corner='nw']{top:30%;left:6%;transform:translateY(-50%);} .__chip[data-corner='ne']{top:30%;right:6%;transform:translateY(-50%);} .__chip[data-corner='sw']{top:70%;left:6%;transform:translateY(-50%);} .__chip[data-corner='se']{top:70%;right:6%;transform:translateY(-50%);}. Also verify all 4 spokes are being passed: StatusCurrentWorkTimeWheel.tsx line 101 slices spokes[0..3] into cornerSpokes — if only 2 spokes reach the surface in this fixture, the SW/SE chips will be empty; the geometry fix above still re-centers whatever chips exist.
  [H] Chip shape & medallion treatment
      FIX: In .scss .statusCurrentWorkTimeWheel__chip (line 4767): change grid-template-columns:14px minmax(0,1fr) to a single centered column (grid-template-columns:minmax(0,1fr); justify-items:center; text-align:center), bump border-radius to ~999px/50% for the pebble/oval read, and widen border to a brass gold ring (border:1.5px solid color-mix(in srgb,var(--observatory-gold) 60%, transparent)). Either drop the ObservatoryDiscMedallion render in StatusCurrentWorkTimeWheel.tsx lines 243-246, or hide it via .statusCurrentWorkTimeWheel__chipMedallion{display:none;} (line 4804). Move chip text spans/strong to grid-column:1 (currently grid-column:2 at lines 4812 and 4823).
  [H] Chip label truncation
      FIX: In .scss .statusCurrentWorkTimeWheel__chip span (line 4811) and strong (line 4822): remove white-space:nowrap / overflow:hidden / text-overflow:ellipsis, or allow two-line wrap (white-space:normal; line-height:1.05). Also widen the chip from width:86px (line 4770) to ~92-96px so the short labels fit. Note the visible labels themselves (Foreground/Expedition vs Bounty/Combat/Prof/Exped) suggest the surface spoke feed differs from the artifact fixture — confirm surface.spokes content if labels must match exactly.
  [H] Hub (core) text order
      FIX: In StatusCurrentWorkTimeWheel.tsx lines 208-210, swap the order: render <small>{foreground.label}</small> BEFORE <strong>{foreground.value}</strong> so the label sits above the value, matching the artifact.
  [M] Hub label color
      FIX: In .scss .statusCurrentWorkTimeWheel__core small (line 4749): change color:var(--observatory-muted) to a jade tone, e.g. color:color-mix(in srgb,var(--observatory-jade) 70%, var(--observatory-ink)); keep the uppercase/letter-spacing.
  [M] Chip value color emphasis
      FIX: In .scss .statusCurrentWorkTimeWheel__chip strong (line 4822): add color:var(--observatory-gold-deep) (or cinnabar for blocked) so the value line picks up the artifact's warm accent. Optionally key off data-segment-state for idle=gold vs active=jade.

##### LEDGERS  (match 38) #####
  [H] Fold card surface (both folds) — parchment background / border / shadow
      FIX: StatusLivingStateObservatory.scss line 5314: remove `.statusFoldedLedgerRail__fold,` from the M3b `.statusLifeDecree, … { background: transparent; border: 0; border-radius: 0; box-shadow: none; }` selector list (block at lines 5312-5326). The fold card IS the ledger card (not a card-in-card), so it must keep its own parchment background/border/shadow defined at lines 4063-4084. After removal, the rules at 4063 (border 1px rgba(120,90,46,.45) + box-shadow 0 5px 12px rgba(40,26,12,.28)) and 4076-4084 (gradients + ∓0.6deg tilt) take effect.
  [H] Recent Changes list rows + count badge
      FIX: Data delta, not CSS: the `blocked-realm-edge` seed in src/systems/ui/status/statusObservatoryFixtures.ts builds `recentRows` (lines 454-458) with only the 'Offline cultivation' row (the extra 'failure-added' row is gated behind visualState==='postFailure'). To match the artifact 1:1 the fixture's recentRows must be expanded to the five demo entries (stability/spirit-root/reserves/hollow-step/weapon-floor). No change to StatusFoldedLedgerRail.tsx — it already maps all rows (visibleChanges caps at 5) and renders recent.rows.length in the badge.
  [H] How Calculated formula rows
      FIX: Data delta, not CSS: src/systems/ui/status/statusObservatoryFixtures.ts line 630 sets `details: { … rows: [] }`. StatusFoldedLedgerRail.tsx (lines 81-86) maps `details.rows.slice(0,5)` so with an empty array nothing renders. Populate the fixture's `details.rows` with the five rows (Qi/s, Stability, Combat Strength, Breakthrough Chance, All sources) to restore the calc list; the per-row .statusFoldedLedgerRail__calc / __calcLabel / __calcLink styling (lines 4143-4165) is already correct.
  [M] 記 corner chop placement (changes fold)
      FIX: Consequence of the high-severity M3b transparency bug — once `.statusFoldedLedgerRail__fold` regains its background/border/padding (and the 5 rows give the card real height), the chop's `bottom:6px` anchor (StatusLivingStateObservatory.scss .statusFoldedLedgerRail__chopSquare, lines 4168-4183) lands correctly at bottom-right. No standalone change needed beyond the fold-transparency fix; verify after.
  [M] Open Ledgers footer affordance
      FIX: Verify whether this control belongs to the ledgers region or to an adjacent region/footer in the React layout. If it is intended in this region, it is absent from StatusFoldedLedgerRail.tsx (which renders only the two folds). If present but clipped, check the `obsRegion--ledgers .obsRegion__content` inset (StatusLivingStateObservatory.scss line 295-296: inset 24px 12px 10px) — the artifact reserves bottom space for it. Confirm against the source-of-truth surface before adding (do not introduce forbidden labels).
