# Apothecary Exact Raw Assets

Raw files in this folder are untouched copies from the APX-00 source archive.

- Do not rename raw files.
- Do not optimize, crop, color-convert, or compress raw files.
- Use normalized role folders for stable runtime-facing names in later packets.
- If future packets need derived crops, keep those derived files outside `raw/` and document the reproducible derivation.
